{
  "design_version": 36,
  "status": "passed",
  "checks": [
    {"id":"cursor-fragments-declared","passed":true,"detail":"TASK-0130 CSS and UI-only controller are exact additive fragments"},
    {"id":"existing-menu-host-reused","passed":true,"detail":"Курсор and Два курсора are inserted before existing graph help in display-overflow-menu"},
    {"id":"plotly-host-preserved","passed":true,"detail":"cursor layer is a sibling inside plot-canvas; data/fullLayout are read-only and host descendants are untouched"},
    {"id":"frontend-state-only","passed":true,"detail":"off/single/dual mode and positions are keyed by paneRuntimeKey with no API, DSP, autosave, session or state revision"},
    {"id":"linked-axes-independent","passed":true,"detail":"relayout clamps only cursors already owned by that pane and never propagates cursor mode or positions"},
    {"id":"prototype-bridge-excluded","passed":true,"detail":"task-0130 prototype bridge and walkthrough are design-only"},
    {"id":"immutable-production-document","passed":true,"detail":"public/index.html remains immutable; runtime menu installation is owned by existing app boot"}
  ]
}
