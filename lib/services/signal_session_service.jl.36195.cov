        - const SIGNAL_ANALYSER_SESSION_DOCUMENT_FIELDS = Set([
        -     "schema",
        -     "version",
        -     "source_revision",
        -     "state",
        - ])
        - const SIGNAL_ANALYSER_SESSION_STATE_FIELDS = Set([
        -     "signals",
        -     "row_selected_signal",
        -     "displays",
        -     "active_display_id",
        -     "next_display_number",
        - ])
        - const SIGNAL_ANALYSER_SESSION_SIGNAL_FIELDS = Set([
        -     "name",
        -     "color",
        -     "sample_rate_hz",
        -     "is_complex",
        -     "visible",
        -     "values",
        - ])
        - const SIGNAL_ANALYSER_SESSION_VALUES_FIELDS = Set(["real", "imag"])
        - const SIGNAL_ANALYSER_SESSION_DISPLAY_FIELDS = Set([
        -     "id",
        -     "name",
        -     "active_plot",
        -     "analysis_signal",
        -     "visible_signals",
        -     "time_limits",
        -     "measurement_kinds",
        -     "spectrum_settings",
        -     "spectrogram_settings",
        -     "persistence_settings",
        -     "stored_settings",
        -     "peaks_enabled",
        - ])
        - const SIGNAL_ANALYSER_SESSION_DISPLAY_WITH_LAYOUT_FIELDS =
        -     union(SIGNAL_ANALYSER_SESSION_DISPLAY_FIELDS, Set(["layout"]))
        - const SIGNAL_ANALYSER_SESSION_LAYOUT_FIELDS = Set([
        -     "version",
        -     "variant",
        -     "rows",
        -     "columns",
        -     "active_pane_id",
        -     "next_pane_number",
        -     "panes",
        - ])
        - const SIGNAL_ANALYSER_SESSION_PANE_FIELDS = Set([
        -     "id",
        -     "plot_type",
        -     "signal_bindings",
        -     "peaks_settings",
        - ])
        - const SIGNAL_ANALYSER_SESSION_LEGACY_PANE_FIELDS = Set([
        -     "id",
        -     "plot_type",
        -     "signal_bindings",
        - ])
        - const SIGNAL_ANALYSER_SESSION_PEAKS_SETTINGS_FIELDS_V1 = Set([
        -     "number_of_peaks",
        -     "minimum_height",
        -     "minimum_distance_samples",
        -     "threshold",
        - ])
        - const SIGNAL_ANALYSER_SESSION_PEAKS_SETTINGS_FIELDS_V2 =
        -     union(SIGNAL_ANALYSER_SESSION_PEAKS_SETTINGS_FIELDS_V1, Set(["mode"]))
        - const SIGNAL_ANALYSER_SESSION_PEAKS_SETTINGS_FIELDS = Set([
        -     "mode",
        -     "number_of_peaks",
        -     "maximum_cutoff",
        -     "minimum_cutoff",
        -     "minimum_distance_samples",
        -     "threshold",
        - ])
        - const SIGNAL_ANALYSER_SESSION_REQUEST_FIELDS = Set(["state_revision", "document"])
        - const SIGNAL_ANALYSER_SESSION_STORED_SETTING_IDS = (
        -     "display.show_legend",
        -     "time.normalize_y",
        -     "time.show_markers",
        -     "time.units",
        -     "time.y_limits",
        -     "time.link_time",
        -     "spectrum.frequency_units",
        -     "spectrum.y_limits",
        -     "spectrum.resolution_type",
        -     "spectrum.rbw",
        -     "spectrum.window_length",
        -     "spectrum.window",
        -     "spectrum.sidelobe_attenuation_db",
        -     "spectrum.overlap_percent",
        -     "spectrum.nfft",
        -     "spectrogram.time_units",
        -     "spectrogram.frequency_units",
        -     "spectrogram.scale",
        -     "spectrogram.time_resolution",
        -     "spectrogram.reassign",
        -     "persistence.time_units",
        -     "persistence.frequency_units",
        -     "persistence.frequency_limits",
        -     "persistence.power_limits",
        -     "persistence.density_limits",
        -     "persistence.frequency_scale",
        -     "persistence.scale",
        -     "persistence.time_resolution",
        -     "persistence.overlap_percent",
        -     "persistence.power_bins",
        - )
        - const SIGNAL_ANALYSER_SESSION_STORED_SETTING_FIELDS =
        -     Set(SIGNAL_ANALYSER_SESSION_STORED_SETTING_IDS)
        - const SIGNAL_ANALYSER_SESSION_MAX_SIGNALS = 256
        - const SIGNAL_ANALYSER_SESSION_MAX_DISPLAYS = 128
        - const SIGNAL_ANALYSER_SESSION_MAX_TOTAL_SAMPLES = 5_000_000
        - const SIGNAL_ANALYSER_SESSION_MAX_TEXT_LENGTH = 1024
        - const SIGNAL_ANALYSER_SESSION_DISPLAY_ID_REGEX = r"^display-[1-9][0-9]*$"
        - 
        - """Coordinates versioned session serialization and atomic aggregate replacement."""
        7 struct SignalAnalyserSessionService end
        - 
        0 function signal_analyser_session_error(
        -     path::AbstractString,
        -     message::AbstractString;
        -     code::AbstractString = "invalid_session",
        - )::SignalAnalyserSessionValidationError
        0     SignalAnalyserSessionValidationError(
        -         String(code),
        -         "Некорректный документ сессии",
        -         Dict(String(path) => String(message)),
        -     )
        - end
        - 
      270 function signal_analyser_session_exact_object(
        -     value,
        -     fields::Set{String},
        -     path::AbstractString,
        - )::AbstractDict
      270     value isa AbstractDict || throw(signal_analyser_session_error(path, "Требуется JSON-объект"))
      270     actual = signal_analyser_payload_keys(value)
      270     actual == fields || begin
        0         missing = sort!(collect(setdiff(fields, actual)))
        0         unknown = sort!(collect(setdiff(actual, fields)))
        0         details = String[]
        0         isempty(missing) || push!(details, "отсутствуют: $(join(missing, ", "))")
        0         isempty(unknown) || push!(details, "неизвестны: $(join(unknown, ", "))")
        0         throw(signal_analyser_session_error(path, join(details, "; ")))
        -     end
      270     value
        - end
        - 
      303 function signal_analyser_session_string(
        -     value,
        -     path::AbstractString;
        -     allow_empty::Bool = false,
        - )::String
      303     value isa AbstractString || throw(signal_analyser_session_error(path, "Требуется строка"))
      303     result = String(value)
      303     (!allow_empty && isempty(result)) && throw(signal_analyser_session_error(path, "Строка не может быть пустой"))
      303     ncodeunits(result) <= SIGNAL_ANALYSER_SESSION_MAX_TEXT_LENGTH || throw(
        -         signal_analyser_session_error(path, "Строка слишком длинная"),
        -     )
      303     result
        - end
        - 
      268 function signal_analyser_session_integer(
        -     value,
        -     path::AbstractString;
        -     minimum::Int = 0,
        - )::Int
      268     value isa Integer && !(value isa Bool) || throw(
        -         signal_analyser_session_error(path, "Требуется целое число"),
        -     )
      268     result = try
      268         Int(value)
        -     catch err
        0         (err isa InexactError || err isa OverflowError) || rethrow()
        0         throw(signal_analyser_session_error(path, "Целое число вне диапазона Int"))
        -     end
      268     result >= minimum || throw(signal_analyser_session_error(path, "Минимальное значение: $minimum"))
      268     result
        - end
        - 
     9342 function signal_analyser_session_float(value, path::AbstractString)::Float64
     9342     value isa Real && !(value isa Bool) || throw(
        -         signal_analyser_session_error(path, "Требуется число, но не Bool"),
        -     )
     9342     result = try
     9342         Float64(value)
        -     catch err
        0         (err isa InexactError || err isa OverflowError) || rethrow()
        0         throw(signal_analyser_session_error(path, "Число вне диапазона Float64"))
        -     end
     9342     isfinite(result) || throw(signal_analyser_session_error(path, "Требуется конечное число"))
     9342     result == 0.0 ? 0.0 : result
        - end
        - 
        - function signal_analyser_session_settings_payload(
        -     display::SignalAnalyserDisplayState,
        - )::Dict{String,Any}
       12     service = SignalSettingsService()
       12     Dict{String,Any}(
        -         field_id => signal_settings_field_value(service, display, field_id)
        -         for field_id in SIGNAL_ANALYSER_SESSION_STORED_SETTING_IDS
        -     )
        - end
        - 
       20 function signal_analyser_session_signal_payload(signal::AnalysedSignal)::Dict{String,Any}
       20     Dict{String,Any}(
        -         "name" => signal.name,
        -         "color" => signal.color,
        -         "sample_rate_hz" => signal.sample_rate_hz,
        -         "is_complex" => signal.is_complex,
        -         "visible" => signal.visible,
        -         "values" => Dict{String,Any}(
        -             "real" => Float64[real(value) for value in signal.values],
        -             "imag" => Float64[imag(value) for value in signal.values],
        -         ),
        -     )
        - end
        - 
       12 function signal_analyser_session_display_payload(
        -     display::SignalAnalyserDisplayState,
        -     layout::SignalDisplayLayoutState,
        - )::Dict{String,Any}
       24     payload = Dict{String,Any}(
        -         "id" => display.id,
        -         "name" => display.name,
        -         "active_plot" => signal_analyser_plot_name(display.active_plot),
        -         "analysis_signal" => signal_analyser_display_analysis_name(display),
        -         "visible_signals" => signal_analyser_display_members(display),
        -         "time_limits" => signal_time_limits_payload(display.time_limits),
        -         "measurement_kinds" => signal_measurement_selection_payload(display.measurement_selection),
        -         "spectrum_settings" => signal_spectrum_settings_payload(display.spectrum_settings),
        -         "spectrogram_settings" => signal_spectrogram_settings_payload(display.spectrogram_settings),
        -         "persistence_settings" => signal_persistence_settings_payload(display.persistence_settings),
        -         "stored_settings" => signal_analyser_session_settings_payload(display),
        -         "peaks_enabled" => display.peaks_enabled,
        -     )
       12     payload["layout"] = signal_display_layout_payload(layout)
       12     payload
        - end
        - 
        - function signal_analyser_session_document_unlocked(
        -     state::SignalAnalyserState,
        - )::SignalAnalyserSessionDocument
       24     SignalAnalyserSessionDocument(
        -         SIGNAL_ANALYSER_SESSION_SCHEMA,
        -         SIGNAL_ANALYSER_SESSION_VERSION,
        -         state.view.state_revision,
        -         copy(state.signals),
        -         state.row_selection,
        -         copy(state.displays),
        -         Dict(
        -             display_id => copy(layout)
        -             for (display_id, layout) in state.display_layouts
        -         ),
        -         state.active_display_id,
        -         state.next_display_number,
        -     )
        - end
        - 
        - function signal_analyser_session_payload(
        -     document::SignalAnalyserSessionDocument,
        - )::Dict{String,Any}
       12     Dict{String,Any}(
        -         "schema" => document.schema,
        -         "version" => document.version,
        -         "source_revision" => document.source_revision,
        -         "state" => Dict{String,Any}(
        -             "signals" => [
        -                 signal_analyser_session_signal_payload(signal) for signal in document.signals
        -             ],
        -             "row_selected_signal" => document.row_selection.signal_name,
        -             "displays" => [
        -                 signal_analyser_session_display_payload(
        -                     display,
        -                     document.display_layouts[display.id],
        -                 ) for display in document.displays
        -             ],
        -             "active_display_id" => document.active_display_id,
        -             "next_display_number" => document.next_display_number,
        -         ),
        -     )
        - end
        - 
       12 function export_signal_analyser_session(
        -     ::SignalAnalyserSessionService,
        -     state::SignalAnalyserState,
        - )::Dict{String,Any}
       12     lock(state.lock) do
       24         document = signal_analyser_session_document_unlocked(state)
       12         Dict{String,Any}(
        -             "ok" => true,
        -             "document" => signal_analyser_session_payload(document),
        -         )
        -     end
        - end
        - 
       10 function signal_analyser_session_parse_signal(
        -     value,
        -     index::Int,
        -     maximum_samples::Int,
        - )::AnalysedSignal
       10     path = "document.state.signals[$index]"
       10     data = signal_analyser_session_exact_object(
        -         value,
        -         SIGNAL_ANALYSER_SESSION_SIGNAL_FIELDS,
        -         path,
        -     )
       10     name = signal_analyser_session_string(signal_analyser_payload_value(data, "name"), "$path.name")
       10     color = signal_analyser_session_string(signal_analyser_payload_value(data, "color"), "$path.color")
       10     sample_rate_hz = signal_analyser_session_float(
        -         signal_analyser_payload_value(data, "sample_rate_hz"),
        -         "$path.sample_rate_hz",
        -     )
       10     sample_rate_hz > 0 || throw(signal_analyser_session_error(
        -         "$path.sample_rate_hz",
        -         "Частота дискретизации должна быть положительной",
        -     ))
       10     is_complex = signal_analyser_payload_value(data, "is_complex")
       10     is_complex isa Bool || throw(signal_analyser_session_error("$path.is_complex", "Требуется boolean"))
       10     visible = signal_analyser_payload_value(data, "visible")
       10     visible isa Bool || throw(signal_analyser_session_error("$path.visible", "Требуется boolean"))
       10     values = signal_analyser_session_exact_object(
        -         signal_analyser_payload_value(data, "values"),
        -         SIGNAL_ANALYSER_SESSION_VALUES_FIELDS,
        -         "$path.values",
        -     )
       10     real_values = signal_analyser_payload_value(values, "real")
       10     imag_values = signal_analyser_payload_value(values, "imag")
       10     real_values isa AbstractVector || throw(signal_analyser_session_error("$path.values.real", "Требуется массив"))
       10     imag_values isa AbstractVector || throw(signal_analyser_session_error("$path.values.imag", "Требуется массив"))
       10     length(real_values) == length(imag_values) || throw(signal_analyser_session_error(
        -         "$path.values",
        -         "Массивы real и imag должны иметь одинаковую длину",
        -     ))
       10     length(real_values) >= 2 || throw(signal_analyser_session_error(
        -         "$path.values",
        -         "Сигнал должен содержать не менее двух отсчётов",
        -     ))
       10     length(real_values) <= maximum_samples || throw(signal_analyser_session_error(
        -         "$path.values",
        -         "Превышен общий лимит $(SIGNAL_ANALYSER_SESSION_MAX_TOTAL_SAMPLES) отсчётов",
        -     ))
       10     samples = Vector{ComplexF64}(undef, length(real_values))
       10     for sample_index in eachindex(real_values, imag_values)
     4611         real_part = signal_analyser_session_float(
        -             real_values[sample_index],
        -             "$path.values.real[$sample_index]",
        -         )
     4611         imag_part = signal_analyser_session_float(
        -             imag_values[sample_index],
        -             "$path.values.imag[$sample_index]",
        -         )
     4611         !is_complex && !iszero(imag_part) && throw(signal_analyser_session_error(
        -             "$path.values.imag[$sample_index]",
        -             "Вещественный сигнал не может содержать мнимую часть",
        -         ))
     4611         samples[sample_index] = ComplexF64(real_part, imag_part)
     4611     end
       10     AnalysedSignal(name, color, sample_rate_hz, samples, is_complex, visible)
        - end
        - 
        - signal_analyser_session_parse_signal(value, index::Int)::AnalysedSignal =
        -     signal_analyser_session_parse_signal(
        -         value,
        -         index,
        -         SIGNAL_ANALYSER_SESSION_MAX_TOTAL_SAMPLES,
        -     )
        - 
        6 function signal_analyser_session_parse_stored_settings(
        -     value,
        -     display::SignalAnalyserDisplayState,
        - )::SignalDisplayStoredSettings
        6     path = "document.state.displays.$(display.id).stored_settings"
        6     data = signal_analyser_session_exact_object(
        -         value,
        -         SIGNAL_ANALYSER_SESSION_STORED_SETTING_FIELDS,
        -         path,
        -     )
        6     stored = SignalDisplayStoredSettings()
        6     for field_id in SIGNAL_ANALYSER_SESSION_STORED_SETTING_IDS
      180         definition = signal_settings_field(SIGNAL_SETTINGS_CATALOG, field_id)::SignalSettingsFieldDefinition
      180         typed_value = try
      180             signal_settings_parse_field_value(
        -                 definition,
        -                 display.id,
        -                 field_id,
        -                 signal_analyser_payload_value(data, field_id),
        -             )
        -         catch err
        0             err isa SignalSettingValidationError || rethrow()
        0             throw(signal_analyser_session_error("$path.$field_id", err.message))
        -         end
      330         command = UpdateSignalSettingCommand(0, display.id, field_id, typed_value)
      180         stored = try
      180             signal_settings_apply_stored_value(stored, command)
        -         catch err
        0             (err isa ArgumentError || err isa SignalSettingValidationError) || rethrow()
        0             throw(signal_analyser_session_error("$path.$field_id", sprint(showerror, err)))
        -         end
      186     end
        6     stored
        - end
        - 
        6 function signal_analyser_session_parse_display(value, index::Int)::SignalAnalyserDisplayState
        6     path = "document.state.displays[$index]"
        6     value isa AbstractDict || throw(signal_analyser_session_error(path, "Требуется JSON-объект"))
        6     actual_fields = signal_analyser_payload_keys(value)
        6     actual_fields in (
        -         SIGNAL_ANALYSER_SESSION_DISPLAY_FIELDS,
        -         SIGNAL_ANALYSER_SESSION_DISPLAY_WITH_LAYOUT_FIELDS,
        -     ) || throw(signal_analyser_session_error(
        -         path,
        -         "Ожидался exact legacy Display или Display с полем layout",
        -     ))
        6     data = value
        6     id = signal_analyser_session_string(signal_analyser_payload_value(data, "id"), "$path.id")
        6     occursin(SIGNAL_ANALYSER_SESSION_DISPLAY_ID_REGEX, id) || throw(
        -         signal_analyser_session_error("$path.id", "Ожидался идентификатор display-N"),
        -     )
        6     name = signal_analyser_session_string(signal_analyser_payload_value(data, "name"), "$path.name")
        6     plot_value = signal_analyser_payload_value(data, "active_plot")
        6     plot_value isa AbstractString && haskey(SIGNAL_ANALYSER_PLOTS_BY_NAME, String(plot_value)) || throw(
        -         signal_analyser_session_error("$path.active_plot", "Допустимо: time, spectrum, spectrogram, persistence"),
        -     )
        6     active_plot = SIGNAL_ANALYSER_PLOTS_BY_NAME[String(plot_value)]
        6     visible_value = signal_analyser_payload_value(data, "visible_signals")
        6     visible_value isa AbstractVector || throw(signal_analyser_session_error("$path.visible_signals", "Требуется массив"))
        6     visible_signals = String[]
        6     for (signal_index, item) in enumerate(visible_value)
        5         push!(visible_signals, signal_analyser_session_string(item, "$path.visible_signals[$signal_index]"))
        5     end
        6     allunique(visible_signals) || throw(signal_analyser_session_error(
        -         "$path.visible_signals",
        -         "Имена сигналов не должны повторяться",
        -     ))
        6     analysis_value = signal_analyser_payload_value(data, "analysis_signal")
       10     analysis_signal = analysis_value === nothing ? nothing :
        -         signal_analyser_session_string(analysis_value, "$path.analysis_signal")
        6     field_errors = Dict{String,String}()
        6     time_limits = signal_analyser_validate_time_limits!(
        -         field_errors,
        -         signal_analyser_payload_value(data, "time_limits"),
        -     )
        6     measurement_selection = signal_analyser_validate_measurement_kinds!(
        -         field_errors,
        -         signal_analyser_payload_value(data, "measurement_kinds"),
        -     )
        6     spectrum_settings = signal_analyser_validate_spectrum_settings!(
        -         field_errors,
        -         signal_analyser_payload_value(data, "spectrum_settings"),
        -     )
        6     spectrogram_settings = signal_analyser_validate_spectrogram_settings!(
        -         field_errors,
        -         signal_analyser_payload_value(data, "spectrogram_settings"),
        -     )
        6     persistence_settings = signal_analyser_validate_persistence_settings!(
        -         field_errors,
        -         signal_analyser_payload_value(data, "persistence_settings"),
        -     )
        6     isempty(field_errors) || throw(SignalAnalyserSessionValidationError(
        -         "invalid_session",
        -         "Некорректный документ сессии",
        -         Dict("$path.$field" => message for (field, message) in field_errors),
        -     ))
        6     peaks_enabled = signal_analyser_payload_value(data, "peaks_enabled")
        6     peaks_enabled isa Bool || throw(signal_analyser_session_error("$path.peaks_enabled", "Требуется boolean"))
        6     base_display = try
        6         SignalAnalyserDisplayState(
        -             id,
        -             name,
        -             active_plot,
        -             analysis_signal,
        -             visible_signals,
        -             time_limits,
        -             measurement_selection::SignalMeasurementSelection,
        -             spectrum_settings::SignalSpectrumSettings,
        -             spectrogram_settings::SignalSpectrogramSettings,
        -             persistence_settings::SignalPersistenceSettings,
        -             peaks_enabled,
        -         )
        -     catch err
        0         err isa ArgumentError || rethrow()
        0         throw(signal_analyser_session_error(path, sprint(showerror, err)))
        -     end
        6     stored_settings = signal_analyser_session_parse_stored_settings(
        -         signal_analyser_payload_value(data, "stored_settings"),
        -         base_display,
        -     )
        6     try
        6         SignalAnalyserDisplayState(
        -             id,
        -             name,
        -             active_plot,
        -             base_display.membership,
        -             base_display.analysis_source,
        -             time_limits,
        -             measurement_selection::SignalMeasurementSelection,
        -             spectrum_settings::SignalSpectrumSettings,
        -             spectrogram_settings::SignalSpectrogramSettings,
        -             persistence_settings::SignalPersistenceSettings,
        -             stored_settings,
        -             peaks_enabled,
        -         )
        -     catch err
        0         err isa ArgumentError || rethrow()
        0         throw(signal_analyser_session_error(path, sprint(showerror, err)))
        -     end
        - end
        - 
      110 function signal_analyser_session_parse_layout_pane(
        -     value,
        -     display::SignalAnalyserDisplayState,
        -     active_pane_id::String,
        -     signals::AbstractVector{AnalysedSignal},
        -     display_index::Int,
        -     pane_index::Int,
        -     session_version::Int,
        - )::SignalDisplayPaneState
      110     path = "document.state.displays[$display_index].layout.panes[$pane_index]"
      110     value isa AbstractDict || throw(signal_analyser_session_error(path, "Требуется JSON-объект"))
      110     pane_fields = signal_analyser_payload_keys(value)
      110     is_legacy_pane = pane_fields == SIGNAL_ANALYSER_SESSION_LEGACY_PANE_FIELDS
      110     is_legacy_pane && session_version != SIGNAL_ANALYSER_LEGACY_SESSION_VERSION && throw(
        -         signal_analyser_session_error(path, "Session v2+ требует peaks_settings для каждой pane"),
        -     )
      220     data = is_legacy_pane ? value :
        -         signal_analyser_session_exact_object(value, SIGNAL_ANALYSER_SESSION_PANE_FIELDS, path)
      110     pane_id = signal_analyser_session_string(
        -         signal_analyser_payload_value(data, "id"),
        -         "$path.id",
        -     )
      110     occursin(SIGNAL_DISPLAY_PANE_ID_REGEX, pane_id) || throw(
        -         signal_analyser_session_error("$path.id", "Ожидался идентификатор pane-N"),
        -     )
      110     plot_value = signal_analyser_payload_value(data, "plot_type")
      110     plot_value isa AbstractString && haskey(SIGNAL_ANALYSER_PLOTS_BY_NAME, String(plot_value)) ||
        -         throw(signal_analyser_session_error(
        -             "$path.plot_type",
        -             "Допустимо: time, spectrum, spectrogram, persistence",
        -         ))
      110     bindings_value = signal_analyser_payload_value(data, "signal_bindings")
      110     bindings_value isa AbstractVector || throw(signal_analyser_session_error(
        -         "$path.signal_bindings",
        -         "Требуется массив",
        -     ))
      110     bindings = String[
        -         signal_analyser_session_string(
        -             item,
        -             "$path.signal_bindings[$binding_index]",
        -         )
        -         for (binding_index, item) in enumerate(bindings_value)
        -     ]
      110     allunique(bindings) || throw(signal_analyser_session_error(
        -         "$path.signal_bindings",
        -         "Имена сигналов не должны повторяться",
        -     ))
      110     plot_type = SIGNAL_ANALYSER_PLOTS_BY_NAME[String(plot_value)]
      110     peaks_settings = if is_legacy_pane
        0         SignalPeaksSettings()
        -     else
      219         expected_settings_fields = session_version == SIGNAL_ANALYSER_LEGACY_SESSION_VERSION ?
        -             SIGNAL_ANALYSER_SESSION_PEAKS_SETTINGS_FIELDS_V1 :
        -             session_version == SIGNAL_ANALYSER_EXTREMA_SESSION_VERSION ?
        -                 SIGNAL_ANALYSER_SESSION_PEAKS_SETTINGS_FIELDS_V2 :
        -                 SIGNAL_ANALYSER_SESSION_PEAKS_SETTINGS_FIELDS
      110         peaks_settings_data = signal_analyser_session_exact_object(
        -             signal_analyser_payload_value(data, "peaks_settings"),
        -             expected_settings_fields,
        -             "$path.peaks_settings",
        -         )
      110         mode = if session_version == SIGNAL_ANALYSER_LEGACY_SESSION_VERSION
        1             MAXIMA_EXTREMA_MODE
        -         else
      109             mode_value = signal_analyser_session_string(
        -                 signal_analyser_payload_value(peaks_settings_data, "mode"),
        -                 "$path.peaks_settings.mode",
        -             )
      109             haskey(SIGNAL_EXTREMA_MODES_BY_NAME, mode_value) || throw(
        -                 signal_analyser_session_error(
        -                     "$path.peaks_settings.mode",
        -                     "Допустимо: maxima, minima, all",
        -                 ),
        -             )
      219             SIGNAL_EXTREMA_MODES_BY_NAME[mode_value]
        -         end
      110         number_of_peaks = signal_analyser_session_integer(
        -             signal_analyser_payload_value(peaks_settings_data, "number_of_peaks"),
        -             "$path.peaks_settings.number_of_peaks",
        -             minimum = 1,
        -         )
      110         number_of_peaks <= SIGNAL_PEAKS_MAX_NUMBER_OF_PEAKS || throw(
        -             signal_analyser_session_error(
        -                 "$path.peaks_settings.number_of_peaks",
        -                 "Максимальное значение: $(SIGNAL_PEAKS_MAX_NUMBER_OF_PEAKS)",
        -             ),
        -         )
      329         parse_cutoff = function (field_id::String)
      219             value = signal_analyser_payload_value(peaks_settings_data, field_id)
      219             value === nothing ? nothing : signal_analyser_session_float(
        -                 value,
        -                 "$path.peaks_settings.$field_id",
        -             )
        -         end
      110         maximum_cutoff, minimum_cutoff = if session_version == SIGNAL_ANALYSER_SESSION_VERSION
      109             parse_cutoff("maximum_cutoff"), parse_cutoff("minimum_cutoff")
        -         else
        1             legacy_height = parse_cutoff("minimum_height")
      111             mode == MINIMA_EXTREMA_MODE ?
        -                 (nothing, legacy_height === nothing ? nothing : -legacy_height) :
        -                 mode == ALL_EXTREMA_MODE ?
        -                     (legacy_height, legacy_height === nothing ? nothing : -legacy_height) :
        -                     (legacy_height, nothing)
        -         end
      110         minimum_distance_samples = signal_analyser_session_integer(
        -             signal_analyser_payload_value(peaks_settings_data, "minimum_distance_samples"),
        -             "$path.peaks_settings.minimum_distance_samples",
        -             minimum = 1,
        -         )
      110         threshold = signal_analyser_session_float(
        -             signal_analyser_payload_value(peaks_settings_data, "threshold"),
        -             "$path.peaks_settings.threshold",
        -         )
      110         threshold >= 0 || throw(signal_analyser_session_error(
        -             "$path.peaks_settings.threshold",
        -             "Требуется неотрицательное число",
        -         ))
      220         SignalPeaksSettings(
        -             mode,
        -             number_of_peaks,
        -             maximum_cutoff,
        -             minimum_cutoff,
        -             minimum_distance_samples,
        -             threshold,
        -         )
        -     end
      110     display_members = signal_analyser_display_members(display)
      110     if pane_id == active_pane_id && plot_type == display.active_plot &&
        -         Set(bindings) == Set(display_members)
        6         return try
        6             SignalDisplayPaneState(
        -                 pane_id,
        -                 plot_type,
        -                 SignalDisplayMembership(bindings),
        -                 display.analysis_source,
        -                 display.time_limits,
        -                 display.measurement_selection,
        -                 display.spectrum_settings,
        -                 display.spectrogram_settings,
        -                 display.persistence_settings,
        -                 display.stored_settings,
        -                 display.peaks_enabled,
        -                 peaks_settings,
        -             )
        -         catch err
        0             err isa ArgumentError || rethrow()
        0             throw(signal_analyser_session_error(path, sprint(showerror, err)))
        -         end
        -     end
      104     known_names = Set(signal.name for signal in signals)
      120     unknown_name = findfirst(name -> !(name in known_names), bindings)
      104     unknown_name === nothing || throw(signal_analyser_session_error(
        -         "$path.signal_bindings[$unknown_name]",
        -         "Pane ссылается на неизвестный сигнал",
        -     ))
      109     analysis_signal = isempty(bindings) ? nothing : signals[
        9         findfirst(signal -> signal.name == first(bindings), signals)::Int
        -     ]
      104     try
      104         SignalDisplayPaneState(
        -             pane_id,
        -             plot_type,
        -             SignalDisplayMembership(bindings),
        -             signal_analysis_source(analysis_signal === nothing ? nothing : analysis_signal.name),
        -             analysis_signal === nothing ? nothing : signal_full_time_limits(
        -                 SignalMeasurementsService(),
        -                 analysis_signal,
        -             ),
        -             SignalMeasurementSelection(),
        -             SignalSpectrumSettings(),
        -             SignalSpectrogramSettings(),
        -             SignalPersistenceSettings(),
        -             SignalDisplayStoredSettings(),
        -             false,
        -             peaks_settings,
        -         )
        -     catch err
        0         err isa ArgumentError || rethrow()
        0         throw(signal_analyser_session_error(path, sprint(showerror, err)))
        -     end
        - end
        - 
        6 function signal_analyser_session_parse_layout(
        -     value,
        -     display::SignalAnalyserDisplayState,
        -     signals::AbstractVector{AnalysedSignal},
        -     display_index::Int,
        -     session_version::Int,
        - )::SignalDisplayLayoutState
        6     value === nothing && return signal_display_default_layout(display)
        6     path = "document.state.displays[$display_index].layout"
        6     data = signal_analyser_session_exact_object(
        -         value,
        -         SIGNAL_ANALYSER_SESSION_LAYOUT_FIELDS,
        -         path,
        -     )
        6     layout_version = signal_analyser_session_integer(
        -         signal_analyser_payload_value(data, "version"),
        -         "$path.version",
        -         minimum = 1,
        -     )
        6     layout_version == SIGNAL_DISPLAY_LAYOUT_VERSION || throw(signal_analyser_session_error(
        -         "$path.version",
        -         "Поддерживается только layout version 1",
        -     ))
        6     rows = signal_analyser_session_integer(
        -         signal_analyser_payload_value(data, "rows"),
        -         "$path.rows",
        -         minimum = SIGNAL_DISPLAY_LAYOUT_MIN_DIMENSION,
        -     )
        6     rows <= SIGNAL_DISPLAY_LAYOUT_MAX_DIMENSION || throw(signal_analyser_session_error(
        -         "$path.rows",
        -         "Максимальное значение: $(SIGNAL_DISPLAY_LAYOUT_MAX_DIMENSION)",
        -     ))
        6     columns = signal_analyser_session_integer(
        -         signal_analyser_payload_value(data, "columns"),
        -         "$path.columns",
        -         minimum = SIGNAL_DISPLAY_LAYOUT_MIN_DIMENSION,
        -     )
        6     columns <= SIGNAL_DISPLAY_LAYOUT_MAX_DIMENSION || throw(signal_analyser_session_error(
        -         "$path.columns",
        -         "Максимальное значение: $(SIGNAL_DISPLAY_LAYOUT_MAX_DIMENSION)",
        -     ))
        6     variant = signal_analyser_session_string(
        -         signal_analyser_payload_value(data, "variant"),
        -         "$path.variant",
        -     )
        6     variant == signal_display_layout_variant(rows, columns) || throw(
        -         signal_analyser_session_error(
        -             "$path.variant",
        -             "Требуется canonical variant $(signal_display_layout_variant(rows, columns))",
        -         ),
        -     )
        6     panes_value = signal_analyser_payload_value(data, "panes")
        6     panes_value isa AbstractVector || throw(signal_analyser_session_error(
        -         "$path.panes",
        -         "Требуется массив",
        -     ))
        6     length(panes_value) == rows * columns || throw(signal_analyser_session_error(
        -         "$path.panes",
        -         "Число panes должно совпадать с topology layout",
        -     ))
        6     active_pane_id = signal_analyser_session_string(
        -         signal_analyser_payload_value(data, "active_pane_id"),
        -         "$path.active_pane_id",
        -     )
        6     panes = SignalDisplayPaneState[
        -         signal_analyser_session_parse_layout_pane(
        -             item,
        -             display,
        -             active_pane_id,
        -             signals,
        -             display_index,
        -             pane_index,
        -             session_version,
        -         )
        -         for (pane_index, item) in enumerate(panes_value)
        -     ]
        6     next_pane_number = signal_analyser_session_integer(
        -         signal_analyser_payload_value(data, "next_pane_number"),
        -         "$path.next_pane_number",
        -         minimum = 2,
        -     )
        6     try
        6         SignalDisplayLayoutState(
        -             layout_version,
        -             variant,
        -             rows,
        -             columns,
        -             panes,
        -             active_pane_id,
        -             next_pane_number,
        -         )
        -     catch err
        0         err isa ArgumentError || rethrow()
        0         throw(signal_analyser_session_error(path, sprint(showerror, err)))
        -     end
        - end
        - 
        6 function signal_analyser_session_validate_candidate!(
        -     state::SignalAnalyserState,
        - )::Nothing
        6     known_names = Set(signal.name for signal in state.signals)
        6     state.row_selection.signal_name in known_names || throw(signal_analyser_session_error(
        -         "document.state.row_selected_signal",
        -         "Выбранный сигнал отсутствует в inventory",
        -     ))
       12     display_ids = [display.id for display in state.displays]
        6     allunique(display_ids) || throw(signal_analyser_session_error(
        -         "document.state.displays",
        -         "Идентификаторы Display не должны повторяться",
        -     ))
        6     Set(keys(state.display_layouts)) == Set(display_ids) || throw(
        -         signal_analyser_session_error(
        -             "document.state.displays",
        -             "Каждый Display должен иметь ровно один layout",
        -         ),
        -     )
        6     state.active_display_id in display_ids || throw(signal_analyser_session_error(
        -         "document.state.active_display_id",
        -         "Active Display отсутствует в displays",
        -     ))
        6     display_numbers = Int[]
        6     for id in display_ids
        6         display_number = tryparse(Int, split(id, '-')[2])
        6         display_number === nothing && throw(signal_analyser_session_error(
        -             "document.state.displays.$id.id",
        -             "Номер Display находится вне диапазона Int",
        -         ))
        6         push!(display_numbers, display_number)
        6     end
        6     maximum_display_number = maximum(display_numbers)
        6     state.next_display_number > maximum_display_number || throw(signal_analyser_session_error(
        -         "document.state.next_display_number",
        -         "Значение должно быть больше номера любого сохранённого Display",
        -     ))
        6     for display in state.displays
        6         path = "document.state.displays.$(display.id)"
        6         layout = signal_analyser_layout_by_display_id(state, display.id)
        6         for pane in layout.panes
      136             all(name -> name in known_names, signal_display_pane_members(pane)) || throw(
        -                 signal_analyser_session_error(
        -                     "$path.layout.panes.$(pane.id).signal_bindings",
        -                     "Pane ссылается на неизвестный сигнал",
        -                 ),
        -             )
      110         end
        6         active_pane = signal_display_active_pane(layout)
        6         members = signal_analyser_display_members(display)
       16         all(name -> name in known_names, members) || throw(signal_analyser_session_error(
        -             "$path.visible_signals",
        -             "Display ссылается на неизвестный сигнал",
        -         ))
        6         members == signal_analyser_inventory_ordered_names(state, members) || throw(
        -             signal_analyser_session_error(
        -                 "$path.visible_signals",
        -                 "Порядок должен совпадать с authoritative inventory",
        -             ),
        -         )
        6         active_pane.plot_type == display.active_plot || throw(
        -             signal_analyser_session_error(
        -                 "$path.layout.active_pane_id",
        -                 "Plot type active pane не совпадает с legacy Display projection",
        -             ),
        -         )
        6         Set(signal_display_pane_members(active_pane)) == Set(members) || throw(
        -             signal_analyser_session_error(
        -                 "$path.layout.active_pane_id",
        -                 "Состав bindings active pane не совпадает с legacy Display projection",
        -             ),
        -         )
       10         analysis_name = signal_analyser_display_analysis_name(display)
       10         analysis_signal = analysis_name === nothing ? nothing : signal_by_name(state, analysis_name)
        6         if analysis_signal !== nothing
        4             signal_time_limits_are_valid(
        -                 state.measurements_service,
        -                 analysis_signal,
        -                 display.time_limits::SignalTimeLimits,
        -             ) || throw(signal_analyser_session_error(
        -                 "$path.time_limits",
        -                 "Интервал должен содержать отсчёт и лежать в домене analysis source",
        -             ))
        4             for (settings_path, limits) in (
        -                 ("spectrum_settings.frequency_limits", display.spectrum_settings.frequency_limits),
        -                 ("spectrogram_settings.frequency_limits", display.spectrogram_settings.frequency_limits),
        -             )
        8                 limits isa ExplicitSignalSpectrumFrequencyLimits || continue
        0                 signal_spectrum_frequency_limits_valid_for_signal(limits, analysis_signal) || throw(
        -                     signal_analyser_session_error(
        -                         "$path.$settings_path",
        -                         "Интервал лежит вне частотного домена analysis source",
        -                     ),
        -                 )
       12             end
        4             persistence_limits = display.stored_settings.persistence.frequency_limits
        4             if persistence_limits !== nothing
        0                 limits = ExplicitSignalSpectrumFrequencyLimits(
        -                     persistence_limits.minimum,
        -                     persistence_limits.maximum,
        -                 )
        0                 signal_spectrum_frequency_limits_valid_for_signal(limits, analysis_signal) || throw(
        -                     signal_analyser_session_error(
        -                         "$path.stored_settings.persistence.frequency_limits",
        -                         "Интервал лежит вне частотного домена analysis source",
        -                     ),
        -                 )
        -             end
        4             resolution = display.stored_settings.spectrogram.time_resolution
        4             if resolution.mode == SPECIFIED_SIGNAL_SETTING
        0                 (resolution.seconds::Float64) <= signal_duration_s(analysis_signal) || throw(
        -                     signal_analyser_session_error(
        -                         "$path.stored_settings.spectrogram.time_resolution",
        -                         "Time resolution превышает duration analysis source",
        -                     ),
        -                 )
        -             end
        -         else
        2             display.stored_settings.persistence.frequency_limits === nothing || throw(
        -                 signal_analyser_session_error(
        -                     "$path.stored_settings.persistence.frequency_limits",
        -                     "Явный интервал требует analysis source",
        -                 ),
        -             )
        2             display.stored_settings.spectrogram.time_resolution.mode == AUTOMATIC_SIGNAL_SETTING || throw(
        -                 signal_analyser_session_error(
        -                     "$path.stored_settings.spectrogram.time_resolution",
        -                     "Specified resolution требует analysis source",
        -                 ),
        -             )
        2             display.stored_settings.persistence.time_resolution.mode == AUTOMATIC_SIGNAL_SETTING || throw(
        -                 signal_analyser_session_error(
        -                     "$path.stored_settings.persistence.time_resolution",
        -                     "Specified resolution требует analysis source",
        -                 ),
        -             )
        -         end
        7         if display.spectrum_settings.frequency_scale == LOG_SPECTRUM_FREQUENCY_SCALE &&
        1             any(signal -> signal.is_complex && signal.name in members, state.signals)
        0             throw(signal_analyser_session_error(
        -                 "$path.spectrum_settings.frequency_scale",
        -                 "Log scale недоступна для Display с комплексным сигналом",
        -             ))
        -         end
        6         try
        6             SignalSpectrogramPresentationSettings(
        -                 display.stored_settings.spectrogram.scale,
        -                 display.spectrogram_settings.power_limits,
        -             )
        -         catch err
        0             err isa ArgumentError || rethrow()
        0             throw(signal_analyser_session_error(
        -                 "$path.spectrogram_settings.power_limits",
        -                 sprint(showerror, err),
        -             ))
        -         end
        6     end
        6     active_display = signal_analyser_active_display(state)
        6     active_members = Set(active_display.membership.signal_names)
       16     all(signal -> signal.visible == (signal.name in active_members), state.signals) || throw(
        -         signal_analyser_session_error(
        -             "document.state.signals",
        -             "Поле visible должно совпадать с membership active Display",
        -         ),
        -     )
        6     nothing
        - end
        - 
        6 function parse_signal_analyser_session_document(value)::SignalAnalyserSessionDocument
        6     data = signal_analyser_session_exact_object(
        -         value,
        -         SIGNAL_ANALYSER_SESSION_DOCUMENT_FIELDS,
        -         "document",
        -     )
        6     schema = signal_analyser_session_string(
        -         signal_analyser_payload_value(data, "schema"),
        -         "document.schema",
        -     )
        6     schema == SIGNAL_ANALYSER_SESSION_SCHEMA || throw(signal_analyser_session_error(
        -         "document.schema",
        -         "Неподдерживаемая schema: $schema";
        -         code = "unsupported_session_schema",
        -     ))
        6     version = signal_analyser_session_integer(
        -         signal_analyser_payload_value(data, "version"),
        -         "document.version",
        -         minimum = 1,
        -     )
        6     version in (
        -         SIGNAL_ANALYSER_LEGACY_SESSION_VERSION,
        -         SIGNAL_ANALYSER_EXTREMA_SESSION_VERSION,
        -         SIGNAL_ANALYSER_SESSION_VERSION,
        -     ) || throw(signal_analyser_session_error(
        -         "document.version",
        -         "Неподдерживаемая версия: $version";
        -         code = "unsupported_session_version",
        -     ))
        6     source_revision = signal_analyser_session_integer(
        -         signal_analyser_payload_value(data, "source_revision"),
        -         "document.source_revision",
        -     )
        6     state_data = signal_analyser_session_exact_object(
        -         signal_analyser_payload_value(data, "state"),
        -         SIGNAL_ANALYSER_SESSION_STATE_FIELDS,
        -         "document.state",
        -     )
        6     signal_values = signal_analyser_payload_value(state_data, "signals")
        6     signal_values isa AbstractVector || throw(signal_analyser_session_error(
        -         "document.state.signals",
        -         "Требуется массив",
        -     ))
        6     1 <= length(signal_values) <= SIGNAL_ANALYSER_SESSION_MAX_SIGNALS || throw(
        -         signal_analyser_session_error(
        -             "document.state.signals",
        -             "Допустимо от 1 до $(SIGNAL_ANALYSER_SESSION_MAX_SIGNALS) сигналов",
        -         ),
        -     )
        6     signals = AnalysedSignal[]
        6     remaining_samples = SIGNAL_ANALYSER_SESSION_MAX_TOTAL_SAMPLES
        6     for (index, value) in enumerate(signal_values)
       10         signal = signal_analyser_session_parse_signal(value, index, remaining_samples)
       10         push!(signals, signal)
       10         remaining_samples -= length(signal.values)
       10     end
        6     allunique(signal.name for signal in signals) || throw(signal_analyser_session_error(
        -         "document.state.signals",
        -         "Имена сигналов не должны повторяться",
        -     ))
        6     row_selection = GlobalSignalSelection(signal_analyser_session_string(
        -         signal_analyser_payload_value(state_data, "row_selected_signal"),
        -         "document.state.row_selected_signal",
        -     ))
        6     display_values = signal_analyser_payload_value(state_data, "displays")
        6     display_values isa AbstractVector || throw(signal_analyser_session_error(
        -         "document.state.displays",
        -         "Требуется массив",
        -     ))
        6     1 <= length(display_values) <= SIGNAL_ANALYSER_SESSION_MAX_DISPLAYS || throw(
        -         signal_analyser_session_error(
        -             "document.state.displays",
        -             "Допустимо от 1 до $(SIGNAL_ANALYSER_SESSION_MAX_DISPLAYS) Display",
        -         ),
        -     )
        6     displays = SignalAnalyserDisplayState[]
        6     display_layouts = Dict{String,SignalDisplayLayoutState}()
        6     for (index, value) in enumerate(display_values)
        6         display = signal_analyser_session_parse_display(value, index)
        6         push!(displays, display)
        6         layout_value = signal_analyser_payload_contains(value, "layout") ?
        -             signal_analyser_payload_value(value, "layout") : nothing
        6         display_layouts[display.id] = signal_analyser_session_parse_layout(
        -             layout_value,
        -             display,
        -             signals,
        -             index,
        -             version,
        -         )
        6     end
        6     active_display_id = signal_analyser_session_string(
        -         signal_analyser_payload_value(state_data, "active_display_id"),
        -         "document.state.active_display_id",
        -     )
        6     next_display_number = signal_analyser_session_integer(
        -         signal_analyser_payload_value(state_data, "next_display_number"),
        -         "document.state.next_display_number",
        -         minimum = 2,
        -     )
        6     SignalAnalyserSessionDocument(
        -         schema,
        -         version,
        -         source_revision,
        -         signals,
        -         row_selection,
        -         displays,
        -         display_layouts,
        -         active_display_id,
        -         next_display_number,
        -     )
        - end
        - 
        6 function parse_import_signal_analyser_session_command(data)::ImportSignalAnalyserSessionCommand
        6     request = signal_analyser_session_exact_object(
        -         data,
        -         SIGNAL_ANALYSER_SESSION_REQUEST_FIELDS,
        -         "body",
        -     )
        6     expected_revision = signal_analyser_session_integer(
        -         signal_analyser_payload_value(request, "state_revision"),
        -         "state_revision",
        -     )
        6     document = parse_signal_analyser_session_document(
        -         signal_analyser_payload_value(request, "document"),
        -     )
        6     ImportSignalAnalyserSessionCommand(expected_revision, document)
        - end
        - 
        6 function signal_analyser_session_candidate(
        -     current::SignalAnalyserState,
        -     document::SignalAnalyserSessionDocument,
        -     revision::Int,
        - )::SignalAnalyserState
       18     active_index = findfirst(display -> display.id == document.active_display_id, document.displays)
        6     active_index === nothing && throw(signal_analyser_session_error(
        -         "document.state.active_display_id",
        -         "Active Display отсутствует в displays",
        -     ))
        6     active_display = document.displays[active_index]
        6     candidate = try
       10         SignalAnalyserState(
        -             copy(document.signals),
        -             SignalAnalyserViewState(
        -                 revision,
        -                 active_display.active_plot,
        -                 signal_analyser_display_analysis_name(active_display),
        -             ),
        -             Dict{String,Dict{String,Any}}(),
        -             ReentrantLock(),
        -             peaks_provider = current.peaks_service.provider,
        -             spectrum_provider = current.spectrum_service.provider,
        -             spectrogram_provider = current.spectrogram_service.provider,
        -             persistence_provider = current.persistence_service.provider,
        -         )
        -     catch err
        0         err isa ArgumentError || rethrow()
        0         throw(signal_analyser_session_error(
        -             "document.state",
        -             sprint(showerror, err),
        -         ))
        -     end
        6     candidate.row_selection = document.row_selection
       12     candidate.displays = copy(document.displays)
        6     candidate.display_layouts = Dict(
        -         display_id => copy(layout)
        -         for (display_id, layout) in document.display_layouts
        -     )
        6     candidate.active_display_id = document.active_display_id
        6     candidate.next_display_number = document.next_display_number
        -     # Session import restores its authoritative bindings verbatim.  Fresh-state
        -     # bootstrap is empty by design, but it must not erase a saved layout here.
        6     signal_analyser_sync_active_display!(candidate, active_display)
        6     signal_analyser_session_validate_candidate!(candidate)
        6     candidate
        - end
        - 
        6 function signal_analyser_publish_session!(
        -     state::SignalAnalyserState,
        -     candidate::SignalAnalyserState,
        - )::Nothing
        6     signal_analyser_cancel_active_output_unlocked!(state)
        6     state.signals = candidate.signals
        6     state.view = candidate.view
        6     state.row_selection = candidate.row_selection
        6     state.displays = candidate.displays
        6     state.display_layouts = candidate.display_layouts
        6     state.active_display_id = candidate.active_display_id
        6     state.next_display_number = candidate.next_display_number
        6     state.plot_cache = candidate.plot_cache
        6     state.spectrum_cache = candidate.spectrum_cache
        6     state.spectrogram_cache = candidate.spectrogram_cache
        6     state.persistence_cache = candidate.persistence_cache
        6     signal_analyser_sync_output_pages_unlocked!(state)
        6     signal_analyser_invalidate_all_outputs_unlocked!(state)
        6     nothing
        - end
        - 
        6 function import_signal_analyser_session!(
        -     ::SignalAnalyserSessionService,
        -     state::SignalAnalyserState,
        -     data,
        - )::Dict{String,Any}
        6     command = parse_import_signal_analyser_session_command(data)
        6     lock(state.lock) do
        6         command.expected_revision == state.view.state_revision || throw(
        -             SignalAnalyserStaleStateError(
        -                 command.expected_revision,
        -                 state.view.state_revision,
        -             ),
        -         )
        6         next_revision = state.view.state_revision + 1
        6         candidate = signal_analyser_session_candidate(state, command.document, next_revision)
        6         signal_analyser_publish_session!(state, candidate)
        6         Dict{String,Any}(
        -             "ok" => true,
        -             "schema" => command.document.schema,
        -             "version" => command.document.version,
        -             "imported_source_revision" => command.document.source_revision,
        -             "state_revision" => next_revision,
        -         )
        -     end
        - end
