(function signalAnalyserApp(window, document) {
  "use strict";

  var api = window.SignalAnalyserApi, PLOTS = ["time", "spectrum", "spectrogram", "persistence"], MEASUREMENT_KINDS = ["minimum", "maximum", "mean", "median", "peak_to_peak", "rms"];
  var TITLES = { time: "Time", spectrum: "Spectrum", spectrogram: "Spectrogram", persistence: "Persistence" };
  var state = null, displays = [], activeDisplayId = null, activeBottomTab = "signals", activeSettingsTab = "display", displayUi = {}, fatalSnapshotError = false, fatalControlsDisabled = false;
  var intentQueue = [], requestInFlight = false, loadRequestId = 0, desiredViews = {}, plotlyPromise = null, plotRenderGeneration = 0, plotRenderTail = Promise.resolve(), plotReassertionPending = false, plotReassertedGeneration = 0, pendingAction = null, peaksBusyDisplayId = null, overflowOpen = false, signalsActionBusy = false, signalsActionOperation = null, signalsAddMenuOpen = false, signalsWorkspaceOpen = false, signalsDeleteOpen = false, signalsDeleteTarget = null, signalsActionError = "", signalsFocusRestore = null, timeDrafts = {}, timeLimitsErrors = {}, timeLimitsContractErrors = {}, measurementKindsErrors = {}, measurementKindsContractErrors = {}, selectionContractErrors = {}, activePlotContractErrors = {}, activePlotPayloadContractErrors = {}, spectrumSettingsErrors = {}, spectrumSettingsContractErrors = {}, spectrumFrequencyLimitsDrafts = {}, spectrumFrequencyLimitsErrors = {}, spectrogramOverlapDrafts = {}, spectrogramOverlapErrors = {}, spectrogramLeakageDrafts = {}, spectrogramLeakageErrors = {}, spectrogramFrequencyLimitsDrafts = {}, spectrogramFrequencyLimitsErrors = {}, spectrogramFrequencyScaleErrors = {}, spectrogramPowerLimitsDrafts = {}, spectrogramPowerLimitsErrors = {}, spectrogramSettingsContractErrors = {}, acceptedSpectrogramSettings = {}, persistenceLeakageDrafts = {}, persistenceLeakageErrors = {}, persistenceSettingsContractErrors = {}, acceptedPersistenceSettings = {};
  var signalsDeleteAcknowledged = false, signalsWorkspaceAcknowledged = false, signalsWorkspaceSuccessName = "", signalsToolbarError = "", signalsAddKeyboardActivation = false;
  var workspaceBrowser = { catalog:null, selected:{}, sampleRate:"", loading:false, busy:false, error:"", retry:false, open:false, successCount:0 }, sessionUi = { open:false, busy:false, file:null, document:null, error:"", success:false, status:"", statusError:false, focus:null };
  var appScriptUrl = document.currentScript && document.currentScript.src;
  var host = document.querySelector("[data-testid='active-plot-host']"), root = document.querySelector("[data-testid='app-shell']");
  var signalHiddenColumns = {}, signalColumnsMenuOpen = false, displayTabOrder = [], draggedDisplayId = null, displayReorderPending = false;

  function object(v) { return v && typeof v === "object"; }
  function esc(v) { return String(v == null ? "" : v).replace(/[&<>"']/g, function(c) { return { "&":"&amp;", "<":"&lt;", ">":"&gt;", "\"":"&quot;", "'":"&#39;" }[c]; }); }
  function testId(value) { return "signal-" + encodeURIComponent(String(value || "signal")).replace(/%/g, "_").replace(/[^A-Za-z0-9_-]/g, function(c) { return "_" + c.charCodeAt(0).toString(16); }); }
  function names() { return (state && state.signals || []).map(function(s) { return String(s.name || ""); }).filter(Boolean); }
  function ordered(values) { var wanted = {}; (values || []).forEach(function(v) { wanted[v] = true; }); return names().filter(function(n) { return wanted[n]; }); }
  function measurementKinds(values) { var wanted = {}; (Array.isArray(values) ? values : ["minimum", "maximum", "mean"]).forEach(function(value) { if (MEASUREMENT_KINDS.indexOf(value) >= 0) wanted[value] = true; }); return MEASUREMENT_KINDS.filter(function(value) { return wanted[value]; }); }
  function measurementKindsSnapshot(value, present) { var seen = {}, valid = Array.isArray(value) && value.every(function(kind) { if (typeof kind !== "string" || MEASUREMENT_KINDS.indexOf(kind) < 0 || seen[kind]) return false; seen[kind] = true; return true; }); return !present ? { valid:true, kinds:measurementKinds() } : valid ? { valid:true, kinds:measurementKinds(value) } : { valid:false, kinds:[] }; }
  function active() { return displays.filter(function(d) { return d.id === activeDisplayId; })[0] || displays[0] || null; }
  function validSnapshotEnvelope(snapshot) { var signalNames = {}, displayIds = {}, validSignals, validDisplays; if (!snapshot || typeof snapshot !== "object" || Array.isArray(snapshot) || !Array.isArray(snapshot.signals) || !Array.isArray(snapshot.displays) || !snapshot.displays.length || typeof snapshot.active_display_id !== "string" || !snapshot.active_display_id) return false; validSignals = snapshot.signals.every(function(signal) { if (!signal || typeof signal !== "object" || Array.isArray(signal) || typeof signal.name !== "string" || !signal.name || signalNames[signal.name]) return false; signalNames[signal.name] = true; return true; }); validDisplays = snapshot.displays.every(function(display) { if (!display || typeof display !== "object" || Array.isArray(display) || typeof display.id !== "string" || !display.id || displayIds[display.id]) return false; displayIds[display.id] = true; return true; }); return validSignals && validDisplays && !!displayIds[snapshot.active_display_id]; }
  function own(object, key) { return Object.prototype.hasOwnProperty.call(object, key); }
  function plainObject(value) { var prototype; if (!value || typeof value !== "object" || Array.isArray(value) || Object.prototype.toString.call(value) !== "[object Object]") return false; prototype = Object.getPrototypeOf(value); return prototype === null || Object.getPrototypeOf(prototype) === null; }
  function sameNames(left, right) { return Array.isArray(left) && Array.isArray(right) && left.length === right.length && left.every(function(value, index) { return value === right[index]; }); }
  function nullableKnownName(value, known) { return value === null || (typeof value === "string" && !!value && !!known[value]); }
  function displaySelectionSnapshot(display, inventory, known) { var visible, orderedVisible, analysis, selected, seen = {}; if (!own(display, "visible_signals") || !Array.isArray(display.visible_signals) || !own(display, "analysis_signal") || !own(display, "selected_signal")) return false; visible = display.visible_signals; if (!visible.every(function(name) { if (typeof name !== "string" || !name || !known[name] || seen[name]) return false; seen[name] = true; return true; })) return false; orderedVisible = inventory.filter(function(name) { return seen[name]; }); if (!sameNames(visible, orderedVisible)) return false; analysis = display.analysis_signal; selected = display.selected_signal; if (!nullableKnownName(analysis, known) || !nullableKnownName(selected, known) || analysis !== selected) return false; if (!visible.length) return analysis === null && selected === null; return analysis !== null && visible.indexOf(analysis) >= 0; }
  function validSelectionSnapshot(snapshot) { var inventory = snapshot.signals.map(function(signal) { return signal.name; }), known = {}, invalid = {}, active, activeVisible; inventory.forEach(function(name) { known[name] = true; }); if (!inventory.length || !own(snapshot, "row_selected_signal") || typeof snapshot.row_selected_signal !== "string" || !snapshot.row_selected_signal || !known[snapshot.row_selected_signal]) return { valid:false, errors:{} }; snapshot.displays.forEach(function(display) { if (!displaySelectionSnapshot(display, inventory, known)) invalid[display.id] = "Некорректный выбор сигналов в ответе сервера."; }); active = snapshot.displays.filter(function(display) { return display.id === snapshot.active_display_id; })[0]; if (invalid[active.id]) return { valid:true, errors:invalid }; if (!own(snapshot, "visible_signals") || !own(snapshot, "analysis_signal") || !own(snapshot, "selected_signal") || !Array.isArray(snapshot.visible_signals) || !sameNames(snapshot.visible_signals, active.visible_signals) || snapshot.analysis_signal !== active.analysis_signal || snapshot.selected_signal !== active.selected_signal || snapshot.analysis_signal !== snapshot.selected_signal || !nullableKnownName(snapshot.analysis_signal, known) || !nullableKnownName(snapshot.selected_signal, known)) return { valid:false, errors:{} }; activeVisible = active.visible_signals; if (!snapshot.signals.every(function(signal) { return own(signal, "visible") && typeof signal.visible === "boolean" && signal.visible === (activeVisible.indexOf(signal.name) >= 0); })) return { valid:false, errors:{} }; return { valid:true, errors:invalid }; }
  function validActivePlotSnapshot(snapshot) { var errors = {}, active = snapshot.displays.filter(function(display) { return display.id === snapshot.active_display_id; })[0]; snapshot.displays.forEach(function(display) { if (!own(display, "active_plot") || typeof display.active_plot !== "string" || PLOTS.indexOf(display.active_plot) < 0) errors[display.id] = "Некорректный тип графика в ответе сервера."; }); if (errors[active.id]) return { valid:true, errors:errors }; if (!own(snapshot, "active_plot") || typeof snapshot.active_plot !== "string" || PLOTS.indexOf(snapshot.active_plot) < 0 || snapshot.active_plot !== active.active_plot) return { valid:false, errors:{} }; return { valid:true, errors:errors }; }
  function validActivePlotPayloadSnapshot(snapshot, selectionErrors, activePlotErrors) { var active = snapshot.displays.filter(function(display) { return display.id === snapshot.active_display_id; })[0], payload, branch, valid, result; if (selectionErrors[active.id] || activePlotErrors[active.id]) return { errors:{} }; payload = snapshot.plot_payload; if (!plainObject(payload) || !exactKeys(payload, ["selected_signal", "visible_signals", "time_traces", "spectrum_traces", "spectrogram", "persistence"]) || payload.selected_signal !== active.selected_signal || !sameNames(payload.visible_signals, active.visible_signals)) { result = {}; result[active.id] = "Некорректные данные активного графика в ответе сервера."; return { errors:result }; } if (active.active_plot === "time" || active.active_plot === "spectrum") { branch = active.active_plot === "time" ? payload.time_traces : payload.spectrum_traces; valid = Array.isArray(branch) && branch.length === active.visible_signals.length && branch.every(function(trace, index) { return plainObject(trace) && own(trace, "signal") && trace.signal === active.visible_signals[index]; }); } else { branch = payload[active.active_plot]; valid = plainObject(branch) && own(branch, "signal") && branch.signal === active.selected_signal; } if (valid) return { errors:{} }; result = {}; result[active.id] = "Некорректные данные активного графика в ответе сервера."; return { errors:result }; }
  function restoreFatalControls() { if (!fatalControlsDisabled) return; document.querySelectorAll("input, select, button").forEach(function(control) { if (control.dataset.fatalDisabled === "true") { control.disabled = false; delete control.dataset.fatalDisabled; } }); fatalControlsDisabled = false; error(""); }
  function resetFatalSnapshot() { var errorPanel, errorText; fatalSnapshotError = true; state = null; displays = []; activeDisplayId = null; desiredViews = {}; intentQueue = []; pendingAction = null; ++plotRenderGeneration; clearFatalPlotHost(); ["display-tabs", "signal-rows"].forEach(function(id) { var node = document.querySelector("[data-testid='" + id + "']") || document.querySelector("[data-" + id + "]"); if (node) node.innerHTML = ""; }); document.querySelectorAll("input, select, button").forEach(function(control) { if (control.getAttribute("data-retry") === null) { control.disabled = true; control.dataset.fatalDisabled = "true"; } }); fatalControlsDisabled = true; errorPanel = document.querySelector("[data-testid='app-error']"); errorText = document.querySelector("[data-error-text]"); if (errorPanel) errorPanel.hidden = false; if (errorText) errorText.textContent = "Некорректная структура snapshot сервера."; }
  function normalize(snapshot) { restoreFatalControls();
    var signals = Array.isArray(snapshot && snapshot.signals) ? snapshot.signals : [];
    var rowSelected = snapshot && (snapshot.row_selected_signal || snapshot.selected_signal) || "";
    return { state_revision:snapshot && snapshot.state_revision, row_selected_signal:rowSelected, analysis_signal:snapshot && (snapshot.analysis_signal !== undefined ? snapshot.analysis_signal : snapshot.selected_signal), signals:signals,
      displays:Array.isArray(snapshot && snapshot.displays) ? snapshot.displays.map(function(d) {
        var ui = displayUi[String(d.id)] || {};
        var analysis = d.analysis_signal !== undefined ? d.analysis_signal : d.selected_signal;
        var hasDisplayTimeLimits = Object.prototype.hasOwnProperty.call(d, "time_limits"), hasRootTimeLimits = !!(snapshot && Object.prototype.hasOwnProperty.call(snapshot, "time_limits"));
        return { id:String(d.id), label:d.name || d.label || "Display", plot:d.active_plot, visible_signals:Array.isArray(d.visible_signals) ? d.visible_signals : [], analysis_signal:analysis || null, selected_signal:analysis || "", time_limits:hasDisplayTimeLimits ? d.time_limits : hasRootTimeLimits ? snapshot.time_limits : null, peaksEnabled:!!d.peaks_enabled, showLegend:ui.showLegend !== undefined ? ui.showLegend : d.show_legend !== false, normalizeY:ui.normalizeY !== undefined ? ui.normalizeY : !!d.normalize_y, showMarkers:ui.showMarkers !== undefined ? ui.showMarkers : !!d.show_markers };
      }) : [], active_display_id:snapshot && snapshot.active_display_id, plots:object(snapshot && snapshot.plots) ? snapshot.plots : {}, plot_payload:object(snapshot && snapshot.plot_payload) ? snapshot.plot_payload : {}, panel:object(snapshot && snapshot.panel) ? snapshot.panel : {}, measurements:object(snapshot && snapshot.measurements) ? snapshot.measurements : null, peaks:object(snapshot && snapshot.peaks) ? snapshot.peaks : null };
  }
  function spectrumFrequencyLimits(value) { return object(value) && typeof value.min_hz === "number" && typeof value.max_hz === "number" && value.units === "Hz" ? { min_hz:value.min_hz, max_hz:value.max_hz, units:"Hz" } : null; }
  function timeLimitsSnapshot(value, present, empty) { var valid = exactKeys(value, ["min_s", "max_s", "units"]) && typeof value.min_s === "number" && isFinite(value.min_s) && typeof value.max_s === "number" && isFinite(value.max_s) && value.min_s <= value.max_s && value.units === "s"; if (!present) return { valid:true, limits:null }; if (value === null) return empty ? { valid:true, limits:null } : { valid:false, limits:null }; return valid && !empty ? { valid:true, limits:value } : { valid:false, limits:null }; }
  function spectrumSettings(value) { var v = value || {}; return { scale:v.scale === "linear" ? "linear" : "db", frequency_scale:v.frequency_scale === "log" ? "log" : "linear", leakage:typeof v.leakage === "number" ? v.leakage : .5, frequency_limits:spectrumFrequencyLimits(v.frequency_limits) }; }
  function spectrumSnapshotSettings(value, present) { var limits = value && value.frequency_limits, exactLimits = limits === null || !!(exactKeys(limits, ["min_hz", "max_hz", "units"]) && typeof limits.min_hz === "number" && isFinite(limits.min_hz) && typeof limits.max_hz === "number" && isFinite(limits.max_hz) && limits.min_hz < limits.max_hz && limits.units === "Hz"), valid = exactKeys(value, ["scale", "frequency_scale", "leakage", "frequency_limits"]) && (value.scale === "db" || value.scale === "linear") && (value.frequency_scale === "linear" || value.frequency_scale === "log") && typeof value.leakage === "number" && isFinite(value.leakage) && value.leakage >= 0 && value.leakage <= 1 && exactLimits; return !present ? { valid:true, settings:{ scale:"db", frequency_scale:"linear", leakage:.5, frequency_limits:null } } : valid ? { valid:true, settings:value } : { valid:false, settings:null }; }
  function spectrogramFrequencyLimits(value) { return object(value) && typeof value.min_hz === "number" && typeof value.max_hz === "number" && value.units === "Hz" ? { min_hz:value.min_hz, max_hz:value.max_hz, units:"Hz" } : null; }
  function exactKeys(value, keys) { var actual = object(value) ? Object.keys(value).sort() : [], expected = keys.slice().sort(); return actual.length === expected.length && actual.every(function(key, index) { return key === expected[index]; }); }
  function spectrogramPowerLimits(value) { return exactKeys(value, ["min_db", "max_db", "units"]) && typeof value.min_db === "number" && isFinite(value.min_db) && typeof value.max_db === "number" && isFinite(value.max_db) && value.min_db < value.max_db && value.units === "dB" ? { min_db:value.min_db, max_db:value.max_db, units:"dB" } : null; }
  function spectrogramPowerRange(value) { if (!exactKeys(value, ["min_db", "max_db", "units"]) || value.units !== "dB" || typeof value.min_db !== "number" || !isFinite(value.min_db) || typeof value.max_db !== "number" || !isFinite(value.max_db) || value.min_db > value.max_db) return { valid:false, range:null }; return { valid:true, range:{ min_db:value.min_db, max_db:value.max_db, units:"dB" } }; }
  function spectrogramRenderedPowerRange(value) { if (!exactKeys(value, ["min", "max", "units"]) || (value.units !== "dB" && value.units !== "power") || typeof value.min !== "number" || !isFinite(value.min) || typeof value.max !== "number" || !isFinite(value.max) || !(value.min < value.max)) return { valid:false, range:null }; return { valid:true, range:{ min:value.min, max:value.max, units:value.units } }; }
  function spectrogramPowerMetadata(payload) { var meta = payload && payload.power_limits, requested, effective, rendered; if (!exactKeys(meta, ["mode", "requested", "effective", "rendered"]) || (meta.mode !== "auto" && meta.mode !== "explicit")) return { valid:false, effective:null, rendered:null }; requested = meta.requested === null ? null : spectrogramPowerLimits(meta.requested); effective = meta.effective === null ? null : spectrogramPowerRange(meta.effective); rendered = meta.rendered === null ? null : spectrogramRenderedPowerRange(meta.rendered); if ((meta.requested !== null && !requested) || (meta.effective !== null && !effective) || (meta.rendered !== null && !rendered)) return { valid:false, effective:null, rendered:null }; if (meta.mode === "auto" && meta.requested !== null) return { valid:false, effective:null, rendered:null }; if (meta.mode === "explicit" && (!requested || !effective || effective.range.min_db !== requested.min_db || effective.range.max_db !== requested.max_db || effective.range.units !== requested.units)) return { valid:false, effective:null, rendered:null }; return { valid:true, effective:effective && effective.range, rendered:rendered && rendered.range }; }
  function persistenceDensityRange(value, strict) { if (!exactKeys(value, ["min", "max", "units"]) || value.units !== "percent" || typeof value.min !== "number" || !isFinite(value.min) || typeof value.max !== "number" || !isFinite(value.max) || value.min < 0 || value.max > 100 || (strict ? !(value.min < value.max) : value.min > value.max)) return { valid:false, range:null }; return { valid:true, range:{ min:value.min, max:value.max, units:"percent" } }; }
  function persistenceDensityMetadata(payload) { var meta = payload && payload.density_limits, requested, effective, rendered; if (!exactKeys(meta, ["mode", "requested", "effective", "rendered"]) || (meta.mode !== "auto" && meta.mode !== "explicit")) return { valid:false, rendered:null }; requested = meta.requested === null ? null : persistenceDensityRange(meta.requested, false); effective = meta.effective === null ? null : persistenceDensityRange(meta.effective, false); rendered = meta.rendered === null ? null : persistenceDensityRange(meta.rendered, true); if ((meta.requested !== null && !requested) || (meta.effective !== null && !effective) || (meta.rendered !== null && !rendered)) return { valid:false, rendered:null }; if (meta.mode === "auto" && meta.requested !== null) return { valid:false, rendered:null }; if (meta.mode === "explicit" && (!requested || !effective || effective.range.min !== requested.range.min || effective.range.max !== requested.range.max || effective.range.units !== requested.range.units || !rendered || rendered.range.min !== requested.range.min || rendered.range.max !== requested.range.max || rendered.range.units !== requested.range.units)) return { valid:false, rendered:null }; return { valid:true, rendered:rendered && rendered.range }; }
  function spectrogramSettings(value) { var v = value || {}; return { overlap_percent:typeof v.overlap_percent === "number" ? v.overlap_percent : 50, leakage:typeof v.leakage === "number" ? v.leakage : .5, frequency_limits:spectrogramFrequencyLimits(v.frequency_limits), frequency_scale:v.frequency_scale === "log" ? "log" : "linear", power_limits:spectrogramPowerLimits(v.power_limits) }; }
  function spectrogramSnapshotSettings(value, present) { var frequency = value && value.frequency_limits, power = value && value.power_limits, validFrequency = frequency === null || !!(exactKeys(frequency, ["min_hz", "max_hz", "units"]) && typeof frequency.min_hz === "number" && isFinite(frequency.min_hz) && typeof frequency.max_hz === "number" && isFinite(frequency.max_hz) && frequency.min_hz < frequency.max_hz && frequency.units === "Hz"), validPower = power === null || !!(exactKeys(power, ["min_db", "max_db", "units"]) && typeof power.min_db === "number" && isFinite(power.min_db) && typeof power.max_db === "number" && isFinite(power.max_db) && power.min_db < power.max_db && power.units === "dB"), valid = exactKeys(value, ["overlap_percent", "leakage", "frequency_limits", "frequency_scale", "power_limits"]) && typeof value.overlap_percent === "number" && isFinite(value.overlap_percent) && value.overlap_percent >= 0 && value.overlap_percent <= 75 && typeof value.leakage === "number" && isFinite(value.leakage) && value.leakage >= 0 && value.leakage <= 1 && (value.frequency_scale === "linear" || value.frequency_scale === "log") && validFrequency && validPower; return !present ? { valid:true, settings:{ overlap_percent:50, leakage:.5, frequency_limits:null, frequency_scale:"linear", power_limits:null } } : valid ? { valid:true, settings:value } : { valid:false, settings:null }; }
  function persistenceSettings(value) { return exactKeys(value, ["leakage"]) && typeof value.leakage === "number" && isFinite(value.leakage) && value.leakage >= 0 && value.leakage <= 1 ? { leakage:value.leakage === 0 ? 0 : value.leakage } : null; }
  function persistenceSnapshotSettings(value, present) { var settings = persistenceSettings(value); return !present ? { valid:true, settings:{ leakage:.5 } } : settings ? { valid:true, settings:settings } : { valid:false, settings:null }; }
  function accept(snapshot) { var selection, activePlot, activePayload; if (!validSnapshotEnvelope(snapshot)) { resetFatalSnapshot(); return false; } selection = validSelectionSnapshot(snapshot); if (!selection.valid) { resetFatalSnapshot(); return false; } activePlot = validActivePlotSnapshot(snapshot); if (!activePlot.valid) { resetFatalSnapshot(); return false; } activePayload = validActivePlotPayloadSnapshot(snapshot, selection.errors, activePlot.errors); fatalSnapshotError = false; state = normalize(snapshot); displays = state.displays; timeLimitsContractErrors = {}; measurementKindsContractErrors = {}; selectionContractErrors = selection.errors; activePlotContractErrors = activePlot.errors; activePlotPayloadContractErrors = activePayload.errors; spectrumSettingsContractErrors = {}; spectrogramSettingsContractErrors = {}; persistenceSettingsContractErrors = {}; displays.forEach(function(display, index) { var source = snapshot.displays[index], hasTimeLimits = !!Object.prototype.hasOwnProperty.call(source, "time_limits"), hasRootTimeLimits = !!Object.prototype.hasOwnProperty.call(snapshot, "time_limits"), time = timeLimitsSnapshot(hasTimeLimits ? source.time_limits : hasRootTimeLimits ? snapshot.time_limits : undefined, hasTimeLimits || hasRootTimeLimits, !display.visible_signals.length), hasMeasurementKinds = !!Object.prototype.hasOwnProperty.call(source, "measurement_kinds"), measurements = measurementKindsSnapshot(hasMeasurementKinds ? source.measurement_kinds : undefined, hasMeasurementKinds), hasSpectrumSettings = !!Object.prototype.hasOwnProperty.call(source, "spectrum_settings"), hasRootSpectrumSettings = !!Object.prototype.hasOwnProperty.call(snapshot, "spectrum_settings"), spectrum = spectrumSnapshotSettings(hasSpectrumSettings ? source.spectrum_settings : hasRootSpectrumSettings ? snapshot.spectrum_settings : undefined, hasSpectrumSettings || hasRootSpectrumSettings), hasSpectrogramSettings = !!Object.prototype.hasOwnProperty.call(source, "spectrogram_settings"), hasRootSpectrogramSettings = !!Object.prototype.hasOwnProperty.call(snapshot, "spectrogram_settings"), spectrogram = spectrogramSnapshotSettings(hasSpectrogramSettings ? source.spectrogram_settings : hasRootSpectrogramSettings ? snapshot.spectrogram_settings : undefined, hasSpectrogramSettings || hasRootSpectrogramSettings), hasDisplaySettings = !!Object.prototype.hasOwnProperty.call(source, "persistence_settings"), hasRootSettings = !!Object.prototype.hasOwnProperty.call(snapshot, "persistence_settings"), persistence = persistenceSnapshotSettings(hasDisplaySettings ? source.persistence_settings : hasRootSettings ? snapshot.persistence_settings : undefined, hasDisplaySettings || hasRootSettings); display.time_limits = time.limits; display.measurement_kinds = measurements.kinds; display.spectrum_settings = spectrum.settings; display.spectrogram_settings = spectrogram.settings; display.persistence_settings = persistence.settings; if (!time.valid) timeLimitsContractErrors[display.id] = "Некорректные границы времени в ответе сервера."; if (!measurements.valid) measurementKindsContractErrors[display.id] = "Некорректный набор Statistics в ответе сервера."; if (!spectrum.valid) spectrumSettingsContractErrors[display.id] = "Некорректные настройки Spectrum в ответе сервера."; if (!spectrogram.valid) spectrogramSettingsContractErrors[display.id] = "Некорректные настройки Spectrogram в ответе сервера."; if (!persistence.valid) persistenceSettingsContractErrors[display.id] = "Некорректные настройки Persistence в ответе сервера."; if (activePlotContractErrors[display.id] || selectionContractErrors[display.id] || activePlotPayloadContractErrors[display.id] || !time.valid || !measurements.valid || !spectrum.valid || !spectrogram.valid || !persistence.valid) quarantineViews(display.id); }); acceptedSpectrogramSettings = {}; acceptedPersistenceSettings = {}; displays.forEach(function(display) { if (!spectrogramSettingsContractErrors[display.id]) acceptedSpectrogramSettings[display.id] = spectrogramSettings(display.spectrogram_settings); if (!persistenceSettingsContractErrors[display.id]) acceptedPersistenceSettings[display.id] = persistenceSettings(display.persistence_settings); }); activeDisplayId = state.active_display_id; applyDesiredViews(); return true; }
  function loading(on, text) { document.querySelector("[data-testid='app-loading']").hidden = !on; document.querySelector("[data-loading-text]").textContent = text || "Loading…"; root.setAttribute("aria-busy", on ? "true" : "false"); }
  function error(message) { var p = document.querySelector("[data-testid='app-error']"); p.hidden = !message; document.querySelector("[data-error-text]").textContent = message || ""; }

  // The local Plotly cartesian bundle is UMD and publishes `moduleName` in some browsers.
  // Normalize it before every draw, then retry the local asset using the app script URL so
  // installations served under a nested base path do not accidentally request /vendor/....
  function plotly() { if (!window.Plotly && window.moduleName && typeof window.moduleName.react === "function") window.Plotly = window.moduleName; return window.Plotly; }
  function hasPlotly() { return !!(plotly() && typeof plotly().react === "function"); }
  function localPlotlyUrl() { return appScriptUrl && window.URL ? new window.URL("vendor/plotly-cartesian-3.1.0.min.js", appScriptUrl).href : "./js/vendor/plotly-cartesian-3.1.0.min.js"; }
  function loadPlotlyScript(url) { return new Promise(function(resolve, reject) { var script = document.createElement("script"); script.src = url; script.async = true; script.onload = function() { hasPlotly() ? resolve(plotly()) : reject(new Error("Plotly was not registered")); }; script.onerror = function() { reject(new Error("Plotly failed to load")); }; document.head.appendChild(script); }); }
  function ensurePlotly() { if (hasPlotly()) return Promise.resolve(plotly()); if (plotlyPromise) return plotlyPromise; plotlyPromise = loadPlotlyScript(localPlotlyUrl()).catch(function(err) { plotlyPromise = null; throw err; }); return plotlyPromise; }
  function currentPayload(plot) { var direct = state.plot_payload, traces, source; if (!plainObject(direct)) return null; if (plot === "time" || plot === "spectrum") { traces = plot === "time" ? direct.time_traces : direct.spectrum_traces; source = traces.filter(function(trace) { return trace.signal === direct.selected_signal; })[0]; return source ? Object.assign({}, source, { traces:traces }) : { traces:traces }; } return direct[plot]; }
  function traceName(t) { return String(t && t.signal || ""); }
  function peaksFor(display) { var p = state && state.peaks; return p && p.enabled === true && display.peaksEnabled && display.plot === "time" && p.display_id === display.id && p.signal_name === display.selected_signal && Array.isArray(p.items) ? p : null; }
  function traceScale(trace) { var values = Array.isArray(trace && trace.y) ? trace.y.filter(function(value) { return typeof value === "number" && isFinite(value); }) : []; if (!values.length) return null; return { min:Math.min.apply(null, values), max:Math.max.apply(null, values) }; }
  function normalizedValues(values, scale) { return !Array.isArray(values) || !scale ? values : values.map(function(value) { return typeof value === "number" && isFinite(value) ? (scale.max === scale.min ? 0 : (value - scale.min) / (scale.max - scale.min)) : value; }); }
  // Peaks use the analysis-source affine scale without clipping: raw extrema can be
  // absent from the bounded Time trace, so a valid marker may intentionally be outside [0,1].
  function peakMarkerTrace(display, scale) { var p = peaksFor(display); if (!p) return null; return { type:"scatter", mode:"markers", name:"Пики", x:p.items.map(function(item) { return item.time_s; }), y:display.normalizeY ? normalizedValues(p.items.map(function(item) { return item.value; }), scale) : p.items.map(function(item) { return item.value; }), marker:{ color:"#d14343", size:8, symbol:"x" }, hovertemplate:"Пик<br>t=%{x}<br>y=%{y}<extra></extra>", meta:{ test_id:"peak-marker-trace", display_id:p.display_id, signal_name:p.signal_name, normalization:"analysis-source-affine-unclipped" } }; }
  function plotData(plot, display) { if (!display.analysis_signal) return []; var p = currentPayload(plot), heat = plot === "spectrogram" || plot === "persistence" || p.type === "heatmap"; if (heat) { var selected = p, y = selected.y || [], scaleMeta = p.frequency_scale || {}, positive = Array.isArray(y) ? y.filter(function(value) { return typeof value === "number" && isFinite(value) && value > 0; }) : [], floor = positive.length ? Math.min.apply(null, positive) / 2 : null, renderY = scaleMeta.effective === "log" ? (floor == null ? [] : y.map(function(value) { return typeof value === "number" && isFinite(value) && value <= 0 ? floor : value; })) : y, limits = plot === "spectrogram" ? spectrogramPowerMetadata(p) : plot === "persistence" ? persistenceDensityMetadata(p) : { valid:true, rendered:null }, trace; if (!Array.isArray(selected.z) || (scaleMeta.effective === "log" && floor == null) || !limits.valid) return []; trace = { type:"heatmap", x:selected.x || [], y:renderY, z:selected.z, colorscale:selected.colorscale || "Jet", colorbar:{ title:{ text:selected.color_label || "" } } }; if (limits.rendered) { trace.zauto = false; trace.zmin = limits.rendered.min; trace.zmax = limits.rendered.max; } else trace.zauto = true; return [trace]; } var raw = p.traces, source = raw.filter(function(t) { return traceName(t) === display.analysis_signal; })[0], sourceScale = traceScale(source), traces = raw.filter(function(t) { return display.visible_signals.indexOf(traceName(t)) >= 0; }).map(function(t) { var s = (state.signals || []).filter(function(x) { return x.name === traceName(t); })[0] || {}, scale = traceScale(t); return Object.assign({}, t, { type:t.type || "scatter", y:plot === "time" && display.normalizeY ? normalizedValues(t.y, scale) : t.y, mode:plot === "time" && display.showMarkers ? "lines+markers" : (t.mode || "lines"), name:traceName(t), line:Object.assign({}, t.line || {}, { color:(t.line || {}).color || s.color || "#1676e6" }) }); }), marker = peakMarkerTrace(display, sourceScale); if (marker) traces.push(marker); return traces; }
  function clearPlotHost() { var Plotly = plotly(); try { if (Plotly && typeof Plotly.purge === "function") Plotly.purge(host); } catch (ignored) {} ["data", "_fullData", "calcdata"].forEach(function(key) { try { host[key] = []; } catch (ignored) {} }); }
  function clearFatalPlotHost() { if (!fatalSnapshotError) return false; clearPlotHost(); host.innerHTML = ""; host.dataset.plotReady = "false"; return true; }
  function invalidTimeData(display) { if (display.plot !== "time") return false; var p = currentPayload("time"), traces = p.traces; return traces.filter(function(trace) { return display.visible_signals.indexOf(traceName(trace)) >= 0; }).some(function(trace) { return !Array.isArray(trace.y) || trace.y.some(function(value) { return typeof value !== "number" || !isFinite(value); }); }); }
  function activeSelectionQuarantined() { var display = active(); return !!(display && selectionContractErrors[display.id]); }
  function activePlotQuarantined() { var display = active(); return !!(display && activePlotContractErrors[display.id]); }
  function activePlotPayloadQuarantined() { var display = active(); return !!(display && activePlotPayloadContractErrors[display.id]); }
  function reassertLatestPlot() { if (fatalSnapshotError || plotReassertionPending) return; plotReassertionPending = true; Promise.resolve().then(function() { var generation; plotReassertionPending = false; if (fatalSnapshotError) return; if (activeSelectionQuarantined()) { renderSelectionQuarantine(active()); return; } if (activePlotQuarantined()) { renderActivePlotQuarantine(active()); return; } if (activePlotPayloadQuarantined()) { renderActivePlotPayloadQuarantine(active()); return; } generation = plotRenderGeneration; if (plotReassertedGeneration === generation) return; plotReassertedGeneration = generation; draw(); plotReassertedGeneration = plotRenderGeneration; }); }
  function renderPlotError(generation) { if (fatalSnapshotError) return; if (generation !== plotRenderGeneration) { reassertLatestPlot(); return; } host.innerHTML = "<div class='plot-placeholder is-error' data-testid='plot-error-state'>Не удалось загрузить локальную библиотеку графиков.</div>"; host.dataset.plotReady = "false"; }
  function queuePlotRender(generation, data, layout) { plotRenderTail = plotRenderTail.then(function() { if (fatalSnapshotError || generation !== plotRenderGeneration) return null; return ensurePlotly().then(function(Plotly) { if (fatalSnapshotError || generation !== plotRenderGeneration) return null; return Plotly.react(host, data, layout, { responsive:true, displaylogo:false }); }).then(function() { if (clearFatalPlotHost()) return; if (activeSelectionQuarantined()) { renderSelectionQuarantine(active()); return; } if (activePlotQuarantined()) { renderActivePlotQuarantine(active()); return; } if (activePlotPayloadQuarantined()) { renderActivePlotPayloadQuarantine(active()); return; } if (generation !== plotRenderGeneration) { reassertLatestPlot(); return; } host.dataset.plotReady = "true"; }, function() { if (clearFatalPlotHost()) return; if (activeSelectionQuarantined()) { renderSelectionQuarantine(active()); return; } if (activePlotQuarantined()) { renderActivePlotQuarantine(active()); return; } if (activePlotPayloadQuarantined()) { renderActivePlotPayloadQuarantine(active()); return; } renderPlotError(generation); }); }, function() { if (clearFatalPlotHost()) return; if (activeSelectionQuarantined()) { renderSelectionQuarantine(active()); return; } if (activePlotQuarantined()) { renderActivePlotQuarantine(active()); return; } if (activePlotPayloadQuarantined()) { renderActivePlotPayloadQuarantine(active()); return; } renderPlotError(generation); }); }
  function draw() { if (fatalSnapshotError) return; var generation = ++plotRenderGeneration, d = active(); if (!d || !state) return; var data = plotData(d.plot, d), p = currentPayload(d.plot), heat = d.plot === "spectrogram" || d.plot === "persistence" || p.type === "heatmap", xaxis = { title:{ text:p.x_label || "" }, gridcolor:"#e7edf3" }; if (d.plot === "time" && object(d.time_limits)) xaxis.range = [d.time_limits.min_s, d.time_limits.max_s]; if (d.plot === "spectrum") xaxis.type = spectrumSettings(d.spectrum_settings).frequency_scale; var spectrogramScale = d.plot === "spectrogram" && currentPayload("spectrogram").frequency_scale && currentPayload("spectrogram").frequency_scale.effective; var spectrogramY = d.plot === "spectrogram" ? (p.y || []) : [], invalidSpectrogramLog = spectrogramScale === "log" && Array.isArray(spectrogramY) && spectrogramY.length > 0 && !spectrogramY.some(function(value) { return typeof value === "number" && isFinite(value) && value > 0; }), invalidSpectrogramPower = d.plot === "spectrogram" && !spectrogramPowerMetadata(p).valid, invalidPersistenceDensity = d.plot === "persistence" && !persistenceDensityMetadata(p).valid, layout = { margin:{ l:62, r:heat ? 72 : 24, t:14, b:55 }, paper_bgcolor:"#fff", plot_bgcolor:"#fff", font:{ family:"Roboto, Arial, sans-serif", color:"#202938" }, xaxis:xaxis, yaxis:{ title:{ text:p.y_label || "" }, gridcolor:"#e7edf3", type:spectrogramScale === "log" ? "log" : undefined, autorange:d.plot === "time" && d.normalizeY ? true : undefined }, showlegend:!heat && d.showLegend, legend:{ orientation:"h", y:-.2 } }; if (invalidSpectrogramPower) { clearPlotHost(); host.innerHTML = "<div class='plot-placeholder is-error' data-testid='spectrogram-power-limits-contract-error-state'>Некорректные метаданные границ мощности Spectrogram.</div>"; host.dataset.plotReady = "false"; return; } if (invalidPersistenceDensity) { clearPlotHost(); host.innerHTML = "<div class='plot-placeholder is-error' data-testid='persistence-density-limits-contract-error-state'>Некорректные метаданные границ плотности Persistence.</div>"; host.dataset.plotReady = "false"; return; } if (invalidSpectrogramLog) { clearPlotHost(); host.innerHTML = "<div class='plot-placeholder is-error' data-testid='spectrogram-log-frequency-error-state'>Невозможно отобразить логарифмическую шкалу частоты без положительных частот.</div>"; host.dataset.plotReady = "false"; return; } if (invalidTimeData(d)) { clearPlotHost(); host.innerHTML = "<div class='plot-placeholder is-error' data-testid='plot-invalid-data-state'>График Time содержит некорректные данные.</div>"; host.dataset.plotReady = "false"; return; } if (!data.length) { clearPlotHost(); host.innerHTML = "<div class='plot-placeholder' data-testid='" + (d.analysis_signal ? "plot-empty-state" : "empty-display-plot-state") + "'>" + (d.analysis_signal ? "Нет данных для отображения." : "Display не содержит сигналов.") + "</div>"; host.dataset.plotReady = "false"; return; } queuePlotRender(generation, data, layout); }
  function orderedDisplayTabs() { displayTabOrder = displays.map(function(display) { return display.id; }); return displays.slice(); }
  function focusDisplayTab(id) { var tab = document.querySelector("[data-display-id='" + id + "']"); if (tab && typeof tab.focus === "function") tab.focus(); }
  function reorderedDisplayIds(id, target) { var order = displays.map(function(display) { return display.id; }), from = order.indexOf(id), to = typeof target === "number" ? target : order.indexOf(target); if (from < 0 || to < 0 || to >= order.length || from === to) return null; order.splice(from, 1); order.splice(to, 0, id); return order; }
  function requestDisplayReorder(order, focusId) { if (fatalSnapshotError || displayReorderPending || !state || !Array.isArray(order) || order.length !== displays.length || order.some(function(id, index) { return typeof id !== "string" || !id || order.indexOf(id) !== index || !displays.some(function(display) { return display.id === id; }); })) return; displayReorderPending = true; renderTabs(); enqueue({ kind:"reorder", payload:{ operation:"reorder", order:order.slice() }, focusDisplayId:focusId }); }
  function moveDisplayTab(id, direction) { var current = displays.map(function(display) { return display.id; }), index = current.indexOf(id), order = reorderedDisplayIds(id, index + direction); if (order) requestDisplayReorder(order, id); }
  function renderTabs() { var nav = document.querySelector("[data-testid='display-tabs']"), previousOwner = nav.querySelector && nav.querySelector("[data-testid='display-tab-scroll']"), scrollLeft = previousOwner ? previousOwner.scrollLeft : nav.scrollLeft, tabOwner; nav.setAttribute("aria-busy", displayReorderPending ? "true" : "false"); nav.dataset.reorderBusy = displayReorderPending ? "true" : "false"; nav.innerHTML = "<span class='display-tab-scroll' data-testid='display-tab-scroll' role='presentation'>" + orderedDisplayTabs().map(function(d) { var on = d.id === activeDisplayId; return "<button class='display-tab" + (on ? " is-active" : "") + "' draggable='" + (!displayReorderPending) + "' data-display-id='" + esc(d.id) + "' data-testid='display-tab-" + esc(d.id) + "' role='tab' aria-selected='" + on + "' tabindex='" + (on ? "0" : "-1") + "' title='Перетащите вкладку или используйте Alt+Стрелка'>" + esc(d.label) + (displays.length > 1 ? "<span class='display-tab-close' data-close-display='" + esc(d.id) + "' data-testid='close-display-" + esc(d.id) + "' aria-label='Close " + esc(d.label) + "'>×</span>" : "") + "</button>"; }).join("") + "</span><span class='display-zone-actions'><button type='button' class='layout-trigger' data-testid='layout-trigger' aria-label='Изменить layout активного Display' aria-haspopup='dialog' aria-controls='layout-popover' aria-expanded='false'><svg viewBox='0 0 24 24' aria-hidden='true'><rect x='3' y='3' width='7' height='7' rx='1'></rect><rect x='14' y='3' width='7' height='7' rx='1'></rect><rect x='3' y='14' width='7' height='7' rx='1'></rect><rect x='14' y='14' width='7' height='7' rx='1'></rect></svg><span data-testid='layout-trigger-label'>—</span><i aria-hidden='true'></i></button><button type='button' class='add-display' data-testid='add-display' aria-label='Add display'>+</button></span>"; tabOwner = nav.querySelector && nav.querySelector("[data-testid='display-tab-scroll']"); if (tabOwner) tabOwner.scrollLeft = scrollLeft; else nav.scrollLeft = scrollLeft; }
  function renderSettings() { var d = active(), select = document.querySelector("[data-testid='plot-type-select']"), panel = state.panel || {}, peaksAction = document.querySelector("[data-testid='find-peaks-action']"), clearAction = document.querySelector("[data-testid='clear-display-action']"), normalizeControl = document.querySelector("[data-testid='normalize-y-checkbox']"), markersControl = document.querySelector("[data-testid='show-markers-checkbox']"), minInput = document.querySelector("[data-testid='time-min-input']"), maxInput = document.querySelector("[data-testid='time-max-input']"), limitsError = document.querySelector("[data-testid='time-limits-error']"), timeContractError = timeLimitsContractErrors[d.id] || "", timeDataAvailable = d.plot === "time" && !!d.analysis_signal && d.visible_signals.length > 0 && !timeContractError, limits = d.time_limits, draft = timeDrafts[d.id], peaksAvailable = d.plot === "time", peaksBusy = peaksBusyDisplayId === d.id, limitsErrorText = timeContractError || timeLimitsErrors[d.id] || ""; document.querySelector("[data-testid='display-plot-title']").textContent = TITLES[d.plot]; select.value = d.plot; var view = document.querySelector("[data-testid='settings-view-select']"); view.innerHTML = PLOTS.map(function(k) { return "<option value='" + k + "'>" + TITLES[k] + "</option>"; }).join(""); view.value = d.plot; document.querySelector("[data-testid='show-legend-checkbox']").checked = d.showLegend; normalizeControl.checked = d.normalizeY; markersControl.checked = d.showMarkers; normalizeControl.disabled = !timeDataAvailable; markersControl.disabled = !timeDataAvailable; if (minInput) { minInput.disabled = !timeDataAvailable; minInput.value = timeContractError ? "" : draft ? draft.min_s : limits && limits.min_s != null ? String(limits.min_s) : ""; } if (maxInput) { maxInput.disabled = !timeDataAvailable; maxInput.value = timeContractError ? "" : draft ? draft.max_s : limits && limits.max_s != null ? String(limits.max_s) : ""; } if (limitsError) { limitsError.hidden = !limitsErrorText; limitsError.textContent = limitsErrorText; } normalizeControl.setAttribute("aria-label", timeDataAvailable ? "Нормализовать ось Y для активного Time Display" : "Нормализация Y доступна только для непустого Time Display"); markersControl.setAttribute("aria-label", timeDataAvailable ? "Показывать маркеры активного Time Display" : "Маркеры доступны только для непустого Time Display"); peaksAction.disabled = !peaksAvailable || peaksBusy || !d.analysis_signal; peaksAction.setAttribute("aria-pressed", d.peaksEnabled ? "true" : "false"); peaksAction.setAttribute("aria-busy", peaksBusy ? "true" : "false"); peaksAction.setAttribute("aria-label", peaksAvailable ? (d.peaksEnabled ? "Скрыть пики" : "Показать пики") : "Пики доступны только для графика Time"); if (clearAction) clearAction.disabled = !d.visible_signals.length; document.querySelector("[data-panel-fields]").innerHTML = (panel.fields || []).map(function(f) { return "<div class='plot-field'><dt>" + esc(f.label || f.id) + "</dt><dd>" + esc(f.value) + (f.unit ? " " + esc(f.unit) : "") + "</dd></div>"; }).join(""); }
  function renderSignalColumnMenu() { var table = document.querySelector("[data-testid='signal-table']"), menu = document.querySelector("[data-testid='signal-columns-menu']"), trigger = document.querySelector("[data-testid='signal-columns-menu-trigger']"), eye = document.querySelector("[data-testid='signal-columns-visibility-trigger']"), names = { color:"Color", "sample-rate":"Sample rate", samples:"Samples", duration:"Duration", type:"Type" }; if (table) table.setAttribute("data-hidden-columns", Object.keys(signalHiddenColumns).filter(function(key) { return signalHiddenColumns[key]; }).join(" ")); if (menu) { menu.hidden = !signalColumnsMenuOpen; menu.innerHTML = "<p class='signal-columns-menu-title'>Столбцы</p>" + Object.keys(names).map(function(key) { var hidden = !!signalHiddenColumns[key]; return "<button type='button' role='menuitemcheckbox' aria-checked='" + (!hidden) + "' data-signal-column-toggle='" + key + "' data-testid='signal-column-toggle-" + key + "'><span>" + names[key] + "</span><span aria-hidden='true'>" + (!hidden ? "✓" : "") + "</span></button>"; }).join(""); } if (trigger) trigger.setAttribute("aria-expanded", signalColumnsMenuOpen ? "true" : "false"); if (eye) eye.setAttribute("aria-label", Object.keys(signalHiddenColumns).some(function(key) { return signalHiddenColumns[key]; }) ? "Показать все столбцы" : "Все доступные столбцы показаны"); }
  function renderSignals() { var d = active(), visible = d.visible_signals, rowActionsBusy = signalsActionBusy ? " disabled aria-busy='true'" : "", deleteDisabled = signalsActionBusy || names().length <= 1 ? " disabled" : ""; document.querySelector("[data-signal-count]").textContent = (state.signals || []).length + " signals"; document.querySelector("[data-testid='toggle-all-signals']").checked = visible.length === names().length; document.querySelector("[data-signal-rows]").innerHTML = (state.signals || []).map(function(s) { var n = String(s.name || ""), on = visible.indexOf(n) >= 0, selected = state.row_selected_signal === n, id = testId(n), infoId = "signal-info-card-" + id, details = "<dl><div><dt>Samples</dt><dd>" + esc(s.sample_count || "—") + "</dd></div><div><dt>Sample rate</dt><dd>" + esc(s.sample_rate_hz || "—") + " Hz</dd></div><div><dt>Duration</dt><dd>" + esc(s.duration_s || "—") + " s</dd></div><div><dt>Type</dt><dd>" + esc(s.data_type || "—") + "</dd></div></dl>"; return "<tr class='signal-row" + (selected ? " is-selected" : "") + "' tabindex='0' aria-selected='" + selected + "' data-signal='" + esc(n) + "' data-row-selected='" + selected + "' data-display-membership='" + on + "' data-testid='signal-row-" + id + "' aria-label='Выбрать сигнал " + esc(n) + "'><td data-column='visibility'><input type='checkbox' data-signal-visibility='" + esc(n) + "' data-testid='signal-checkbox-" + id + "' " + (on ? "checked" : "") + " aria-checked='" + on + "' aria-label='Показывать " + esc(n) + "'></td><td class='signal-name-cell' data-column='name'>" + esc(n) + "</td><td data-column='color'><span class='color-swatch' style='--signal-color:" + esc(s.color || "#1676e6") + "'></span></td><td data-column='sample-rate'>" + esc(s.sample_rate_hz || "—") + " Hz</td><td data-column='samples'>" + esc(s.sample_count || "—") + "</td><td data-column='duration'>" + esc(s.duration_s || "—") + " s</td><td data-column='type'>" + esc(s.data_type || "—") + "</td><td class='signal-actions-cell' data-column='actions'><span class='signal-row-actions' aria-label='Действия " + esc(n) + "'><button type='button' class='signal-row-action' data-signal-duplicate='" + esc(n) + "' data-testid='signal-duplicate-action-" + id + "' aria-label='Дублировать " + esc(n) + "' title='Дублировать'" + rowActionsBusy + "><img class='icon-asset' src='./icons/copy.svg' alt='' aria-hidden='true'></button><button type='button' class='signal-row-action is-danger' data-signal-delete='" + esc(n) + "' data-testid='signal-delete-action-" + id + "' aria-label='Удалить " + esc(n) + "' title='Удалить'" + deleteDisabled + "><img class='icon-asset' src='./icons/trash.svg' alt='' aria-hidden='true'></button></span><span class='signal-info-cell'><button type='button' class='signal-info-trigger' data-signal-info='" + esc(n) + "' data-testid='signal-info-action-" + id + "' aria-label='Показать информацию о " + esc(n) + "' aria-describedby='" + infoId + "' aria-expanded='false' aria-controls='" + infoId + "' title='Информация о сигнале'>i</button><div id='" + infoId + "' class='signal-info-card' data-testid='" + infoId + "' role='tooltip'>" + details + "</div></span></td></tr>"; }).join(""); renderSignalColumnMenu(); }
  function measurementTestId(item) { return "measurement-row-" + encodeURIComponent(String(item && item.id || "item")).replace(/%/g, "_").replace(/[^A-Za-z0-9_-]/g, function(c) { return "_" + c.charCodeAt(0).toString(16); }); }
  function measurementValue(value, unit) { return esc(value) + (unit ? " <span class='measurement-unit'>" + esc(unit) + "</span>" : ""); }
  function measurementTime(value, unit) { return value == null ? "—" : measurementValue(value, unit); }
  function renderMeasurements() { var m = state.measurements, c = document.querySelector("[data-measurements-content]"); if (!active().analysis_signal) { c.innerHTML = "<p class='measurements-state' data-testid='empty-display-measurements-state'>В Display нет сигнала для измерений.</p>"; return; } if (!m) { c.innerHTML = "<p class='measurements-state' data-testid='measurements-loading-state'>Загрузка измерений…</p>"; return; } if (!Array.isArray(m.items)) { c.innerHTML = "<p class='measurements-state is-error' data-testid='measurements-error-state'>Не удалось отобразить измерения выбранного сигнала.</p>"; return; } var units = object(m.units) ? m.units : {}, signalName = m.signal_name || active().selected_signal || "Сигнал не выбран", ordinate = m.ordinate === "magnitude" ? "модуль" : m.ordinate === "real" ? "действительная часть" : "", signalAttribute = esc(signalName); c.innerHTML = "<div class='measurements-heading'><strong data-testid='measurements-signal-name'>" + signalAttribute + "</strong>" + (ordinate ? " <span class='measurement-ordinate' data-testid='measurements-ordinate'>(" + esc(ordinate) + ")</span>" : "") + "</div>" + (m.items.length ? "<table class='measurements-table' data-testid='measurements-table' data-selected-signal='" + signalAttribute + "'><thead><tr><th>Показатель</th><th>Значение</th><th>Время</th><th>Отсчёт</th></tr></thead><tbody>" + m.items.map(function(item) { return "<tr data-testid='" + measurementTestId(item) + "' data-signal='" + signalAttribute + "' data-selected-signal='" + signalAttribute + "'><td>" + esc(item.label || item.id) + "</td><td>" + measurementValue(item.value, units.value) + "</td><td>" + measurementTime(item.time_s, units.time) + "</td><td>" + (item.sample_index == null ? "—" : esc(item.sample_index)) + "</td></tr>"; }).join("") + "</tbody></table>" : "<p class='measurements-state' data-testid='measurements-empty-state'>Для выбранного сигнала нет измерений.</p>"); }
  function peakTestId(item) { return "peak-row-" + encodeURIComponent(String(item && item.id || "peak")).replace(/%/g, "_").replace(/[^A-Za-z0-9_-]/g, function(c) { return "_" + c.charCodeAt(0).toString(16); }); }
  function renderPeaks() { var p = state.peaks, c = document.querySelector("[data-peaks-content]"), d = active(); if (!d.analysis_signal) { c.innerHTML = "<p class='peaks-state' data-testid='empty-display-peaks-state'>В Display нет сигнала для поиска пиков.</p>"; return; } if (!p || p.enabled !== true) { c.innerHTML = ""; return; } if (p.error) { c.innerHTML = "<p class='peaks-state is-error' data-testid='peaks-error-state'>" + esc(p.error) + "</p>"; return; } if (!Array.isArray(p.items)) { c.innerHTML = "<p class='peaks-state' data-testid='peaks-loading-state'>Загрузка пиков…</p>"; return; } var units = object(p.units) ? p.units : {}, signalName = p.signal_name || d.selected_signal || "Сигнал не выбран", scope = esc(signalName), displayId = esc(p.display_id || d.id), revision = esc(p.state_revision); c.innerHTML = "<div class='peaks-heading'><strong data-testid='peaks-signal-name'>" + scope + "</strong></div>" + (p.items.length ? "<table class='peaks-table' data-testid='peaks-table' data-display-id='" + displayId + "' data-selected-signal='" + scope + "' data-state-revision='" + revision + "'><thead><tr><th>Значение</th><th>Время</th><th>Отсчёт</th><th>Ширина</th><th>Prominence</th></tr></thead><tbody>" + p.items.map(function(item) { return "<tr data-testid='" + peakTestId(item) + "' data-display-id='" + displayId + "' data-signal='" + scope + "' data-value='" + esc(item.value) + "' data-time-s='" + esc(item.time_s) + "' data-sample-index='" + esc(item.sample_index) + "' data-width-samples='" + esc(item.width_samples) + "' data-prominence='" + esc(item.prominence) + "'><td data-peak-field='value'>" + measurementValue(item.value, units.value) + "</td><td data-peak-field='time_s'>" + measurementTime(item.time_s, units.time) + "</td><td data-peak-field='sample_index'>" + (item.sample_index == null ? "—" : esc(item.sample_index)) + "</td><td data-peak-field='width_samples'>" + measurementValue(item.width_samples, units.width) + "</td><td data-peak-field='prominence'>" + measurementValue(item.prominence, units.prominence) + "</td></tr>"; }).join("") + "</tbody></table>" : "<p class='peaks-state' data-testid='peaks-empty-state'>Пики не найдены.</p>"); }
  function activateBottomTab(tabId, focus) { activeBottomTab = tabId; if (fatalSnapshotError) return; renderBottomTabs(); var target = Array.prototype.slice.call(document.querySelectorAll("[data-bottom-tab]")).filter(function(item) { return item.dataset.bottomTab === tabId; })[0]; if (focus && target && typeof target.focus === "function") target.focus(); }
  function renderBottomTabs() { var peaksEnabled = !!(state.peaks && state.peaks.enabled); if (!peaksEnabled && activeBottomTab === "peaks") activeBottomTab = "signals"; document.querySelectorAll("[data-bottom-tab]").forEach(function(tab) { var isPeaks = tab.dataset.bottomTab === "peaks", selected = tab.dataset.bottomTab === activeBottomTab; tab.hidden = isPeaks && !peaksEnabled; tab.classList.toggle("is-active", selected); tab.setAttribute("aria-selected", selected ? "true" : "false"); tab.setAttribute("tabindex", selected ? "0" : "-1"); }); document.querySelector("[data-testid='bottom-panel-signals']").hidden = activeBottomTab !== "signals"; document.querySelector("[data-testid='measurements-panel']").hidden = activeBottomTab !== "measurements"; document.querySelector("[data-testid='peaks-panel']").hidden = activeBottomTab !== "peaks"; }

  function sameTimeLimits(a, b) { return a === b || !!(a && b && Number(a.min_s) === Number(b.min_s) && Number(a.max_s) === Number(b.max_s) && (a.units || "s") === (b.units || "s")); }
  function viewTarget(d) { return { displayId:d.id, active_plot:d.plot, row_selected_signal:state && state.row_selected_signal || "", analysis_signal:d.analysis_signal, visible_signals:d.visible_signals.slice(), time_limits:d.time_limits || null, measurement_kinds:measurementKinds(d.measurement_kinds), spectrum_settings:d.spectrum_settings === null ? null : spectrumSettings(d.spectrum_settings), spectrogram_settings:spectrogramSettings(d.spectrogram_settings), persistence_settings:persistenceSettings(d.persistence_settings), peaks_enabled:!!d.peaksEnabled }; }
  function sameView(a, b) { return a && b && a.displayId === b.displayId && a.active_plot === b.active_plot && a.row_selected_signal === b.row_selected_signal && a.analysis_signal === b.analysis_signal && a.peaks_enabled === b.peaks_enabled && JSON.stringify(a.spectrum_settings) === JSON.stringify(b.spectrum_settings) && JSON.stringify(spectrogramSettings(a.spectrogram_settings)) === JSON.stringify(spectrogramSettings(b.spectrogram_settings)) && JSON.stringify(persistenceSettings(a.persistence_settings)) === JSON.stringify(persistenceSettings(b.persistence_settings)) && sameTimeLimits(a.time_limits, b.time_limits) && measurementKinds(a.measurement_kinds).join("\u0001") === measurementKinds(b.measurement_kinds).join("\u0001") && a.visible_signals.join("\u0001") === b.visible_signals.join("\u0001"); }
  function quarantinedDisplay(id) { return !!(selectionContractErrors[id] || activePlotContractErrors[id] || activePlotPayloadContractErrors[id] || timeLimitsContractErrors[id] || measurementKindsContractErrors[id] || spectrumSettingsContractErrors[id] || spectrogramSettingsContractErrors[id] || persistenceSettingsContractErrors[id]); }
  function quarantineViews(id) { delete desiredViews[id]; delete timeDrafts[id]; delete timeLimitsErrors[id]; delete measurementKindsErrors[id]; intentQueue = intentQueue.filter(function(intent) { return !(intent.kind === "view" && intent.target && intent.target.displayId === id); }); pendingAction = intentQueue[intentQueue.length - 1] || null; }
  function enqueue(intent) { if (fatalSnapshotError) return; if (intent.kind === "view" && quarantinedDisplay(intent.target.displayId)) { quarantineViews(intent.target.displayId); return; } var tail = intentQueue[intentQueue.length - 1]; if (intent.kind === "view" && tail && tail.kind === "view" && tail.target.displayId === intent.target.displayId) intentQueue[intentQueue.length - 1] = intent; else intentQueue.push(intent); pendingAction = intentQueue[intentQueue.length - 1] || null; drain(); }
  function requeueLatestViewAfterStale(intent) { if (fatalSnapshotError) return; var displayId = intent.target.displayId; if (quarantinedDisplay(displayId)) { quarantineViews(displayId); return; } intent.target = desiredViews[displayId] || intent.target; intentQueue = intentQueue.filter(function(queued) { return !(queued.kind === "view" && queued.target && queued.target.displayId === displayId); }); intentQueue.unshift(intent); pendingAction = intentQueue[intentQueue.length - 1] || null; }
  function viewErrorMessage(e) { var payload = e && e.payload || {}, errorPayload = payload.error || {}, fields = errorPayload.fields || payload.fields || payload.errors || {}; return fields.persistence_settings || fields.spectrogram_settings || fields.spectrum_settings || fields.measurement_kinds || fields.time_limits || fields.min_s || fields.max_s || errorPayload.message || payload.message || "Сервер отклонил настройки Display."; }
  function displayMutation(operation, id) { if (fatalSnapshotError) return; var payload = { operation:operation }; if (id) payload.display_id = id; enqueue({ kind:"display", payload:payload }); }
  function selectDisplay(id) { if (id !== activeDisplayId) displayMutation("select", id); }
  function addDisplay() { displayMutation("create"); }
  function closeDisplay(id) { if (displays.length > 1) displayMutation("close", id); }
  function signalActionErrorMessage(e) { var payload = e && e.payload || {}, inner = payload.error || {}; return inner.message || payload.message || "Не удалось изменить список сигналов."; }
  function selectedSignalName() { var selected = state && state.row_selected_signal; return names().indexOf(selected) >= 0 ? selected : null; }
  function canExtractTimeLimits() { var display = active(); return !!(display && display.analysis_signal && object(display.time_limits)); }
  function signalIntentValid(intent) { if (intent.operation === "import_workspace") return true; if (intent.operation === "extract_time_limits") return canExtractTimeLimits() && active().id === intent.display_id; if (intent.operation === "delete") return names().length > 1 && names().indexOf(intent.signal_name) >= 0; return names().indexOf(intent.signal_name) >= 0; }
  function signalPayload(intent) { var payload = { state_revision:state.state_revision, operation:intent.operation }; if (intent.operation === "import_workspace") { payload.variable_name = intent.variable_name; payload.signal_name = intent.signal_name; payload.sample_rate_hz = intent.sample_rate_hz; } else if (intent.operation === "extract_time_limits") payload.display_id = intent.display_id; else payload.signal_name = intent.signal_name; return payload; }
  function focusLater(control) { if (control && typeof control.focus === "function") window.setTimeout(function() { control.focus(); }, 0); }
  function focusFirstInvalid(control) { focusLater(control); }
  function activeSignalsDialog() { return signalsWorkspaceOpen ? document.querySelector("[data-testid='signals-workspace-dialog']") : signalsDeleteOpen ? document.querySelector("[data-testid='signals-delete-dialog']") : null; }
  function setSignalsModalBackground(disabled) { if (!root) return; if (disabled) signalsToolbarError = ""; root.inert = !!disabled; if (disabled) root.setAttribute("aria-hidden", "true"); else if (typeof root.removeAttribute === "function") root.removeAttribute("aria-hidden"); }
  function setSignalsAddMenu(open, focus) { var trigger = document.querySelector("[data-testid='signals-add-action']"), menu = document.querySelector("[data-testid='signals-add-menu']"), first = document.querySelector("[data-testid='signals-add-workspace-action']"); signalsAddMenuOpen = !!open; if (signalsAddMenuOpen) signalsToolbarError = ""; if (menu) menu.hidden = !signalsAddMenuOpen; if (trigger) trigger.setAttribute("aria-expanded", signalsAddMenuOpen ? "true" : "false"); if (signalsAddMenuOpen && focus) focusLater(first); if (!signalsAddMenuOpen && focus) focusLater(trigger); }
  function setWorkspaceValidation(variableInvalid, sampleRateInvalid) { var variable = document.querySelector("[data-testid='signals-workspace-variable-input']"), rate = document.querySelector("[data-testid='signals-workspace-sample-rate-input']"); if (variable) variable.setAttribute("aria-invalid", variableInvalid ? "true" : "false"); if (rate) rate.setAttribute("aria-invalid", sampleRateInvalid ? "true" : "false"); }
  function setSignalsDialogState() { var workspace = document.querySelector("[data-testid='signals-workspace-dialog']"), deletion = document.querySelector("[data-testid='signals-delete-dialog']"), errorPanel = document.querySelector("[data-testid='signals-action-error']"), errorText = document.querySelector("[data-testid='signals-action-error-text']"), selected = selectedSignalName(), count = names().length, busyText = signalsActionBusy && signalsActionOperation === "import_workspace" ? "Импорт…" : "Импортировать", errorHost = signalsDeleteOpen ? deletion : workspace, acknowledged = signalsDeleteOpen && signalsDeleteAcknowledged, variable = document.querySelector("[data-testid='signals-workspace-variable-input']"), rate = document.querySelector("[data-testid='signals-workspace-sample-rate-input']"), sampleText = rate ? rate.value.trim() : "", sampleRate = Number(sampleText), variableInvalid = !!(variable && variable.dataset.validationInvalid === "true"), sampleRateInvalid = !!(sampleText && (!isFinite(sampleRate) || sampleRate <= 0)); if (workspace) workspace.hidden = !signalsWorkspaceOpen; if (deletion) deletion.hidden = !signalsDeleteOpen; setWorkspaceValidation(variableInvalid, sampleRateInvalid); if (signalsWorkspaceOpen && signalsActionError && (variableInvalid || sampleRateInvalid)) focusLater(variableInvalid ? variable : rate); if (errorPanel) { errorPanel.hidden = !signalsActionError; if (errorText) errorText.textContent = signalsActionError; if (errorHost && typeof errorHost.querySelector === "function") { var body = errorHost.querySelector(".signals-dialog-body"); if (body && errorPanel.parentNode !== body && typeof body.appendChild === "function") body.appendChild(errorPanel); } } ["signals-workspace-cancel", "signals-workspace-close"].forEach(function(id) { var button = document.querySelector("[data-testid='" + id + "']"); if (button) button.disabled = signalsActionBusy && signalsActionOperation === "import_workspace"; }); ["signals-delete-cancel", "signals-delete-close"].forEach(function(id) { var button = document.querySelector("[data-testid='" + id + "']"); if (button) { button.disabled = signalsActionBusy && signalsActionOperation === "delete"; if (id === "signals-delete-cancel") button.hidden = acknowledged; } }); var submit = document.querySelector("[data-testid='signals-workspace-submit']"); if (submit) { submit.disabled = signalsActionBusy && signalsActionOperation === "import_workspace"; submit.textContent = busyText; } var confirm = document.querySelector("[data-testid='signals-delete-confirm']"), done = document.querySelector("[data-testid='signals-delete-done']"), success = document.querySelector("[data-testid='signals-delete-success']"), successName = document.querySelector("[data-testid='signals-delete-success-name']"), description = document.querySelector("#signals-delete-description"), title = document.querySelector("#signals-delete-title"); if (confirm) { confirm.disabled = acknowledged || (signalsActionBusy && signalsActionOperation === "delete"); confirm.hidden = acknowledged; } if (done) done.hidden = !acknowledged; if (success) success.hidden = !acknowledged; if (successName) successName.textContent = signalsDeleteTarget || ""; if (description) description.hidden = acknowledged; if (title) title.textContent = acknowledged ? "Сигнал удалён" : "Удалить сигнал?"; var name = document.querySelector("[data-testid='signals-delete-name']"); if (name) name.textContent = signalsDeleteTarget || ""; var add = document.querySelector("[data-testid='signals-add-action']"); if (add) add.disabled = false; }
  function enabledFocusTarget(control) { return !!(control && !control.disabled && !control.hidden); }
  function focusSignalsDismissal(fallback) { var selected = document.querySelector(".signal-row.is-selected"), add = document.querySelector("[data-testid='signals-add-action']"), target = enabledFocusTarget(fallback) ? fallback : enabledFocusTarget(selected) ? selected : add; focusLater(target); }
  function setSignalsDialogState() { var workspace = document.querySelector("[data-testid='signals-workspace-dialog']"), deletion = document.querySelector("[data-testid='signals-delete-dialog']"), errorPanel = document.querySelector("[data-testid='signals-action-error']"), errorText = document.querySelector("[data-testid='signals-action-error-text']"), selected = selectedSignalName(), count = names().length, workspaceAcknowledged = signalsWorkspaceOpen && signalsWorkspaceAcknowledged, deleteAcknowledged = signalsDeleteOpen && signalsDeleteAcknowledged, variable = document.querySelector("[data-testid='signals-workspace-variable-input']"), rate = document.querySelector("[data-testid='signals-workspace-sample-rate-input']"), form = document.querySelector("[data-testid='signals-workspace-form']"), workspaceSuccess = document.querySelector("[data-testid='signals-workspace-success']"), workspaceSuccessName = document.querySelector("[data-testid='signals-workspace-success-name']"), workspaceDone = document.querySelector("[data-testid='signals-workspace-done']"), workspaceSubmit = document.querySelector("[data-testid='signals-workspace-submit']"), workspaceCancel = document.querySelector("[data-testid='signals-workspace-cancel']"), sampleText = rate ? rate.value.trim() : "", sampleRate = Number(sampleText), variableInvalid = !!(variable && variable.dataset.validationInvalid === "true"), sampleRateInvalid = !!(sampleText && (!isFinite(sampleRate) || sampleRate <= 0)), errorHost = signalsDeleteOpen ? deletion : workspace; if (workspace) workspace.hidden = !signalsWorkspaceOpen; if (deletion) deletion.hidden = !signalsDeleteOpen; setWorkspaceValidation(variableInvalid, sampleRateInvalid); if (form) form.hidden = workspaceAcknowledged; if (workspaceSuccess) workspaceSuccess.hidden = !workspaceAcknowledged; if (workspaceSuccessName) workspaceSuccessName.textContent = signalsWorkspaceSuccessName; if (workspaceDone) workspaceDone.hidden = !workspaceAcknowledged; if (workspaceSubmit) { workspaceSubmit.hidden = workspaceAcknowledged; workspaceSubmit.disabled = workspaceAcknowledged || (signalsActionBusy && signalsActionOperation === "import_workspace"); workspaceSubmit.textContent = signalsActionBusy && signalsActionOperation === "import_workspace" ? "Импорт…" : "Импортировать"; } if (workspaceCancel) { workspaceCancel.hidden = workspaceAcknowledged; workspaceCancel.disabled = signalsActionBusy && signalsActionOperation === "import_workspace"; } ["signals-workspace-close"].forEach(function(id) { var button = document.querySelector("[data-testid='" + id + "']"); if (button) button.disabled = signalsActionBusy && signalsActionOperation === "import_workspace"; }); if (signalsWorkspaceOpen && signalsActionError && (variableInvalid || sampleRateInvalid)) focusFirstInvalid(variableInvalid ? variable : rate); if (errorPanel) { errorPanel.hidden = !signalsActionError; if (errorText) errorText.textContent = signalsActionError; if (errorHost && typeof errorHost.querySelector === "function") { var body = errorHost.querySelector(".signals-dialog-body"); if (body && errorPanel.parentNode !== body && typeof body.appendChild === "function") body.appendChild(errorPanel); } } ["signals-delete-cancel", "signals-delete-close"].forEach(function(id) { var button = document.querySelector("[data-testid='" + id + "']"); if (button) { button.disabled = signalsActionBusy && signalsActionOperation === "delete"; if (id === "signals-delete-cancel") button.hidden = deleteAcknowledged; } }); var confirm = document.querySelector("[data-testid='signals-delete-confirm']"), done = document.querySelector("[data-testid='signals-delete-done']"), success = document.querySelector("[data-testid='signals-delete-success']"), successName = document.querySelector("[data-testid='signals-delete-success-name']"), description = document.querySelector("#signals-delete-description"), title = document.querySelector("#signals-delete-title"); if (confirm) { confirm.disabled = deleteAcknowledged || (signalsActionBusy && signalsActionOperation === "delete"); confirm.hidden = deleteAcknowledged; } if (done) done.hidden = !deleteAcknowledged; if (success) success.hidden = !deleteAcknowledged; if (successName) successName.textContent = signalsDeleteTarget || ""; if (description) description.hidden = deleteAcknowledged; if (title) title.textContent = deleteAcknowledged ? "Сигнал удалён" : "Удалить сигнал?"; var name = document.querySelector("[data-testid='signals-delete-name']"); if (name) name.textContent = signalsDeleteTarget || ""; var add = document.querySelector("[data-testid='signals-add-action']"); if (add) add.disabled = false; }
  function closeSignalsWorkspace() { if (!signalsActionBusy) { var variable = document.querySelector("[data-testid='signals-workspace-variable-input']"); signalsWorkspaceOpen = false; signalsWorkspaceAcknowledged = false; signalsWorkspaceSuccessName = ""; signalsActionError = ""; if (variable) variable.dataset.validationInvalid = "false"; setWorkspaceValidation(false, false); setSignalsModalBackground(false); focusSignalsDismissal(signalsFocusRestore); signalsFocusRestore = null; } renderSignalsActions(); }
  function closeSignalsDelete() { if (!signalsActionBusy) { signalsDeleteOpen = false; signalsDeleteTarget = null; signalsDeleteAcknowledged = false; signalsActionError = ""; setSignalsModalBackground(false); focusSignalsDismissal(signalsFocusRestore); signalsFocusRestore = null; } renderSignalsActions(); }
  function signalsAction(intent) { if (signalsActionBusy || !state || (intent.operation === "import_workspace" && !workspaceIntentValid(intent)) || !signalIntentValid(intent)) return; signalsActionBusy = true; signalsActionOperation = intent.operation; signalsActionError = ""; signalsToolbarError = ""; renderSignalsActions(); function attempt() { return api.signals(signalPayload(intent)).then(function(snapshot) { if (!accept(snapshot)) throw new Error("Некорректный snapshot сервера."); if (intent.operation === "delete") signalsDeleteAcknowledged = true; if (intent.operation === "import_workspace") { signalsWorkspaceAcknowledged = true; signalsWorkspaceSuccessName = state.row_selected_signal || ""; } signalsToolbarError = ""; }).catch(function(e) { if (e && e.status===409 && e.payload && e.payload.current) { if (!accept(e.payload.current)) return; if (!intent.staleRetry && signalIntentValid(intent)) { intent.staleRetry = true; return attempt(); } } if (intent.operation === "duplicate" || intent.operation === "extract_time_limits") signalsToolbarError = signalActionErrorMessage(e); else signalsActionError = signalActionErrorMessage(e); }); } attempt().finally(function() { signalsActionBusy = false; signalsActionOperation = null; render(); renderSignalsActions(); if (intent.operation === "delete" && signalsDeleteAcknowledged) focusLater(document.querySelector("[data-testid='signals-delete-done']")); if (intent.operation === "import_workspace" && signalsWorkspaceAcknowledged) focusLater(document.querySelector("[data-testid='signals-workspace-done']")); }); }
  function renderSignalsToolbarError() { var panel = document.querySelector("[data-testid='signals-toolbar-error']"), text = document.querySelector("[data-testid='signals-toolbar-error-text']"); if (panel) panel.hidden = !signalsToolbarError; if (text) text.textContent = signalsToolbarError; }
  function renderSignalsActions() { setSignalsDialogState(); renderSignalsToolbarError(); }
  function closeSignalsWorkspace() { if (!signalsActionBusy) { var variable = document.querySelector("[data-testid='signals-workspace-variable-input']"); signalsWorkspaceOpen = false; signalsActionError = ""; if (variable) variable.dataset.validationInvalid = "false"; setWorkspaceValidation(false, false); setSignalsModalBackground(false); focusLater(signalsFocusRestore); signalsFocusRestore = null; } renderSignalsActions(); }
  function closeSignalsDelete() { if (!signalsActionBusy) { signalsDeleteOpen = false; signalsDeleteTarget = null; signalsDeleteAcknowledged = false; signalsActionError = ""; setSignalsModalBackground(false); focusLater(signalsFocusRestore); signalsFocusRestore = null; } renderSignalsActions(); }
  function workspaceIntentValid(intent) { var variable = document.querySelector("[data-testid='signals-workspace-variable-input']"), rate = document.querySelector("[data-testid='signals-workspace-sample-rate-input']"), variableInvalid = !String(intent.variable_name || "").trim(), sampleRateInvalid = intent.sample_rate_hz !== null && (!isFinite(intent.sample_rate_hz) || intent.sample_rate_hz <= 0); if (!variableInvalid && !sampleRateInvalid) return true; if (variable) variable.dataset.validationInvalid = variableInvalid ? "true" : "false"; setWorkspaceValidation(variableInvalid, sampleRateInvalid); signalsActionError = variableInvalid ? "Введите имя переменной рабочей области." : "Введите конечную положительную частоту дискретизации в Гц."; renderSignalsActions(); focusFirstInvalid(variableInvalid ? variable : rate); return false; }
  function signalsAction(intent) { if (signalsActionBusy || !state || (intent.operation === "import_workspace" && !workspaceIntentValid(intent)) || !signalIntentValid(intent)) return; signalsActionBusy = true; signalsActionOperation = intent.operation; signalsActionError = ""; signalsToolbarError = ""; renderSignalsActions(); function attempt() { return api.signals(signalPayload(intent)).then(function(snapshot) { if (!accept(snapshot)) throw new Error("Некорректный snapshot сервера."); if (intent.operation === "delete") signalsDeleteAcknowledged = true; signalsToolbarError = ""; }).catch(function(e) { if (e && e.status===409 && e.payload && e.payload.current) { if (!accept(e.payload.current)) return; if (!intent.staleRetry && signalIntentValid(intent)) { intent.staleRetry = true; return attempt(); } } if (intent.operation === "duplicate" || intent.operation === "extract_time_limits") signalsToolbarError = signalActionErrorMessage(e); else signalsActionError = signalActionErrorMessage(e); }); } attempt().finally(function() { signalsActionBusy = false; signalsActionOperation = null; render(); renderSignalsActions(); }); }
  function bindSignalsActions() { var add = document.querySelector("[data-testid='signals-add-action']"), menu = document.querySelector("[data-testid='signals-add-menu']"), workspace = document.querySelector("[data-testid='signals-add-workspace-action']"), workspaceSubmit = document.querySelector("[data-testid='signals-workspace-submit']"), workspaceCancel = document.querySelector("[data-testid='signals-workspace-cancel']"), workspaceClose = document.querySelector("[data-testid='signals-workspace-close']"), deleteConfirm = document.querySelector("[data-testid='signals-delete-confirm']"), deleteCancel = document.querySelector("[data-testid='signals-delete-cancel']"), deleteClose = document.querySelector("[data-testid='signals-delete-close']"); if (add) { add.addEventListener("click", function() { if (!signalsActionBusy) setSignalsAddMenu(!signalsAddMenuOpen, !signalsAddMenuOpen); }); add.addEventListener("keydown", function(e) { if (["ArrowDown", "Enter", " ", "Spacebar"].indexOf(e.key) >= 0) { e.preventDefault(); setSignalsAddMenu(true, true); } }); } if (workspace) workspace.addEventListener("click", function() { ["signals-workspace-variable-input", "signals-workspace-name-input", "signals-workspace-sample-rate-input"].forEach(function(id) { var input = document.querySelector("[data-testid='" + id + "']"); if (input) input.value = ""; }); signalsFocusRestore = add; setSignalsAddMenu(false); signalsWorkspaceOpen = true; setSignalsModalBackground(true); renderSignalsActions(); focusLater(document.querySelector("[data-testid='signals-workspace-variable-input']")); }); if (workspaceSubmit) workspaceSubmit.addEventListener("click", function() { var variable = document.querySelector("[data-testid='signals-workspace-variable-input']"), name = document.querySelector("[data-testid='signals-workspace-name-input']"), rate = document.querySelector("[data-testid='signals-workspace-sample-rate-input']"), sampleText = rate ? rate.value.trim() : "", sampleRate = sampleText === "" ? null : Number(sampleText); if (sampleText && (!isFinite(sampleRate) || sampleRate <= 0)) { signalsActionError = "Введите конечную положительную частоту дискретизации в Гц."; renderSignalsActions(); return; } signalsAction({ operation:"import_workspace", variable_name:variable ? variable.value : "", signal_name:name && name.value === "" ? null : name ? name.value : null, sample_rate_hz:sampleRate }); }); [workspaceCancel, workspaceClose].forEach(function(button) { if (button) button.addEventListener("click", closeSignalsWorkspace); }); if (deleteConfirm) deleteConfirm.addEventListener("click", function() { if (signalsDeleteTarget && names().length > 1) signalsAction({ operation:"delete", signal_name:signalsDeleteTarget }); }); [deleteCancel, deleteClose].forEach(function(button) { if (button) button.addEventListener("click", closeSignalsDelete); }); if (menu) menu.addEventListener("keydown", function(e) { var items = [workspace].filter(Boolean), current = items.indexOf(e.target), next; if (e.key === "Escape") { e.preventDefault(); setSignalsAddMenu(false, true); return; } if (["ArrowDown", "ArrowUp"].indexOf(e.key) < 0 || !items.length) return; e.preventDefault(); next = items[(current + (e.key === "ArrowDown" ? 1 : items.length - 1)) % items.length]; if (next && !next.disabled) next.focus(); }); if (document.addEventListener) { document.addEventListener("click", function(e) { if (!signalsAddMenuOpen || (menu && menu.contains && menu.contains(e.target)) || (add && add.contains && add.contains(e.target))) return; setSignalsAddMenu(false); }); document.addEventListener("scroll", function() { if (signalsAddMenuOpen) setSignalsAddMenu(false); }, true); document.addEventListener("keydown", trapSignalsKeyboard); } if (window.addEventListener) window.addEventListener("resize", function() { if (signalsAddMenuOpen) setSignalsAddMenu(false); }); }
  function trapSignalsKeyboard(e) { var browserSuccess = workspaceNode && workspaceNode("signals-workspace-success"), browserDialog = workspaceBrowser && (workspaceBrowser.open ? workspaceNode("signals-workspace-dialog") : browserSuccess && !browserSuccess.hidden ? browserSuccess : null), dialog = activeSignalsDialog(), controls, current; if (browserDialog) { if (e.key === "Escape" || e.key === "Enter") { e.preventDefault(); e.stopImmediatePropagation(); return; } if (e.key !== "Tab" || typeof browserDialog.querySelectorAll !== "function") return; controls = Array.prototype.slice.call(browserDialog.querySelectorAll("input, button")).filter(function(control) { return !control.disabled && !control.hidden; }); if (!controls.length) return; current = controls.indexOf(document.activeElement); if (e.shiftKey && current <= 0) { e.preventDefault(); controls[controls.length - 1].focus(); } else if (!e.shiftKey && current === controls.length - 1) { e.preventDefault(); controls[0].focus(); } return; } if (!dialog) return; if (e.key === "Escape" && !signalsActionBusy) { e.preventDefault(); if (signalsWorkspaceOpen) closeSignalsWorkspace(); else closeSignalsDelete(); return; } if (e.key !== "Tab" || signalsActionBusy || typeof dialog.querySelectorAll !== "function") return; controls = Array.prototype.slice.call(dialog.querySelectorAll("input, button:not([disabled])")).filter(function(control) { return !control.hidden; }); if (!controls.length) return; current = controls.indexOf(document.activeElement); if (e.shiftKey && current <= 0) { e.preventDefault(); controls[controls.length - 1].focus(); } else if (!e.shiftKey && current === controls.length - 1) { e.preventDefault(); controls[0].focus(); } }
  function bindSignalsWorkspaceValidation() { var variable = document.querySelector("[data-testid='signals-workspace-variable-input']"), rate = document.querySelector("[data-testid='signals-workspace-sample-rate-input']"), done = document.querySelector("[data-testid='signals-delete-done']"); function updateValidation() { var sampleText = rate ? rate.value.trim() : "", sampleRate = Number(sampleText), variableInvalid = !!(variable && variable.dataset.validationInvalid === "true"), sampleRateInvalid = !!(sampleText && (!isFinite(sampleRate) || sampleRate <= 0)); setWorkspaceValidation(variableInvalid, sampleRateInvalid); } if (variable) variable.addEventListener("input", function() { variable.dataset.validationInvalid = variable.value.trim() ? "false" : "true"; updateValidation(); }); if (rate) rate.addEventListener("input", updateValidation); if (done) done.addEventListener("click", closeSignalsDelete); }
  function bindSignalsErrorClose() { var close = document.querySelector("[data-testid='signals-action-error-close']"); if (close) close.addEventListener("click", function() { signalsActionError = ""; renderSignalsActions(); }); }
  function bindSignalsToolbarErrorClose() { var close = document.querySelector("[data-testid='signals-toolbar-error-close']"); if (close) close.addEventListener("click", function() { signalsToolbarError = ""; renderSignalsActions(); }); }
  function bindSignalsMenuKeyboard() { var menu = document.querySelector("[data-testid='signals-add-menu']"), add = document.querySelector("[data-testid='signals-add-action']"), workspace = document.querySelector("[data-testid='signals-add-workspace-action']"); if (!menu) return; menu.addEventListener("keydown", function(e) { var items = [workspace].filter(function(item) { return item && !item.disabled; }), current = items.indexOf(e.target), next; if (!items.length) return; if (e.key === "Home") { e.preventDefault(); items[0].focus(); return; } if (e.key === "End") { e.preventDefault(); items[items.length - 1].focus(); return; } if (e.key === "Tab") { setSignalsAddMenu(false); return; } if (e.key !== "ArrowDown" && e.key !== "ArrowUp") return; e.preventDefault(); next = items[(current + (e.key === "ArrowDown" ? 1 : items.length - 1)) % items.length]; next.focus(); }); if (add) add.addEventListener("keydown", function(e) { if (e.key === "Home" || e.key === "End") e.preventDefault(); }); }
  function setSignalsAddMenu(open, focus) { var trigger = document.querySelector("[data-testid='signals-add-action']"), menu = document.querySelector("[data-testid='signals-add-menu']"), first = document.querySelector("[data-testid='signals-add-workspace-action']"), active = document.activeElement, wasInside = !!(menu && menu.contains && menu.contains(active)); signalsAddMenuOpen = !!open; if (signalsAddMenuOpen) signalsToolbarError = ""; if (menu) menu.hidden = !signalsAddMenuOpen; if (trigger) trigger.setAttribute("aria-expanded", signalsAddMenuOpen ? "true" : "false"); if (signalsAddMenuOpen && focus) focusLater(first); if (!signalsAddMenuOpen && (focus || wasInside)) focusLater(trigger); }
  function closeSignalsWorkspace() { if (!signalsActionBusy) { var variable = document.querySelector("[data-testid='signals-workspace-variable-input']"); signalsWorkspaceOpen = false; signalsWorkspaceAcknowledged = false; signalsWorkspaceSuccessName = ""; signalsActionError = ""; if (variable) variable.dataset.validationInvalid = "false"; setWorkspaceValidation(false, false); setSignalsModalBackground(false); focusSignalsDismissal(signalsFocusRestore); signalsFocusRestore = null; } renderSignalsActions(); }
  function closeSignalsDelete() { if (!signalsActionBusy) { signalsDeleteOpen = false; signalsDeleteTarget = null; signalsDeleteAcknowledged = false; signalsActionError = ""; setSignalsModalBackground(false); focusSignalsDismissal(signalsFocusRestore); signalsFocusRestore = null; } renderSignalsActions(); }
  function signalsAction(intent) { if (signalsActionBusy || !state || (intent.operation === "import_workspace" && !workspaceIntentValid(intent)) || !signalIntentValid(intent)) return; signalsActionBusy = true; signalsActionOperation = intent.operation; signalsActionError = ""; signalsToolbarError = ""; render(); function attempt() { return api.signals(signalPayload(intent)).then(function(snapshot) { if (!accept(snapshot)) throw new Error("Некорректный snapshot сервера."); if (intent.operation === "delete") { signalsDeleteAcknowledged = true; focusLater(document.querySelector("[data-testid='signals-delete-done']")); } if (intent.operation === "import_workspace") { signalsWorkspaceAcknowledged = true; signalsWorkspaceSuccessName = state.row_selected_signal || ""; focusLater(document.querySelector("[data-testid='signals-workspace-done']")); } signalsToolbarError = ""; }).catch(function(e) { if (e && e.status===409 && e.payload && e.payload.current) { if (!accept(e.payload.current)) return; if (!intent.staleRetry && signalIntentValid(intent)) { intent.staleRetry = true; return attempt(); } } if (intent.operation === "duplicate" || intent.operation === "extract_time_limits") signalsToolbarError = signalActionErrorMessage(e); else signalsActionError = signalActionErrorMessage(e); }); } attempt().finally(function() { signalsActionBusy = false; signalsActionOperation = null; render(); renderSignalsActions(); }); }
  function bindSignalsWorkspaceValidation() { var variable = document.querySelector("[data-testid='signals-workspace-variable-input']"), rate = document.querySelector("[data-testid='signals-workspace-sample-rate-input']"), deleteDone = document.querySelector("[data-testid='signals-delete-done']"), workspaceDone = document.querySelector("[data-testid='signals-workspace-done']"); function updateValidation() { var sampleText = rate ? rate.value.trim() : "", sampleRate = Number(sampleText), variableInvalid = !!(variable && variable.dataset.validationInvalid === "true"), sampleRateInvalid = !!(sampleText && (!isFinite(sampleRate) || sampleRate <= 0)); setWorkspaceValidation(variableInvalid, sampleRateInvalid); } if (variable) variable.addEventListener("input", function() { variable.dataset.validationInvalid = variable.value.trim() ? "false" : "true"; updateValidation(); }); if (rate) rate.addEventListener("input", updateValidation); if (deleteDone) deleteDone.addEventListener("click", closeSignalsDelete); if (workspaceDone) workspaceDone.addEventListener("click", closeSignalsWorkspace); }
  function bindSignalsMenuKeyboard() { var menu = document.querySelector("[data-testid='signals-add-menu']"), add = document.querySelector("[data-testid='signals-add-action']"), workspace = document.querySelector("[data-testid='signals-add-workspace-action']"); if (!menu) return; menu.addEventListener("keydown", function(e) { var items = [workspace].filter(function(item) { return item && !item.disabled; }), current = items.indexOf(e.target), next; /* Tab and Shift+Tab leave the transient menu for deterministic toolbar controls. */ if (e.key === "Tab") { e.preventDefault(); setSignalsAddMenu(false); focusLater(add); return; } if (!items.length) return; if (e.key === "Home") { e.preventDefault(); items[0].focus(); return; } if (e.key === "End") { e.preventDefault(); items[items.length - 1].focus(); return; } if (e.key !== "ArrowDown" && e.key !== "ArrowUp") return; e.preventDefault(); next = items[(current + (e.key === "ArrowDown" ? 1 : items.length - 1)) % items.length]; next.focus(); }); }
  function bindSignalsKeyboardActivationGuard() { var add = document.querySelector("[data-testid='signals-add-action']"); if (!add || !document.addEventListener) return; document.addEventListener("keydown", function(e) { if (e.target !== add || ["Enter", " ", "Spacebar"].indexOf(e.key) < 0) { trapSignalsKeyboard(e); return; } signalsAddKeyboardActivation = true; window.setTimeout(function() { signalsAddKeyboardActivation = false; }, 0); }, true); document.addEventListener("click", function(e) { if (!signalsAddKeyboardActivation || (e.target !== add && !(add.contains && add.contains(e.target)))) return; signalsAddKeyboardActivation = false; e.preventDefault(); e.stopImmediatePropagation(); }, true); }
  function sessionNode(id) { return document.querySelector("[data-testid='" + id + "']"); }
  function sessionMessage(error) { var payload = error && error.payload, nested = payload && payload.error, fields = nested && nested.fields, details = fields && typeof fields === "object" ? Object.keys(fields).map(function(key) { return key + ": " + fields[key]; }).join("; ") : ""; return details || (nested && nested.message) || (error && error.message) || "Не удалось обработать сессию."; }
  function renderSessionUi() { var dialog = sessionNode("session-import-dialog"), input = sessionNode("session-file-input"), name = sessionNode("session-file-name"), error = sessionNode("session-import-error"), success = sessionNode("session-import-success"), submit = sessionNode("session-import-submit"), cancel = sessionNode("session-import-cancel"), close = sessionNode("session-import-close"), exportButton = sessionNode("export-action"), importButton = sessionNode("import-session-action"), status = sessionNode("session-action-status"); if (dialog) { dialog.hidden = !sessionUi.open; dialog.setAttribute("aria-busy", sessionUi.busy ? "true" : "false"); } if (name) name.textContent = sessionUi.file ? sessionUi.file.name : "Файл не выбран."; if (error) { error.hidden = !sessionUi.error; error.textContent = sessionUi.error; } if (success) success.hidden = !sessionUi.success; if (status) { status.hidden = !sessionUi.status; status.textContent = sessionUi.status; status.classList.toggle("is-error", sessionUi.statusError); } if (input) input.disabled = sessionUi.busy; if (submit) { submit.disabled = sessionUi.busy || !sessionUi.document; submit.setAttribute("aria-busy", sessionUi.busy ? "true" : "false"); submit.textContent = sessionUi.busy ? "Импорт…" : "Импортировать"; } [cancel, close].forEach(function(control) { if (control) control.disabled = sessionUi.busy; }); if (exportButton) { exportButton.disabled = sessionUi.busy; exportButton.setAttribute("aria-busy", sessionUi.busy ? "true" : "false"); } if (importButton) importButton.disabled = sessionUi.busy; }
  function closeSessionImport() { if (sessionUi.busy) return; sessionUi.open = false; sessionUi.error = ""; sessionUi.success = false; sessionUi.document = null; sessionUi.file = null; var input = sessionNode("session-file-input"); if (input) input.value = ""; setSignalsModalBackground(false); renderSessionUi(); if (sessionUi.focus && typeof sessionUi.focus.focus === "function") sessionUi.focus.focus(); sessionUi.focus = null; }
  function openSessionImport() { if (sessionUi.busy) return; sessionUi.open = true; sessionUi.error = ""; sessionUi.success = false; sessionUi.document = null; sessionUi.file = null; sessionUi.focus = sessionNode("import-session-action"); setSignalsModalBackground(true); renderSessionUi(); focusLater(sessionNode("session-file-input")); }
  function readSessionFile(file) { if (!file) return; sessionUi.file = file; sessionUi.document = null; sessionUi.error = ""; sessionUi.success = false; sessionUi.busy = true; renderSessionUi(); Promise.resolve(typeof file.text === "function" ? file.text() : new Promise(function(resolve, reject) { var reader = new FileReader(); reader.onload = function() { resolve(reader.result); }; reader.onerror = reject; reader.readAsText(file); })).then(function(text) { var value = JSON.parse(text); if (!plainObject(value)) throw new Error("Файл должен содержать JSON-документ сессии."); sessionUi.document = value; }).catch(function(error) { sessionUi.file = null; sessionUi.document = null; sessionUi.error = error instanceof SyntaxError ? "Не удалось прочитать JSON-документ сессии." : sessionMessage(error); }).finally(function() { sessionUi.busy = false; renderSessionUi(); }); }
  function downloadSession() { if (sessionUi.busy || !api || typeof api.session !== "function") return; sessionUi.busy = true; sessionUi.error = ""; sessionUi.status = "Сохранение сессии…"; sessionUi.statusError = false; renderSessionUi(); api.session().then(function(response) { if (!response || response.ok !== true || !plainObject(response.document)) throw new Error("Сервер вернул некорректный документ сессии."); var blob = new Blob([JSON.stringify(response.document, null, 2)], { type:"application/json" }), url = window.URL.createObjectURL(blob), link = document.createElement("a"); link.href = url; link.download = "signal-analyser-session.json"; link.style.display = "none"; document.body.appendChild(link); link.click(); link.remove(); window.setTimeout(function() { window.URL.revokeObjectURL(url); }, 0); sessionUi.status = "Сессия сохранена."; }).catch(function(error) { sessionUi.status = sessionMessage(error); sessionUi.statusError = true; }).finally(function() { sessionUi.busy = false; renderSessionUi(); }); }
  function reloadSessionState() { loading(true, "Загрузка импортированной сессии…"); return api.getState().then(function(snapshot) { if (!accept(snapshot)) throw new Error("Сервер вернул некорректное состояние после импорта."); render(); }).finally(function() { loading(false); }); }
  function importSession() { if (sessionUi.busy || !sessionUi.document || !state || !api || typeof api.importSession !== "function") return; sessionUi.busy = true; sessionUi.error = ""; sessionUi.success = false; renderSessionUi(); api.importSession({ state_revision:state.state_revision, document:sessionUi.document }).then(function(response) { if (!response || response.ok !== true) throw new Error("Сервер не подтвердил импорт сессии."); return reloadSessionState(); }).then(function() { sessionUi.success = true; sessionUi.document = null; }).catch(function(error) { sessionUi.error = sessionMessage(error); if (error && error.status === 409) return reloadSessionState().catch(function() { /* The error remains visible and the user can choose the file again. */ }); }).finally(function() { sessionUi.busy = false; renderSessionUi(); }); }
  function bindSessionControls() { var exportButton = sessionNode("export-action"), importButton = sessionNode("import-session-action"), input = sessionNode("session-file-input"), submit = sessionNode("session-import-submit"), cancel = sessionNode("session-import-cancel"), close = sessionNode("session-import-close"), dialog = sessionNode("session-import-dialog"); if (exportButton) exportButton.addEventListener("click", downloadSession); if (importButton) importButton.addEventListener("click", openSessionImport); if (input) input.addEventListener("change", function() { readSessionFile(input.files && input.files[0]); }); if (submit) submit.addEventListener("click", importSession); [cancel, close].forEach(function(control) { if (control) control.addEventListener("click", closeSessionImport); }); if (dialog) dialog.addEventListener("keydown", function(event) { if (event.key === "Escape") { event.preventDefault(); closeSessionImport(); } }); renderSessionUi(); }
  function bind() {
    window.addEventListener("signal-analyser-settings-state", function(e) { if (e && e.detail && accept(e.detail)) { error(""); loading(false); render(); } });
    window.addEventListener("signal-analyser-settings-revision", function(e) { var detail = e && e.detail || {}, current; if (!state || typeof detail.revision !== "number") return; current = typeof state.state_revision === "number" ? state.state_revision : -Infinity; if (detail.revision < current) return; state.state_revision = detail.revision; root.dataset.stateRevision = String(detail.revision); });
    window.addEventListener("signal-analyser-settings-presentation", function(e) { var settings = e && e.detail, values = {}, ui; if (!settings || typeof settings.display_id !== "string" || !Array.isArray(settings.fields)) return; settings.fields.forEach(function(field) { if (field && (field.id === "display.show_legend" || field.id === "time.normalize_y" || field.id === "time.show_markers")) values[field.id] = field.value; }); ui = displayUi[settings.display_id] || {}; if (typeof values["display.show_legend"] === "boolean") ui.showLegend = values["display.show_legend"]; if (typeof values["time.normalize_y"] === "boolean") ui.normalizeY = values["time.normalize_y"]; if (typeof values["time.show_markers"] === "boolean") ui.showMarkers = values["time.show_markers"]; displayUi[settings.display_id] = ui; if (settings.display_id === activeDisplayId) render(); });
    bindSignalsKeyboardActivationGuard();
    bindSessionControls();
    bindSignalsActions();
    bindSignalsWorkspaceValidation();
    bindSignalsErrorClose();
    bindSignalsToolbarErrorClose();
    bindSignalsMenuKeyboard();
    document.querySelector("[data-testid='display-tabs']").addEventListener("click", function(e) {
      var close = e.target.closest("[data-close-display]");
      if (close) { closeDisplay(close.dataset.closeDisplay); return; }
      var tab = e.target.closest("[data-display-id]");
      if (tab) selectDisplay(tab.dataset.displayId);
      if (e.target.closest("[data-testid='add-display']")) addDisplay();
    });
    var displayTabs = document.querySelector("[data-testid='display-tabs']");
    displayTabs.addEventListener("dragstart", function(e) { var tab = e.target.closest("[data-display-id]"); if (!tab || displayReorderPending) { e.preventDefault(); return; } draggedDisplayId = tab.dataset.displayId; if (e.dataTransfer) e.dataTransfer.effectAllowed = "move"; });
    displayTabs.addEventListener("dragover", function(e) { if (!displayReorderPending && draggedDisplayId && e.target.closest("[data-display-id]")) e.preventDefault(); });
    displayTabs.addEventListener("drop", function(e) { var target = e.target.closest("[data-display-id]"), order; if (!target || !draggedDisplayId || displayReorderPending) return; e.preventDefault(); order = reorderedDisplayIds(draggedDisplayId, target.dataset.displayId); if (order) requestDisplayReorder(order, draggedDisplayId); draggedDisplayId = null; });
    displayTabs.addEventListener("dragend", function() { draggedDisplayId = null; });
    displayTabs.addEventListener("keydown", function(e) { var tab = e.target.closest("[data-display-id]"); if (!tab || displayReorderPending || !e.altKey || (e.key !== "ArrowLeft" && e.key !== "ArrowRight")) return; e.preventDefault(); moveDisplayTab(tab.dataset.displayId, e.key === "ArrowLeft" ? -1 : 1); });
    document.querySelector("[data-testid='plot-type-select']").addEventListener("change", function(e) { update({ plot:e.target.value }); });
    document.querySelector("[data-testid='settings-view-select']").addEventListener("change", function(e) { update({ plot:e.target.value }); });
    ["show-legend-checkbox", "normalize-y-checkbox", "show-markers-checkbox"].forEach(function(id) {
      document.querySelector("[data-testid='" + id + "']").addEventListener("change", function(e) {
        var change = {};
        change[id === "show-legend-checkbox" ? "showLegend" : id === "normalize-y-checkbox" ? "normalizeY" : "showMarkers"] = e.target.checked;
        update(change);
      });
    });
    var statisticsAction = document.querySelector("[data-testid='signal-statistics-action']");
    if (statisticsAction) statisticsAction.addEventListener("click", function() { activeSettingsTab = "measurements"; activateBottomTab("measurements", true); render(); });
    document.querySelector("[data-testid='find-peaks-action']").addEventListener("click", function() {
      if (fatalSnapshotError) return;
      var d = active();
      if (!d || d.plot !== "time" || peaksBusyDisplayId) return;
      peaksBusyDisplayId = d.id;
      update({ peaksEnabled:!d.peaksEnabled });
    });
    document.querySelector("[data-signal-rows]").addEventListener("change", function(e) {
      if (fatalSnapshotError) return;
      var box = e.target.closest("[data-signal-visibility]"), d = active();
      if (!box || !d) return;
      var visible = d.visible_signals.slice(), name = box.dataset.signalVisibility, source = d.analysis_signal;
      if (box.checked && visible.indexOf(name) < 0) visible.push(name);
      if (!box.checked) visible = visible.filter(function(n) { return n !== name; });
      if (!visible.length) source = null;
      else if (visible.indexOf(source) < 0) source = visible[0];
      update({ visible_signals:visible, analysis_signal:source, peaksEnabled:visible.length ? d.peaksEnabled : false });
    });
    document.querySelector("[data-signal-rows]").addEventListener("click", function(e) {
      var duplicate = e.target.closest("[data-signal-duplicate]"), remove = e.target.closest("[data-signal-delete]"), info = e.target.closest("[data-signal-info]");
      if (duplicate) { e.stopPropagation(); if (!signalsActionBusy) signalsAction({ operation:"duplicate", signal_name:duplicate.dataset.signalDuplicate }); return; }
      if (remove) { e.stopPropagation(); if (!signalsActionBusy && names().length > 1) { signalsDeleteTarget = remove.dataset.signalDelete; signalsFocusRestore = remove; signalsDeleteOpen = true; setSignalsModalBackground(true); renderSignalsActions(); focusLater(document.querySelector("[data-testid='signals-delete-confirm']")); } return; }
      if (info) { var expanded = info.getAttribute("aria-expanded") !== "true", infoRow = info.closest(".signal-row"); e.stopPropagation(); info.setAttribute("aria-expanded", expanded ? "true" : "false"); info.setAttribute("aria-label", (expanded ? "Скрыть" : "Показать") + " информацию о " + info.dataset.signalInfo); if (infoRow) infoRow.dataset.infoExpanded = expanded ? "true" : "false"; return; }
      if (fatalSnapshotError || e.target.matches("input,button")) return;
      var row = e.target.closest("[data-signal]"), d = active();
      if (!row || !d) return;
      var member = d.visible_signals.indexOf(row.dataset.signal) >= 0;
      update({ row_selected_signal:row.dataset.signal, analysis_signal:member ? row.dataset.signal : d.analysis_signal });
    });
    document.querySelector("[data-signal-rows]").addEventListener("keydown", function(e) {
      if (fatalSnapshotError || ["Enter", " ", "Spacebar"].indexOf(e.key) < 0 || e.target.matches("input,button")) return;
      var row = e.target.closest("[data-signal]"), d = active();
      if (!row || !d) return;
      e.preventDefault();
      var member = d.visible_signals.indexOf(row.dataset.signal) >= 0;
      update({ row_selected_signal:row.dataset.signal, analysis_signal:member ? row.dataset.signal : d.analysis_signal });
    });
    document.querySelector("[data-testid='toggle-all-signals']").addEventListener("change", function(e) {
      if (fatalSnapshotError) return;
      var d = active();
      if (!d) return;
      var visible = e.target.checked ? names() : [], source = visible.length ? (visible.indexOf(d.analysis_signal) >= 0 ? d.analysis_signal : visible[0]) : null;
      update({ visible_signals:visible, analysis_signal:source, peaksEnabled:visible.length ? d.peaksEnabled : false });
    });
    var columnsTrigger = document.querySelector("[data-testid='signal-columns-menu-trigger']"), columnsEye = document.querySelector("[data-testid='signal-columns-visibility-trigger']"), columnsMenu = document.querySelector("[data-testid='signal-columns-menu']");
    if (columnsTrigger) columnsTrigger.addEventListener("click", function() { signalColumnsMenuOpen = !signalColumnsMenuOpen; renderSignalColumnMenu(); if (signalColumnsMenuOpen && columnsMenu) { var first = columnsMenu.querySelector("[data-signal-column-toggle]"); if (first) first.focus(); } });
    if (columnsEye) columnsEye.addEventListener("click", function() { signalHiddenColumns = {}; signalColumnsMenuOpen = false; renderSignalColumnMenu(); });
    if (columnsMenu) columnsMenu.addEventListener("click", function(e) { var toggle = e.target.closest("[data-signal-column-toggle]"); if (!toggle) return; var key = toggle.dataset.signalColumnToggle; signalHiddenColumns[key] = !signalHiddenColumns[key]; renderSignalColumnMenu(); });
    document.addEventListener("click", function(e) { if (signalColumnsMenuOpen && !e.target.closest("[data-testid='signal-columns-menu'],[data-testid='signal-columns-menu-trigger']")) { signalColumnsMenuOpen = false; renderSignalColumnMenu(); } });
    if (columnsMenu) columnsMenu.addEventListener("keydown", function(e) { var items = Array.prototype.slice.call(columnsMenu.querySelectorAll("[data-signal-column-toggle]")), current = items.indexOf(e.target); if (e.key === "Escape") { signalColumnsMenuOpen = false; renderSignalColumnMenu(); if (columnsTrigger) columnsTrigger.focus(); return; } if (current < 0 || (e.key !== "ArrowDown" && e.key !== "ArrowUp")) return; e.preventDefault(); items[(current + (e.key === "ArrowDown" ? 1 : items.length - 1)) % items.length].focus(); });
    var overflowTrigger = document.querySelector("[data-testid='display-overflow-trigger']"), overflowMenu = document.querySelector("[data-testid='display-overflow-menu']"), clearDisplay = document.querySelector("[data-testid='clear-display-action']");
    function setOverflow(open, focus) { overflowOpen = open; if (overflowMenu) overflowMenu.hidden = !open; if (overflowTrigger) overflowTrigger.setAttribute("aria-expanded", open ? "true" : "false"); if (focus && typeof focus.focus === "function") focus.focus(); }
    function closeOverflow(focus) { setOverflow(false, focus ? overflowTrigger : null); }
    if (overflowTrigger) { overflowTrigger.addEventListener("click", function() { setOverflow(!overflowOpen, !overflowOpen ? clearDisplay : null); }); overflowTrigger.addEventListener("keydown", function(e) { if (["ArrowDown", "Enter", " ", "Spacebar"].indexOf(e.key) < 0) return; e.preventDefault(); setOverflow(true, clearDisplay); }); }
    if (clearDisplay) clearDisplay.addEventListener("click", function() {
      if (fatalSnapshotError) return;
      var d = active();
      if (!d || !d.visible_signals.length) return;
      closeOverflow(true);
      update({ visible_signals:[], analysis_signal:null, peaksEnabled:false });
    });
    if (overflowMenu) overflowMenu.addEventListener("keydown", function(e) { if (e.key === "Escape") { e.preventDefault(); closeOverflow(true); return; } if (["ArrowDown", "ArrowUp", "Home", "End"].indexOf(e.key) >= 0) { e.preventDefault(); if (clearDisplay && typeof clearDisplay.focus === "function") clearDisplay.focus(); } if (["Enter", " ", "Spacebar"].indexOf(e.key) >= 0 && e.target === clearDisplay) { e.preventDefault(); clearDisplay.click(); } });
    var bottomTablist = document.querySelector("[role='tablist'][aria-label='Данные анализатора']");
    bottomTablist.addEventListener("click", function(e) { var b = e.target.closest("[data-bottom-tab]"); if (!b || b.hidden) return; activateBottomTab(b.dataset.bottomTab, false); });
    bottomTablist.addEventListener("keydown", function(e) { var key = e.key, tabs = Array.prototype.slice.call(document.querySelectorAll("[data-bottom-tab]")).filter(function(tab) { return !tab.hidden; }), current = tabs.indexOf(e.target.closest("[data-bottom-tab]")); if (!tabs.length || current < 0 || ["ArrowLeft", "ArrowRight", "Home", "End"].indexOf(key) < 0) return; e.preventDefault(); var target = key === "Home" ? tabs[0] : key === "End" ? tabs[tabs.length - 1] : tabs[(current + (key === "ArrowRight" ? 1 : tabs.length - 1)) % tabs.length]; activateBottomTab(target.dataset.bottomTab, true); });
    document.querySelector("[data-retry]").addEventListener("click", load);
    window.addEventListener("resize", function() { if (state) draw(); });
  }
  function drain() {
    if (fatalSnapshotError || requestInFlight || !intentQueue.length || !state) return;
    var intent = intentQueue.shift();
    pendingAction = intentQueue[intentQueue.length - 1] || null;
    if (intent.kind === "view" && quarantinedDisplay(intent.target.displayId)) { drain(); return; }
    requestInFlight = true;
    loading(true, intent.kind === "reorder" ? "Сохранение порядка Display…" : "Updating display…");
    error("");
    var call = intent.kind === "view" ? api.view({ state_revision:state.state_revision, active_plot:intent.target.active_plot, row_selected_signal:intent.target.row_selected_signal, analysis_signal:intent.target.analysis_signal, visible_signals:intent.target.visible_signals, time_limits:intent.target.time_limits, measurement_kinds:intent.target.measurement_kinds, spectrum_settings:intent.target.spectrum_settings, spectrogram_settings:intent.target.spectrogram_settings, persistence_settings:intent.target.persistence_settings, peaks_enabled:intent.target.peaks_enabled }) : api.displays(Object.assign({ state_revision:state.state_revision }, intent.payload));
    call.then(function(snapshot) {
      if (intent.kind === "view" && sameView(desiredViews[intent.target.displayId], intent.target)) delete desiredViews[intent.target.displayId];
      if (!accept(snapshot)) return;
      if (intent.timeLimitsCommit) { delete timeDrafts[intent.target.displayId]; delete timeLimitsErrors[intent.target.displayId]; }
      if (intent.measurementKindsCommit) delete measurementKindsErrors[intent.target.displayId];
      if (intent.spectrumSettingsCommit) delete spectrumSettingsErrors[intent.target.displayId];
      if (intent.spectrumFrequencyLimitsCommit) { delete spectrumFrequencyLimitsDrafts[intent.target.displayId]; delete spectrumFrequencyLimitsErrors[intent.target.displayId]; }
      if (intent.spectrogramOverlapCommit) { delete spectrogramOverlapDrafts[intent.target.displayId]; delete spectrogramOverlapErrors[intent.target.displayId]; }
      if (intent.spectrogramFrequencyScaleCommit) delete spectrogramFrequencyScaleErrors[intent.target.displayId];
      if (intent.spectrogramLeakageCommit) { delete spectrogramLeakageDrafts[intent.target.displayId]; delete spectrogramLeakageErrors[intent.target.displayId]; }
      if (intent.spectrogramFrequencyLimitsCommit) { delete spectrogramFrequencyLimitsDrafts[intent.target.displayId]; delete spectrogramFrequencyLimitsErrors[intent.target.displayId]; }
      if (intent.spectrogramPowerLimitsCommit) { delete spectrogramPowerLimitsDrafts[intent.target.displayId]; delete spectrogramPowerLimitsErrors[intent.target.displayId]; }
      if (intent.persistenceLeakageCommit) { delete persistenceLeakageDrafts[intent.target.displayId]; delete persistenceLeakageErrors[intent.target.displayId]; }
      if (intent.peaksToggle && state.peaks && state.peaks.enabled === true) activeBottomTab = "peaks";
      if (intent.peaksToggle && (!state.peaks || state.peaks.enabled !== true) && activeBottomTab === "peaks") activeBottomTab = "signals";
      render();
    }).catch(function(e) {
      if (e && e.status===409 && e.payload && e.payload.current) {
        if (intent.kind === "reorder") { if (!accept(e.payload.current)) return; error("Порядок Display изменился на сервере. Повторите перестановку."); render(); return; }
        if (intent.kind === "view" && intent.staleReplayAttempted) {
          var staleReplayMatchesDesired = sameView(desiredViews[intent.target.displayId], intent.target);
          if (staleReplayMatchesDesired) delete desiredViews[intent.target.displayId];
          if (!accept(e.payload.current)) return;
          if (intent.spectrogramSettingsCommit && staleReplayMatchesDesired) {
            var staleSpectrogramDisplay = displays.filter(function(item) { return item.id === intent.target.displayId; })[0];
            if (staleSpectrogramDisplay) staleSpectrogramDisplay.spectrogram_settings = spectrogramSettings(acceptedSpectrogramSettings[intent.target.displayId] || intent.previousSpectrogramSettings);
            if (intent.spectrogramOverlapCommit) { delete spectrogramOverlapDrafts[intent.target.displayId]; spectrogramOverlapErrors[intent.target.displayId] = viewErrorMessage(e); }
            if (intent.spectrogramLeakageCommit) { delete spectrogramLeakageDrafts[intent.target.displayId]; spectrogramLeakageErrors[intent.target.displayId] = viewErrorMessage(e); }
            if (intent.spectrogramFrequencyScaleCommit) spectrogramFrequencyScaleErrors[intent.target.displayId] = viewErrorMessage(e);
            if (intent.spectrogramFrequencyLimitsCommit) { delete spectrogramFrequencyLimitsDrafts[intent.target.displayId]; spectrogramFrequencyLimitsErrors[intent.target.displayId] = viewErrorMessage(e); }
            if (intent.spectrogramPowerLimitsCommit) { delete spectrogramPowerLimitsDrafts[intent.target.displayId]; spectrogramPowerLimitsErrors[intent.target.displayId] = viewErrorMessage(e); }
          }
          if (intent.persistenceSettingsCommit && staleReplayMatchesDesired) {
            var stalePersistenceDisplay = displays.filter(function(item) { return item.id === intent.target.displayId; })[0];
            if (stalePersistenceDisplay) stalePersistenceDisplay.persistence_settings = persistenceSettings(acceptedPersistenceSettings[intent.target.displayId] || intent.previousPersistenceSettings);
            if (intent.persistenceLeakageCommit) { delete persistenceLeakageDrafts[intent.target.displayId]; persistenceLeakageErrors[intent.target.displayId] = viewErrorMessage(e); }
          }
          error("Не удалось обновить Display.");
          render();
          return;
        }
        if (!accept(e.payload.current)) return;
        render();
        if (intent.kind === "view") { intent.staleReplayAttempted = true; requeueLatestViewAfterStale(intent); } else { intentQueue.unshift(intent); pendingAction = intentQueue[intentQueue.length - 1] || null; }
        return;
      }
      if (intent.timeLimitsCommit) {
        var limitDisplay = displays.filter(function(item) { return item.id === intent.target.displayId; })[0];
        if (limitDisplay) limitDisplay.time_limits = intent.previousTimeLimits || null;
        delete timeDrafts[intent.target.displayId];
        timeLimitsErrors[intent.target.displayId] = viewErrorMessage(e);
        if (sameView(desiredViews[intent.target.displayId], intent.target)) delete desiredViews[intent.target.displayId];
      }
      if (intent.measurementKindsCommit) {
        var measurementDisplay = displays.filter(function(item) { return item.id === intent.target.displayId; })[0];
        if (measurementDisplay) measurementDisplay.measurement_kinds = intent.previousMeasurementKinds;
        measurementKindsErrors[intent.target.displayId] = viewErrorMessage(e);
        if (sameView(desiredViews[intent.target.displayId], intent.target)) delete desiredViews[intent.target.displayId];
      }
      if (intent.spectrumSettingsCommit) {
        var spectrumDisplay = displays.filter(function(item) { return item.id === intent.target.displayId; })[0];
        if (spectrumDisplay) spectrumDisplay.spectrum_settings = intent.previousSpectrumSettings;
        if (intent.spectrumFrequencyLimitsCommit) { delete spectrumFrequencyLimitsDrafts[intent.target.displayId]; spectrumFrequencyLimitsErrors[intent.target.displayId] = viewErrorMessage(e); } else spectrumSettingsErrors[intent.target.displayId] = viewErrorMessage(e);
        if (sameView(desiredViews[intent.target.displayId], intent.target)) delete desiredViews[intent.target.displayId];
      }
      if (intent.spectrogramSettingsCommit) {
        var spectrogramDisplay = displays.filter(function(item) { return item.id === intent.target.displayId; })[0];
        if (spectrogramDisplay) spectrogramDisplay.spectrogram_settings = spectrogramSettings(acceptedSpectrogramSettings[intent.target.displayId] || intent.previousSpectrogramSettings);
        if (intent.spectrogramOverlapCommit) { delete spectrogramOverlapDrafts[intent.target.displayId]; spectrogramOverlapErrors[intent.target.displayId] = viewErrorMessage(e); }
        if (intent.spectrogramLeakageCommit) { delete spectrogramLeakageDrafts[intent.target.displayId]; spectrogramLeakageErrors[intent.target.displayId] = viewErrorMessage(e); }
        if (intent.spectrogramFrequencyScaleCommit) spectrogramFrequencyScaleErrors[intent.target.displayId] = viewErrorMessage(e);
        if (intent.spectrogramFrequencyLimitsCommit) { delete spectrogramFrequencyLimitsDrafts[intent.target.displayId]; spectrogramFrequencyLimitsErrors[intent.target.displayId] = viewErrorMessage(e); }
        if (intent.spectrogramPowerLimitsCommit) { delete spectrogramPowerLimitsDrafts[intent.target.displayId]; spectrogramPowerLimitsErrors[intent.target.displayId] = viewErrorMessage(e); }
        if (sameView(desiredViews[intent.target.displayId], intent.target)) delete desiredViews[intent.target.displayId];
      }
      if (intent.persistenceSettingsCommit) {
        var persistenceDisplay = displays.filter(function(item) { return item.id === intent.target.displayId; })[0];
        if (persistenceDisplay) persistenceDisplay.persistence_settings = persistenceSettings(acceptedPersistenceSettings[intent.target.displayId] || intent.previousPersistenceSettings);
        if (intent.persistenceLeakageCommit) { delete persistenceLeakageDrafts[intent.target.displayId]; persistenceLeakageErrors[intent.target.displayId] = viewErrorMessage(e); }
        if (sameView(desiredViews[intent.target.displayId], intent.target)) delete desiredViews[intent.target.displayId];
      }
      if (intent.peaksToggle && sameView(desiredViews[intent.target.displayId], intent.target)) {
        var display = displays.filter(function(item) { return item.id === intent.target.displayId; })[0];
        if (display) display.peaksEnabled = intent.previousPeaksEnabled;
        delete desiredViews[intent.target.displayId];
      }
      error(intent.timeLimitsCommit || intent.measurementKindsCommit || intent.spectrumSettingsCommit || intent.spectrogramSettingsCommit || intent.persistenceSettingsCommit ? "" : intent.kind === "reorder" ? (e && e.status === 422 ? "Сервер отклонил порядок Display." : "Не удалось изменить порядок Display.") : (intent.kind === "view" && e && e.status === 422 ? "Сервер отклонил настройки Display." : "Не удалось обновить Display."));
      render();
    }).finally(function() {
      if (intent.peaksToggle && !intentQueue.some(function(next) { return next.peaksToggle && next.target.displayId === intent.target.displayId; })) peaksBusyDisplayId = null;
      if (intent.kind === "reorder") displayReorderPending = false;
      requestInFlight = false;
      loading(false);
      if (!fatalSnapshotError) { render(); if (intent.kind === "reorder") focusDisplayTab(intent.focusDisplayId); drain(); }
    });
  }
  function commitTimeLimits() { var d = active(), minInput = document.querySelector("[data-testid='time-min-input']"), maxInput = document.querySelector("[data-testid='time-max-input']"); if (!d || !minInput || !maxInput || minInput.disabled || maxInput.disabled) return; var minText = minInput.value.trim(), maxText = maxInput.value.trim(), min = Number(minText), max = Number(maxText); timeDrafts[d.id] = { min_s:minInput.value, max_s:maxInput.value }; if (!minText || !maxText || !isFinite(min) || !isFinite(max)) { timeLimitsErrors[d.id] = "Введите конечные значения границ времени в секундах."; render(); return; } var next = { min_s:min, max_s:max, units:"s" }; if (sameTimeLimits(d.time_limits, next)) { delete timeDrafts[d.id]; delete timeLimitsErrors[d.id]; render(); return; } delete timeLimitsErrors[d.id]; update({ time_limits:next }); }
  function bindTimeLimitInputs() { var minInput = document.querySelector("[data-testid='time-min-input']"), maxInput = document.querySelector("[data-testid='time-max-input']"); function captureTimeDraft() { var d = active(); if (!d || !minInput || !maxInput) return; timeDrafts[d.id] = { min_s:minInput.value, max_s:maxInput.value }; delete timeLimitsErrors[d.id]; } [minInput, maxInput].forEach(function(input) { if (!input || input.dataset.timeLimitsBound === "true") return; input.dataset.timeLimitsBound = "true"; input.addEventListener("input", captureTimeDraft); input.addEventListener("change", commitTimeLimits); input.addEventListener("blur", commitTimeLimits); input.addEventListener("keydown", function(e) { if (e.key === "Enter") { e.preventDefault(); commitTimeLimits(); } }); }); }
  function applyDesiredViews() { Object.keys(desiredViews).forEach(function(id) { var d = displays.filter(function(item) { return item.id === id; })[0], target = desiredViews[id]; if (quarantinedDisplay(id)) { delete desiredViews[id]; return; } if (!d || !target) return; d.plot = target.active_plot; d.analysis_signal = target.analysis_signal; d.selected_signal = target.analysis_signal || ""; d.visible_signals = target.visible_signals.slice(); d.time_limits = target.time_limits; d.measurement_kinds = measurementKinds(target.measurement_kinds); d.spectrum_settings = target.spectrum_settings === null ? null : spectrumSettings(target.spectrum_settings); d.spectrogram_settings = spectrogramSettings(target.spectrogram_settings); d.persistence_settings = persistenceSettings(target.persistence_settings); d.peaksEnabled = !!target.peaks_enabled; state.row_selected_signal = target.row_selected_signal; }); }
  function setSelectionQuarantineControls(disabled) { document.querySelectorAll("input, select").forEach(function(control) { if (disabled) { control.disabled = true; control.dataset.selectionQuarantined = "true"; } else if (control.dataset.selectionQuarantined === "true") { control.disabled = false; delete control.dataset.selectionQuarantined; } }); ["find-peaks-action", "clear-display-action"].forEach(function(id) { var control = document.querySelector("[data-testid='" + id + "']"); if (!control) return; if (disabled) { control.disabled = true; control.dataset.selectionQuarantined = "true"; } else if (control.dataset.selectionQuarantined === "true") { control.disabled = false; delete control.dataset.selectionQuarantined; } }); }
  function renderSelectionQuarantine(display) { ++plotRenderGeneration; clearPlotHost(); host.innerHTML = "<div class='plot-placeholder is-error' role='alert' data-testid='display-selection-contract-error-state'>" + esc(selectionContractErrors[display.id]) + "</div>"; host.dataset.plotReady = "false"; setSelectionQuarantineControls(true); renderQuarantineInventory(display); }
  function setActivePlotQuarantineControls(disabled) { document.querySelectorAll("input, select").forEach(function(control) { if (disabled) { control.disabled = true; control.dataset.activePlotQuarantined = "true"; } else if (control.dataset.activePlotQuarantined === "true") { control.disabled = false; delete control.dataset.activePlotQuarantined; } }); ["find-peaks-action", "clear-display-action"].forEach(function(id) { var control = document.querySelector("[data-testid='" + id + "']"); if (!control) return; if (disabled) { control.disabled = true; control.dataset.activePlotQuarantined = "true"; } else if (control.dataset.activePlotQuarantined === "true") { control.disabled = false; delete control.dataset.activePlotQuarantined; } }); }
  function renderQuarantineInventory(display) { var panelFields = document.querySelector("[data-panel-fields]"); if (panelFields) panelFields.innerHTML = ""; document.querySelector("[data-signal-rows]").innerHTML = (state.signals || []).map(function(signal) { var name = String(signal.name || ""), selected = state.row_selected_signal === name, id = testId(name); return "<tr class='signal-row" + (selected ? " is-selected" : "") + "' data-signal='" + esc(name) + "' data-row-selected='" + selected + "' data-testid='signal-row-" + id + "' aria-disabled='true'><td aria-label='Membership unavailable'>—</td><td>" + esc(name) + "</td><td><span class='color-swatch' style='--signal-color:" + esc(signal.color || "#1676e6") + "'></span></td><td>" + esc(signal.sample_rate_hz || "—") + " Hz</td><td>" + esc(signal.sample_count || "—") + "</td><td>" + esc(signal.duration_s || "—") + " s</td><td>" + esc(signal.data_type || "—") + "</td><td>—</td></tr>"; }).join(""); document.querySelector("[data-signal-count]").textContent = (state.signals || []).length + " signals"; document.querySelector("[data-measurements-content]").innerHTML = ""; document.querySelector("[data-peaks-content]").innerHTML = ""; }
  function renderActivePlotQuarantine(display) { ++plotRenderGeneration; clearPlotHost(); host.innerHTML = "<div class='plot-placeholder is-error' role='alert' data-testid='display-active-plot-contract-error-state'>" + esc(activePlotContractErrors[display.id]) + "</div>"; host.dataset.plotReady = "false"; setActivePlotQuarantineControls(true); renderQuarantineInventory(display); }
  function renderActivePlotPayloadQuarantine(display) { ++plotRenderGeneration; clearPlotHost(); host.innerHTML = "<div class='plot-placeholder is-error' role='alert' data-testid='display-active-plot-payload-contract-error-state'>" + esc(activePlotPayloadContractErrors[display.id]) + "</div>"; host.dataset.plotReady = "false"; setActivePlotQuarantineControls(true); renderQuarantineInventory(display); }
  function notifyLayoutModule(display) { if (typeof window.CustomEvent === "function") window.dispatchEvent(new window.CustomEvent("signal-analyser-rendered", { detail:{ activeDisplayId:display.id, revision:state.state_revision } })); }
  function render() { var display = active(); if (!state || !display) return; root.dataset.activeDisplayId = display.id; root.dataset.stateRevision = state.state_revision == null ? "" : String(state.state_revision); root.dataset.rowSelectedSignal = state.row_selected_signal || ""; if (typeof window.CustomEvent === "function") window.dispatchEvent(new window.CustomEvent("signal-analyser-settings-context", { detail:{ displayId:display.id, revision:state.state_revision } })); root.dataset.analysisSignal = display.analysis_signal || ""; root.dataset.peaksEnabled = state.peaks && state.peaks.enabled === true ? "true" : "false"; root.dataset.normalizeY = display.normalizeY ? "true" : "false"; root.dataset.showMarkers = display.showMarkers ? "true" : "false"; host.dataset.normalizeY = root.dataset.normalizeY; host.dataset.showMarkers = root.dataset.showMarkers; renderTabs(); notifyLayoutModule(display); if (selectionContractErrors[display.id]) { setActivePlotQuarantineControls(false); renderSelectionQuarantine(display); return; } setSelectionQuarantineControls(false); if (activePlotContractErrors[display.id]) { renderActivePlotQuarantine(display); return; } if (activePlotPayloadContractErrors[display.id]) { renderActivePlotPayloadQuarantine(display); return; } setActivePlotQuarantineControls(false); renderSettings(); renderStatisticsControls(); renderSpectrogramQuarantine(); renderSignals(); renderSignalsActions(); renderMeasurements(); renderPeaks(); renderBottomTabs(); draw(); }
  function update(change) { if (fatalSnapshotError) return; var d = active(), localUiOnly = Object.keys(change).every(function(k) { return k === "showLegend" || k === "normalizeY" || k === "showMarkers"; }), peaksToggle = Object.prototype.hasOwnProperty.call(change, "peaksEnabled"), timeLimitsCommit = Object.prototype.hasOwnProperty.call(change, "time_limits"), measurementKindsCommit = Object.prototype.hasOwnProperty.call(change, "measurement_kinds"), spectrumSettingsCommit = Object.prototype.hasOwnProperty.call(change, "spectrum_settings"), spectrogramSettingsCommit = Object.prototype.hasOwnProperty.call(change, "spectrogram_settings"), persistenceSettingsCommit = Object.prototype.hasOwnProperty.call(change, "persistence_settings"), spectrumFrequencyLimitsCommit = !!change.spectrumFrequencyLimitsCommit, spectrogramOverlapCommit = !!change.spectrogramOverlapCommit, spectrogramLeakageCommit = !!change.spectrogramLeakageCommit, spectrogramFrequencyLimitsCommit = !!change.spectrogramFrequencyLimitsCommit, spectrogramFrequencyScaleCommit = !!change.spectrogramFrequencyScaleCommit, spectrogramPowerLimitsCommit = !!change.spectrogramPowerLimitsCommit, persistenceLeakageCommit = !!change.persistenceLeakageCommit, previousPeaksEnabled = d.peaksEnabled, previousTimeLimits = d.time_limits, previousMeasurementKinds = measurementKinds(d.measurement_kinds), previousSpectrumSettings = spectrumSettings(d.spectrum_settings), previousSpectrogramSettings = spectrogramSettings(d.spectrogram_settings), previousPersistenceSettings = persistenceSettings(d.persistence_settings); if (quarantinedDisplay(d.id)) { if (!localUiOnly) error(selectionContractErrors[d.id] || activePlotContractErrors[d.id] || activePlotPayloadContractErrors[d.id] || timeLimitsContractErrors[d.id] || measurementKindsContractErrors[d.id] || spectrumSettingsContractErrors[d.id] || spectrogramSettingsContractErrors[d.id] || persistenceSettingsContractErrors[d.id]); render(); return; } delete change.spectrumFrequencyLimitsCommit; delete change.spectrogramOverlapCommit; delete change.spectrogramLeakageCommit; delete change.spectrogramFrequencyLimitsCommit; delete change.spectrogramFrequencyScaleCommit; delete change.spectrogramPowerLimitsCommit; delete change.persistenceLeakageCommit; if (Object.prototype.hasOwnProperty.call(change, "row_selected_signal")) state.row_selected_signal = change.row_selected_signal; if (Object.prototype.hasOwnProperty.call(change, "analysis_signal")) { d.analysis_signal = change.analysis_signal; d.selected_signal = change.analysis_signal || ""; } Object.keys(change).forEach(function(k) { if (k !== "row_selected_signal" && k !== "analysis_signal") d[k] = k === "measurement_kinds" ? measurementKinds(change[k]) : k === "spectrum_settings" ? spectrumSettings(change[k]) : k === "spectrogram_settings" ? spectrogramSettings(change[k]) : k === "persistence_settings" ? persistenceSettings(change[k]) : change[k]; }); if (d.plot !== "time") d.peaksEnabled = false; d.visible_signals = ordered(d.visible_signals); render(); if (localUiOnly) { displayUi[d.id] = { showLegend:d.showLegend, normalizeY:d.normalizeY, showMarkers:d.showMarkers }; return; } var target = viewTarget(d); desiredViews[d.id] = target; enqueue({ kind:"view", target:target, peaksToggle:peaksToggle, timeLimitsCommit:timeLimitsCommit, measurementKindsCommit:measurementKindsCommit, spectrumSettingsCommit:spectrumSettingsCommit, spectrogramSettingsCommit:spectrogramSettingsCommit, persistenceSettingsCommit:persistenceSettingsCommit, spectrumFrequencyLimitsCommit:spectrumFrequencyLimitsCommit, spectrogramOverlapCommit:spectrogramOverlapCommit, spectrogramLeakageCommit:spectrogramLeakageCommit, spectrogramFrequencyLimitsCommit:spectrogramFrequencyLimitsCommit, spectrogramFrequencyScaleCommit:spectrogramFrequencyScaleCommit, spectrogramPowerLimitsCommit:spectrogramPowerLimitsCommit, persistenceLeakageCommit:persistenceLeakageCommit, previousPeaksEnabled:previousPeaksEnabled, previousTimeLimits:previousTimeLimits, previousMeasurementKinds:previousMeasurementKinds, previousSpectrumSettings:previousSpectrumSettings, previousSpectrogramSettings:previousSpectrogramSettings, previousPersistenceSettings:previousPersistenceSettings }); }
  function bindSettingsKeyboard() { var tabs = document.querySelector(".settings-tabs"); if (!tabs || tabs.dataset.keyboardBound === "true") return; tabs.dataset.keyboardBound = "true"; tabs.addEventListener("keydown", function(e) { var items = Array.prototype.slice.call(tabs.querySelectorAll("[data-settings-tab]")), current = items.indexOf(e.target.closest("[data-settings-tab]")); if (current < 0 || ["ArrowLeft", "ArrowRight", "Home", "End"].indexOf(e.key) < 0) return; e.preventDefault(); var target = e.key === "Home" ? items[0] : e.key === "End" ? items[items.length - 1] : items[(current + (e.key === "ArrowRight" ? 1 : items.length - 1)) % items.length]; activeSettingsTab = target.dataset.settingsTab; render(); target.focus(); }); }
  function renderStatisticsControls() { renderSpectrumSettings(); renderSpectrogramSettings(); renderPersistenceSettings(); var d = active(), controls = document.querySelector("[data-testid='statistics-controls']"), errorPanel = document.querySelector("[data-testid='statistics-selection-error']"), contractError = measurementKindsContractErrors[d.id] || "", errorText = contractError || measurementKindsErrors[d.id] || ""; document.querySelectorAll("[data-settings-tab]").forEach(function(tab) { var on = tab.dataset.settingsTab === activeSettingsTab; tab.classList.toggle("is-active", on); tab.setAttribute("aria-selected", on ? "true" : "false"); tab.setAttribute("tabindex", on ? "0" : "-1"); }); document.querySelectorAll("[data-settings-panel]").forEach(function(panel) { panel.hidden = panel.dataset.settingsPanel !== activeSettingsTab; }); document.querySelectorAll("[data-display-settings-actions]").forEach(function(panel) { panel.hidden = activeSettingsTab !== "display"; }); if (!controls) return; controls.querySelectorAll("input[type='checkbox']").forEach(function(input) { input.checked = measurementKinds(d.measurement_kinds).indexOf(input.value) >= 0; input.disabled = !d.analysis_signal || !!contractError; }); errorPanel.hidden = !errorText; errorPanel.textContent = errorText; }
  function load() { var requestId = ++loadRequestId, responseReceived = false; bindWorkspaceBrowser(); bindTimeLimitInputs(); bindStatisticsControls(); bindSpectrumSettings(); bindSpectrumFrequencyLimits(); bindSpectrogramSettings(); bindSpectrogramPowerLimits(); bindPersistenceSettings(); bindSettingsKeyboard(); loading(true, "Загрузка Signal Analyzer…"); error(""); Promise.resolve().then(function() { return api.getState(); }).then(function(s) { var currentRevision; if (requestId !== loadRequestId) return; responseReceived = true; currentRevision = state && typeof state.state_revision === "number" ? state.state_revision : null; if (currentRevision !== null && typeof s.state_revision === "number" && s.state_revision < currentRevision) return; if (accept(s)) { error(""); render(); } }).catch(function(loadError) { if (requestId !== loadRequestId || fatalSnapshotError || (!responseReceived && state)) return; if (responseReceived && window.console && typeof window.console.error === "function") window.console.error("Signal Analyzer render failed.", loadError); error("Не удалось загрузить данные Signal Analyzer."); }).finally(function() { if (requestId === loadRequestId) loading(false); }); }
  function bindStatisticsControls() { var controls=document.querySelector("[data-testid='statistics-controls']"), tabs=document.querySelector(".settings-tabs"); if(tabs&&!tabs.dataset.statisticsBound){tabs.dataset.statisticsBound="true";tabs.addEventListener("click",function(e){var tab=e.target.closest("[data-settings-tab]");if(tab){activeSettingsTab=tab.dataset.settingsTab;render();}});} if(controls&&!controls.dataset.statisticsBound){controls.dataset.statisticsBound="true";controls.addEventListener("change",function(e){if(e.target.closest("input[type='checkbox']")){update({measurement_kinds:Array.prototype.slice.call(controls.querySelectorAll("input[type='checkbox']:checked")).map(function(x){return x.value;})});}});} }  bind(); load();
  function workspaceNode(id) { var node = document.querySelector("[data-testid='" + id + "']") || document.getElementById && document.getElementById(id); if (node && id === "signals-workspace-title" && typeof node.setAttribute === "function") { node.setAttribute("data-testid", id); node.setAttribute("tabindex", "-1"); } return node; }
  function workspaceRaw(entry) { return !!(entry && (entry._requiresSampleRate || entry.sample_rate_requirement === "required")); }
  function workspaceSelected() { var selected = (workspaceBrowser.catalog && workspaceBrowser.catalog.variables || []).filter(function(entry) { return !!workspaceBrowser.selected[entry.variable_id]; }); workspaceSyncA11y(selected); return selected; }
  function workspaceSyncA11y(selected) { var raw = (selected || []).some(workspaceRaw), value = Number(workspaceBrowser.sampleRate), invalid = raw && (workspaceBrowser.sampleRate.trim() === "" || !isFinite(value) || value <= 0), rate = workspaceNode("signals-workspace-sample-rate-input"), list = workspaceNode("signals-workspace-list"); if (rate) { rate.setAttribute("aria-invalid", invalid ? "true" : "false"); rate.setAttribute("aria-describedby", invalid ? "signals-workspace-sample-rate-hint signals-workspace-sample-rate-error" : "signals-workspace-sample-rate-hint"); } if (list) list.setAttribute("aria-busy", workspaceBrowser.loading || workspaceBrowser.busy ? "true" : "false"); }
  function workspaceSampleRateValid() { var selected = (workspaceBrowser.catalog && workspaceBrowser.catalog.variables || []).filter(function(entry) { return !!workspaceBrowser.selected[entry.variable_id]; }), raw = selected.some(workspaceRaw), value = Number(workspaceBrowser.sampleRate); return !raw || (workspaceBrowser.sampleRate.trim() !== "" && isFinite(value) && value > 0); }
  function workspaceFocus(control) { focusLater(control); }
  function workspaceSetOpen(open) { workspaceBrowser.open = !!open; signalsWorkspaceOpen = !!open; var dialog = workspaceNode("signals-workspace-dialog"); if (dialog) dialog.hidden = !open; setSignalsModalBackground(open); }
  function renderWorkspaceBrowser(message) { var dialog = workspaceNode("signals-workspace-dialog"), form = workspaceNode("signals-workspace-form"), list = workspaceNode("signals-workspace-list"), loadingNode = workspaceNode("signals-workspace-loading"), empty = workspaceNode("signals-workspace-empty"), refresh = workspaceNode("signals-workspace-refresh"), count = workspaceNode("signals-workspace-selection-count"), group = workspaceNode("signals-workspace-sample-rate-group"), rate = workspaceNode("signals-workspace-sample-rate-input"), rateError = workspaceNode("signals-workspace-sample-rate-error"), batchError = workspaceNode("signals-workspace-batch-error"), retry = workspaceNode("signals-workspace-retry"), submit = workspaceNode("signals-workspace-submit"), cancel = workspaceNode("signals-workspace-cancel"), close = workspaceNode("signals-workspace-close"), entries = workspaceBrowser.catalog && workspaceBrowser.catalog.variables || [], selected = workspaceSelected(), raw = selected.some(workspaceRaw), invalidRate = raw && !workspaceSampleRateValid(), locked = workspaceBrowser.loading || workspaceBrowser.busy; if (dialog) dialog.hidden = !workspaceBrowser.open; if (form) form.setAttribute("aria-busy", locked ? "true" : "false"); if (loadingNode) loadingNode.hidden = !workspaceBrowser.loading; if (empty) empty.hidden = workspaceBrowser.loading || !!entries.length || !!workspaceBrowser.error; if (list) list.innerHTML = entries.map(function(entry) { var id = entry.variable_id, selectable = entry.selectable === true, disabled = !selectable || locked, checked = !!workspaceBrowser.selected[id], reason = entry.reason || "", compatible = entry.compatibility || "incompatible"; return "<article class='signals-workspace-entry" + (selectable ? "" : " is-incompatible") + "' data-testid='signals-workspace-entry-" + esc(id) + "'><input type='checkbox' id='signals-workspace-select-" + esc(id) + "' data-testid='signals-workspace-select-" + esc(id) + "' data-workspace-variable-id='" + esc(id) + "'" + (checked ? " checked" : "") + (disabled ? " disabled" : "") + (reason ? " aria-describedby='signals-workspace-reason-" + esc(id) + "'" : "") + "><div><label for='signals-workspace-select-" + esc(id) + "' data-testid='signals-workspace-name-" + esc(id) + "'>" + esc(entry.name) + "</label><div class='signals-workspace-metadata'><span data-testid='signals-workspace-type-" + esc(id) + "'>Тип: " + esc(entry.type) + "</span><span data-testid='signals-workspace-shape-" + esc(id) + "'>Размер: " + esc((entry.shape || []).join(" × ")) + "</span><span data-testid='signals-workspace-sample-count-" + esc(id) + "'>Отсчёты: " + esc(entry.sample_count) + "</span><span data-testid='signals-workspace-source-kind-" + esc(id) + "'>Источник: " + esc(entry.source_kind) + "</span><span data-testid='signals-workspace-compatibility-" + esc(id) + "'>" + esc(compatible) + "</span><span data-testid='signals-workspace-sample-rate-requirement-" + esc(id) + "'>" + esc(entry.sample_rate_requirement) + "</span></div>" + (reason ? "<p id='signals-workspace-reason-" + esc(id) + "' data-testid='signals-workspace-reason-" + esc(id) + "'>" + esc(reason) + "</p>" : "") + "</div></article>"; }).join(""); if (count) count.textContent = message || "Выбрано: " + selected.length; if (group) group.hidden = !raw; if (rate) { rate.value = workspaceBrowser.sampleRate; rate.required = raw; rate.disabled = locked; rate.setAttribute("aria-invalid", invalidRate ? "true" : "false"); } if (rateError) { rateError.hidden = !invalidRate; rateError.textContent = invalidRate ? "Введите конечную положительную частоту дискретизации." : ""; } if (batchError) { batchError.hidden = !workspaceBrowser.error; batchError.textContent = workspaceBrowser.error; } if (retry) retry.hidden = !workspaceBrowser.retry; [refresh, retry, cancel, close].forEach(function(control) { if (control) control.disabled = locked; }); if (submit) { submit.disabled = locked || !selected.length || invalidRate; submit.textContent = workspaceBrowser.busy ? "Добавление…" : "Добавить"; } }
  function loadWorkspaceCatalog(resetMessage) { workspaceBrowser.loading = true; workspaceBrowser.busy = false; workspaceBrowser.error = ""; workspaceBrowser.retry = false; workspaceBrowser.catalog = null; workspaceBrowser.selected = {}; renderWorkspaceBrowser(resetMessage); workspaceFocus(workspaceNode("signals-workspace-title")); api.workspaceVariables().then(function(catalog) { (catalog.variables || []).forEach(function(entry) { var raw = entry.sample_rate_requirement === "required"; entry._requiresSampleRate = raw; entry.compatibility = "Совместимость: " + (entry.compatibility === "compatible" ? "Совместимо" : entry.compatibility === "requires_sample_rate" ? "Нужна частота дискретизации" : entry.compatibility === "unsupported" ? "Не поддерживается" : "Несовместимо"); entry.sample_rate_requirement = "Частота дискретизации: " + (raw ? "Требуется" : entry.sample_rate_requirement === "unsupported" || entry.source_kind === "unsupported" ? "Не поддерживается" : "Не требуется"); }); workspaceBrowser.catalog = catalog; workspaceBrowser.loading = false; renderWorkspaceBrowser((resetMessage ? resetMessage + " " : "") + (catalog.truncated ? "Показана только часть каталога: " + catalog.variables.length + " из " + catalog.total + " (лимит 1000)." : "")); var first = document.querySelector("[data-workspace-variable-id]:not([disabled])"); workspaceFocus(first || workspaceNode("signals-workspace-refresh")); }).catch(function(e) { workspaceBrowser.loading = false; workspaceBrowser.catalog = null; workspaceBrowser.selected = {}; workspaceBrowser.error = signalActionErrorMessage(e); workspaceBrowser.retry = true; renderWorkspaceBrowser(); workspaceFocus(workspaceNode("signals-workspace-retry")); }); }
  function openWorkspaceBrowser() { workspaceBrowser.open = true; workspaceBrowser.catalog = null; workspaceBrowser.selected = {}; workspaceBrowser.sampleRate = ""; workspaceBrowser.successCount = 0; signalsFocusRestore = workspaceNode("signals-add-workspace-action"); workspaceSetOpen(true); loadWorkspaceCatalog(); }
  function closeWorkspaceBrowser() { if (workspaceBrowser.loading || workspaceBrowser.busy) return; workspaceSetOpen(false); workspaceBrowser.error = ""; renderWorkspaceBrowser(); focusLater(workspaceNode("signals-add-workspace-action")); }
  function workspaceCatalogFresh() { var catalog = workspaceBrowser.catalog, expires = catalog && Date.parse(catalog.expires_at); return !!(catalog && typeof catalog.catalog_revision === "string" && isFinite(expires) && expires > Date.now()); }
  function workspaceConflictCode(e) { var p = e && e.payload || {}, inner = p.error || {}, code = inner.code || p.code || ""; if (code === "stale_state" && p.current && accept(p.current)) render(); return code === "stale_state" && !workspaceCatalogFresh() ? "stale_workspace_catalog" : code; }
  function submitWorkspaceBatch(replayed, context) { var selected = workspaceSelected(), rate = Number(workspaceBrowser.sampleRate), before = (state.signals || []).length, activeId = context && context.activeId || active() && active().id, catalogRevision = context && context.catalogRevision || workspaceBrowser.catalog && workspaceBrowser.catalog.catalog_revision; if (!selected.length || !workspaceSampleRateValid() || workspaceBrowser.busy || !workspaceBrowser.catalog) { renderWorkspaceBrowser(); return; } workspaceBrowser.busy = true; workspaceBrowser.error = ""; renderWorkspaceBrowser(); api.signals({ state_revision:state.state_revision, operation:"import_workspace_batch", catalog_revision:catalogRevision, selections:selected.map(function(entry) { return { variable_id:entry.variable_id, sample_rate_hz:workspaceRaw(entry) ? rate : null }; }) }).then(function(snapshot) { if (!accept(snapshot)) throw new Error("Некорректный snapshot сервера."); workspaceBrowser.successCount = Math.max(0, (state.signals || []).length - before); workspaceBrowser.open = false; workspaceBrowser.busy = false; workspaceSetOpen(false); var success = workspaceNode("signals-workspace-success"), successCount = workspaceNode("signals-workspace-success-count"); if (success) success.hidden = false; if (successCount) successCount.textContent = "Добавлено: " + workspaceBrowser.successCount; setSignalsModalBackground(true); focusLater(workspaceNode("signals-workspace-done")); render(); }).catch(function(e) { var code = workspaceConflictCode(e), current = e && e.payload && e.payload.current; workspaceBrowser.busy = false; if (code === "stale_state" && !replayed && workspaceCatalogFresh() && current && accept(current) && active() && active().id === activeId && workspaceBrowser.catalog && workspaceBrowser.catalog.catalog_revision === catalogRevision) { return submitWorkspaceBatch(true, { activeId:activeId, catalogRevision:catalogRevision }); } if (code === "stale_workspace_catalog" || code === "workspace_changed") return loadWorkspaceCatalog("Каталог обновлён. Выберите переменные снова."); workspaceBrowser.error = code === "stale_state" ? "Состояние Display изменилось. Повторите добавление." : signalActionErrorMessage(e); renderWorkspaceBrowser(); }); }
  function bindWorkspaceBrowser() { if (!document.addEventListener || document.documentElement && document.documentElement.dataset.workspaceBrowserBound === "true") return; if (document.documentElement) document.documentElement.dataset.workspaceBrowserBound = "true"; document.addEventListener("click", function(e) { var target = e.target, workspaceAdd = target && target.closest && target.closest("[data-testid='signals-add-workspace-action']"), refresh = target && target.closest && target.closest("[data-testid='signals-workspace-refresh'],[data-testid='signals-workspace-retry']"), submit = target && target.closest && target.closest("[data-testid='signals-workspace-submit']"), close = target && target.closest && target.closest("[data-testid='signals-workspace-close'],[data-testid='signals-workspace-cancel']"), done = target && target.closest && target.closest("[data-testid='signals-workspace-done']"); if (workspaceAdd) { e.preventDefault(); e.stopImmediatePropagation(); setSignalsAddMenu(false); openWorkspaceBrowser(); } else if (refresh) { e.preventDefault(); e.stopImmediatePropagation(); loadWorkspaceCatalog("Каталог обновлён. Выберите переменные снова."); } else if (submit) { e.preventDefault(); e.stopImmediatePropagation(); submitWorkspaceBatch(); } else if (close) { e.preventDefault(); e.stopImmediatePropagation(); closeWorkspaceBrowser(); } else if (done) { e.preventDefault(); e.stopImmediatePropagation(); var success = workspaceNode("signals-workspace-success"); if (success) success.hidden = true; setSignalsModalBackground(false); focusLater(workspaceNode("signals-add-workspace-action")); } }, true); document.addEventListener("keydown", function(e) { if (workspaceBrowser.open && (e.key === "Escape" || e.key === "Enter")) { e.preventDefault(); e.stopImmediatePropagation(); } }, true); document.addEventListener("change", function(e) { var input = e.target; if (input && input.dataset && input.dataset.workspaceVariableId) { workspaceBrowser.selected[input.dataset.workspaceVariableId] = !!input.checked; renderWorkspaceBrowser(); } }, true); document.addEventListener("input", function(e) { if (e.target === workspaceNode("signals-workspace-sample-rate-input")) { workspaceBrowser.sampleRate = e.target.value; renderWorkspaceBrowser(); } }, true); }
  function closeSignalsWorkspace() { if (workspaceBrowser && workspaceBrowser.open) return; if (!signalsActionBusy) { signalsWorkspaceOpen = false; signalsWorkspaceAcknowledged = false; signalsWorkspaceSuccessName = ""; signalsActionError = ""; setSignalsModalBackground(false); focusSignalsDismissal(signalsFocusRestore); signalsFocusRestore = null; } renderSignalsActions(); }
  function trapSignalsKeyboard(e) { var success = workspaceNode("signals-workspace-success"), dialog = workspaceBrowser.open ? workspaceNode("signals-workspace-dialog") : success && !success.hidden ? success : null, controls, current; if (dialog) { if (e.key === "Escape" || e.key === "Enter") { e.preventDefault(); e.stopImmediatePropagation(); return; } if (e.key !== "Tab" || typeof dialog.querySelectorAll !== "function") return; controls = Array.prototype.slice.call(dialog.querySelectorAll("input, button")).filter(function(control) { return !control.disabled && !control.hidden; }); if (!controls.length) return; current = controls.indexOf(document.activeElement); if (e.shiftKey && current <= 0) { e.preventDefault(); controls[controls.length - 1].focus(); } else if (!e.shiftKey && current === controls.length - 1) { e.preventDefault(); controls[0].focus(); } return; } if (!signalsDeleteOpen) return; if (e.key === "Escape" && !signalsActionBusy) { e.preventDefault(); closeSignalsDelete(); return; } if (e.key !== "Tab" || signalsActionBusy) return; dialog = workspaceNode("signals-delete-dialog"); if (!dialog || typeof dialog.querySelectorAll !== "function") return; controls = Array.prototype.slice.call(dialog.querySelectorAll("input, button")).filter(function(control) { return !control.disabled && !control.hidden; }); if (!controls.length) return; current = controls.indexOf(document.activeElement); if (e.shiftKey && current <= 0) { e.preventDefault(); controls[controls.length - 1].focus(); } else if (!e.shiftKey && current === controls.length - 1) { e.preventDefault(); controls[0].focus(); } }
  function setSignalsDialogState() { var deletion = workspaceNode("signals-delete-dialog"), errorPanel = workspaceNode("signals-action-error"), errorText = workspaceNode("signals-action-error-text"), selected = selectedSignalName(), count = names().length, acknowledged = signalsDeleteOpen && signalsDeleteAcknowledged; if (deletion) deletion.hidden = !signalsDeleteOpen; if (errorPanel) { errorPanel.hidden = !(signalsDeleteOpen && signalsActionError); if (errorText) errorText.textContent = signalsActionError; if (signalsDeleteOpen && deletion && typeof deletion.querySelector === "function") { var body = deletion.querySelector(".signals-dialog-body"); if (body && errorPanel.parentNode !== body && typeof body.appendChild === "function") body.appendChild(errorPanel); } } ["signals-delete-cancel", "signals-delete-close"].forEach(function(id) { var control = workspaceNode(id); if (control) { control.disabled = signalsActionBusy && signalsActionOperation === "delete"; if (id === "signals-delete-cancel") control.hidden = acknowledged; } }); var confirm = workspaceNode("signals-delete-confirm"), done = workspaceNode("signals-delete-done"), success = workspaceNode("signals-delete-success"), successName = workspaceNode("signals-delete-success-name"), description = document.querySelector("#signals-delete-description"), title = document.querySelector("#signals-delete-title"); if (confirm) { confirm.disabled = acknowledged || (signalsActionBusy && signalsActionOperation === "delete"); confirm.hidden = acknowledged; } if (done) done.hidden = !acknowledged; if (success) success.hidden = !acknowledged; if (successName) successName.textContent = signalsDeleteTarget || ""; if (description) description.hidden = acknowledged; if (title) title.textContent = acknowledged ? "Сигнал удалён" : "Удалить сигнал?"; var name = workspaceNode("signals-delete-name"); if (name) name.textContent = signalsDeleteTarget || ""; var add = workspaceNode("signals-add-action"); if (add) add.disabled = false; }
  function hasVisibleComplexSignal(d) { return (state.signals || []).some(function(signal) { return d.visible_signals.indexOf(signal.name) >= 0 && /комплекс/i.test(String(signal.data_type || "")); }); }
  function renderSpectrumSettings() { var block = document.querySelector("[data-testid='spectrum-settings']"); if (!block) return; var d = active(), contractError = spectrumSettingsContractErrors[d.id] || "", settings = contractError ? null : spectrumSettings(d.spectrum_settings), enabled = d.plot === "spectrum" && !!d.analysis_signal && !contractError, scale = document.querySelector("[data-testid='spectrum-scale-select']"), frequency = document.querySelector("[data-testid='spectrum-frequency-scale-select']"), leakage = document.querySelector("[data-testid='spectrum-leakage-input']"), value = document.querySelector("[data-testid='spectrum-leakage-value']"), message = document.querySelector("[data-testid='spectrum-settings-error']"), minInput = document.querySelector("[data-testid='spectrum-frequency-min-input']"), maxInput = document.querySelector("[data-testid='spectrum-frequency-max-input']"), limitsError = document.querySelector("[data-testid='spectrum-frequency-limits-error']"), payload = d.plot === "spectrum" ? currentPayload("spectrum") : null, effective = payload && payload.frequency_limits && payload.frequency_limits.effective, canonicalLimits = settings && (settings.frequency_limits || effective), draft = spectrumFrequencyLimitsDrafts[d.id], complex = hasVisibleComplexSignal(d), displayedLeakage = settings ? String(settings.leakage) : "", errorText = contractError || spectrumSettingsErrors[d.id] || ""; block.hidden = d.plot !== "spectrum"; if (scale) { scale.value = settings ? settings.scale : ""; scale.disabled = !enabled; } if (frequency) { frequency.value = settings ? settings.frequency_scale : ""; frequency.disabled = !enabled; Array.prototype.slice.call(frequency.options || []).forEach(function(option) { if (option.value === "log") option.disabled = !enabled || complex; }); } if (leakage) { leakage.value = displayedLeakage; leakage.disabled = !enabled; } if (value) value.textContent = displayedLeakage; if (minInput) { minInput.disabled = !enabled; minInput.value = settings && draft ? draft.min_hz : canonicalLimits && canonicalLimits.min_hz != null ? String(canonicalLimits.min_hz) : ""; } if (maxInput) { maxInput.disabled = !enabled; maxInput.value = settings && draft ? draft.max_hz : canonicalLimits && canonicalLimits.max_hz != null ? String(canonicalLimits.max_hz) : ""; } if (limitsError) { limitsError.hidden = !spectrumFrequencyLimitsErrors[d.id]; limitsError.textContent = spectrumFrequencyLimitsErrors[d.id] || ""; } if (message) { message.hidden = !errorText; message.textContent = errorText; } }
  function bindSpectrumSettings() { var scale = document.querySelector("[data-testid='spectrum-scale-select']"), frequency = document.querySelector("[data-testid='spectrum-frequency-scale-select']"), leakage = document.querySelector("[data-testid='spectrum-leakage-input']"); function commit() { var d = active(); if (!d || !scale || !frequency || !leakage || scale.disabled || frequency.disabled || leakage.disabled) return; var next = Object.assign({}, spectrumSettings(d.spectrum_settings), { scale:scale.value, frequency_scale:frequency.value, leakage:Number(leakage.value) }); if (!isFinite(next.leakage) || next.leakage < 0 || next.leakage > 1 || JSON.stringify(spectrumSettings(d.spectrum_settings)) === JSON.stringify(next)) { render(); return; } delete spectrumSettingsErrors[d.id]; update({ spectrum_settings:next }); } [scale, frequency, leakage].forEach(function(control) { if (!control || control.dataset.spectrumBound === "true") return; control.dataset.spectrumBound = "true"; control.addEventListener("change", commit); }); }
  function bindSpectrumFrequencyLimits() { var minInput = document.querySelector("[data-testid='spectrum-frequency-min-input']"), maxInput = document.querySelector("[data-testid='spectrum-frequency-max-input']"); function capture() { var d = active(); if (!d || !minInput || !maxInput) return; spectrumFrequencyLimitsDrafts[d.id] = { min_hz:minInput.value, max_hz:maxInput.value }; delete spectrumFrequencyLimitsErrors[d.id]; } function commit() { var d = active(); if (!d || !minInput || !maxInput || minInput.disabled || maxInput.disabled) return; var minText = minInput.value.trim(), maxText = maxInput.value.trim(), min = Number(minText), max = Number(maxText), settings = spectrumSettings(d.spectrum_settings), next; spectrumFrequencyLimitsDrafts[d.id] = { min_hz:minInput.value, max_hz:maxInput.value }; if (!minText && !maxText) next = Object.assign({}, settings, { frequency_limits:null }); else if (!minText || !maxText || !isFinite(min) || !isFinite(max) || !(min < max)) { delete spectrumFrequencyLimitsDrafts[d.id]; spectrumFrequencyLimitsErrors[d.id] = "Введите две конечные, строго возрастающие границы частоты в Гц."; render(); return; } else next = Object.assign({}, settings, { frequency_limits:{ min_hz:min, max_hz:max, units:"Hz" } }); if (JSON.stringify(next) === JSON.stringify(settings)) { delete spectrumFrequencyLimitsDrafts[d.id]; delete spectrumFrequencyLimitsErrors[d.id]; render(); return; } delete spectrumFrequencyLimitsErrors[d.id]; update({ spectrum_settings:next, spectrumFrequencyLimitsCommit:true }); } [minInput, maxInput].forEach(function(input) { if (!input || input.dataset.spectrumFrequencyLimitsBound === "true") return; input.dataset.spectrumFrequencyLimitsBound = "true"; input.addEventListener("input", capture); input.addEventListener("change", commit); input.addEventListener("blur", commit); input.addEventListener("keydown", function(e) { if (e.key === "Enter") { e.preventDefault(); commit(); } }); }); }
  function mountSpectrogramFrequencyLimits() { var block = document.querySelector("[data-testid='spectrogram-settings']"), controls = document.querySelector("[data-testid='spectrogram-frequency-limits-controls']"), powerControls = document.querySelector("[data-testid='spectrogram-power-limits-controls']"); [controls, powerControls].forEach(function(control) { if (block && control && control.parentNode !== block && typeof block.appendChild === "function") block.appendChild(control); if (control) control.hidden = false; }); }
  function renderSpectrogramSettings() { var block = document.querySelector("[data-testid='spectrogram-settings']"), activeDisplay = active(); if (!block) return; if (!activeDisplay || activeDisplay.plot !== "spectrogram") { block.hidden = true; return; } mountSpectrogramFrequencyLimits(); var d = activeDisplay, input = document.querySelector("[data-testid='spectrogram-overlap-percent-input']"), message = document.querySelector("[data-testid='spectrogram-overlap-percent-error']"), leakage = document.querySelector("[data-testid='spectrogram-leakage-input']"), leakageValue = document.querySelector("[data-testid='spectrogram-leakage-value']"), leakageError = document.querySelector("[data-testid='spectrogram-leakage-error']"), minInput = document.querySelector("[data-testid='spectrogram-frequency-min-input']"), maxInput = document.querySelector("[data-testid='spectrogram-frequency-max-input']"), limitsError = document.querySelector("[data-testid='spectrogram-frequency-limits-error']"), scale = document.querySelector("[data-testid='spectrogram-frequency-scale-select']"), scaleEffective = document.querySelector("[data-testid='spectrogram-frequency-scale-effective']"), scaleError = document.querySelector("[data-testid='spectrogram-frequency-scale-error']"), powerMin = document.querySelector("[data-testid='spectrogram-power-min-input']"), powerMax = document.querySelector("[data-testid='spectrogram-power-max-input']"), powerEffective = document.querySelector("[data-testid='spectrogram-power-limits-effective']"), powerError = document.querySelector("[data-testid='spectrogram-power-limits-error']"), enabled = !!d.analysis_signal, draft = spectrogramOverlapDrafts[d.id], leakageDraft = spectrogramLeakageDrafts[d.id], limitsDraft = spectrogramFrequencyLimitsDrafts[d.id], powerDraft = spectrogramPowerLimitsDrafts[d.id], settings = spectrogramSettings(d.spectrogram_settings), payload = currentPayload("spectrogram"), effective = payload.frequency_limits && payload.frequency_limits.effective, canonicalLimits = settings.frequency_limits || effective, powerMeta = spectrogramPowerMetadata(payload), power = settings.power_limits, displayedLeakage = leakageDraft !== undefined ? leakageDraft : String(settings.leakage), scaleMeta = payload.frequency_scale || {}, effectiveScale = enabled ? (scaleMeta.effective || "linear") : null; block.hidden = !enabled; if (input) { input.disabled = !enabled; input.value = draft !== undefined ? draft : String(settings.overlap_percent); input.setAttribute("aria-invalid", spectrogramOverlapErrors[d.id] ? "true" : "false"); } if (message) { message.hidden = !spectrogramOverlapErrors[d.id]; message.textContent = spectrogramOverlapErrors[d.id] || ""; } if (leakage) { leakage.disabled = !enabled; leakage.value = displayedLeakage; leakage.setAttribute("aria-invalid", spectrogramLeakageErrors[d.id] ? "true" : "false"); } if (leakageValue) leakageValue.textContent = displayedLeakage; if (leakageError) { leakageError.hidden = !spectrogramLeakageErrors[d.id]; leakageError.textContent = spectrogramLeakageErrors[d.id] || ""; } if (minInput) { minInput.disabled = !enabled; minInput.value = limitsDraft ? limitsDraft.min_hz : canonicalLimits && canonicalLimits.min_hz != null ? String(canonicalLimits.min_hz) : ""; minInput.setAttribute("aria-invalid", spectrogramFrequencyLimitsErrors[d.id] ? "true" : "false"); } if (maxInput) { maxInput.disabled = !enabled; maxInput.value = limitsDraft ? limitsDraft.max_hz : canonicalLimits && canonicalLimits.max_hz != null ? String(canonicalLimits.max_hz) : ""; maxInput.setAttribute("aria-invalid", spectrogramFrequencyLimitsErrors[d.id] ? "true" : "false"); } if (limitsError) { limitsError.hidden = !spectrogramFrequencyLimitsErrors[d.id]; limitsError.textContent = spectrogramFrequencyLimitsErrors[d.id] || ""; } if (scale) { scale.value = settings.frequency_scale; scale.disabled = !enabled || !Array.isArray(scaleMeta.available) || scaleMeta.available.length < 2; } if (scaleEffective) scaleEffective.textContent = effectiveScale === "log" ? "Log" : effectiveScale === "linear" ? "Linear" : ""; if (scaleError) { scaleError.hidden = !spectrogramFrequencyScaleErrors[d.id]; scaleError.textContent = spectrogramFrequencyScaleErrors[d.id] || ""; } if (powerMin) { powerMin.disabled = !enabled; powerMin.value = powerDraft ? powerDraft.min_db : power ? String(power.min_db) : ""; powerMin.setAttribute("aria-invalid", spectrogramPowerLimitsErrors[d.id] ? "true" : "false"); } if (powerMax) { powerMax.disabled = !enabled; powerMax.value = powerDraft ? powerDraft.max_db : power ? String(power.max_db) : ""; powerMax.setAttribute("aria-invalid", spectrogramPowerLimitsErrors[d.id] ? "true" : "false"); } if (powerEffective) powerEffective.textContent = powerMeta.valid && powerMeta.effective ? "Effective: " + powerMeta.effective.min_db + "…" + powerMeta.effective.max_db + " dB" : powerMeta.valid ? "Effective: —" : "Effective: invalid metadata"; if (powerError) { powerError.hidden = !spectrogramPowerLimitsErrors[d.id]; powerError.textContent = spectrogramPowerLimitsErrors[d.id] || ""; } }
  function bindSpectrogramSettings() { mountSpectrogramFrequencyLimits(); var input = document.querySelector("[data-testid='spectrogram-overlap-percent-input']"), leakage = document.querySelector("[data-testid='spectrogram-leakage-input']"), leakageValue = document.querySelector("[data-testid='spectrogram-leakage-value']"), minInput = document.querySelector("[data-testid='spectrogram-frequency-min-input']"), maxInput = document.querySelector("[data-testid='spectrogram-frequency-max-input']"), scale = document.querySelector("[data-testid='spectrogram-frequency-scale-select']"); function capture() { var d = active(); if (!d || !input) return; spectrogramOverlapDrafts[d.id] = input.value; delete spectrogramOverlapErrors[d.id]; } function commit() { var d = active(); if (!d || !input || input.disabled) return; var text = input.value.trim(), value = Number(text), settings = spectrogramSettings(d.spectrogram_settings), next; if (!text || !isFinite(value) || value < 0 || value > 75) { delete spectrogramOverlapDrafts[d.id]; spectrogramOverlapErrors[d.id] = "Введите конечное значение Overlap от 0 до 75%."; render(); return; } next = Object.assign({}, settings, { overlap_percent:value }); if (JSON.stringify(next) === JSON.stringify(settings)) { delete spectrogramOverlapDrafts[d.id]; delete spectrogramOverlapErrors[d.id]; render(); return; } delete spectrogramOverlapErrors[d.id]; update({ spectrogram_settings:next, spectrogramOverlapCommit:true }); } function captureLeakage() { var d = active(); if (!d || !leakage) return; spectrogramLeakageDrafts[d.id] = leakage.value; delete spectrogramLeakageErrors[d.id]; if (leakageValue) leakageValue.textContent = leakage.value; } function commitLeakage() { var d = active(); if (!d || !leakage || leakage.disabled) return; var text = leakage.value.trim(), value = Number(text), settings = spectrogramSettings(d.spectrogram_settings), next; if (!text || !isFinite(value) || value < 0 || value > 1) { delete spectrogramLeakageDrafts[d.id]; spectrogramLeakageErrors[d.id] = "Введите конечное значение Leakage от 0 до 1."; render(); return; } next = Object.assign({}, settings, { leakage:value }); if (JSON.stringify(next) === JSON.stringify(settings)) { delete spectrogramLeakageDrafts[d.id]; delete spectrogramLeakageErrors[d.id]; render(); return; } delete spectrogramLeakageErrors[d.id]; update({ spectrogram_settings:next, spectrogramLeakageCommit:true }); } function captureLimits() { var d = active(); if (!d || !minInput || !maxInput) return; spectrogramFrequencyLimitsDrafts[d.id] = { min_hz:minInput.value, max_hz:maxInput.value }; delete spectrogramFrequencyLimitsErrors[d.id]; } function commitLimits() { var d = active(); if (!d || !minInput || !maxInput || minInput.disabled || maxInput.disabled) return; var minText = minInput.value.trim(), maxText = maxInput.value.trim(), min = Number(minText), max = Number(maxText), settings = spectrogramSettings(d.spectrogram_settings), next; spectrogramFrequencyLimitsDrafts[d.id] = { min_hz:minInput.value, max_hz:maxInput.value }; if (!minText && !maxText) next = Object.assign({}, settings, { frequency_limits:null }); else if (!minText || !maxText || !isFinite(min) || !isFinite(max) || !(min < max)) { delete spectrogramFrequencyLimitsDrafts[d.id]; spectrogramFrequencyLimitsErrors[d.id] = "Введите две конечные, строго возрастающие границы частоты в Гц."; render(); return; } else next = Object.assign({}, settings, { frequency_limits:{ min_hz:min, max_hz:max, units:"Hz" } }); if (JSON.stringify(next) === JSON.stringify(settings)) { delete spectrogramFrequencyLimitsDrafts[d.id]; delete spectrogramFrequencyLimitsErrors[d.id]; render(); return; } delete spectrogramFrequencyLimitsErrors[d.id]; update({ spectrogram_settings:next, spectrogramFrequencyLimitsCommit:true }); } if (input && input.dataset.spectrogramOverlapBound !== "true") { input.dataset.spectrogramOverlapBound = "true"; input.addEventListener("input", capture); input.addEventListener("change", commit); input.addEventListener("blur", commit); input.addEventListener("keydown", function(e) { if (e.key === "Enter") { e.preventDefault(); commit(); } }); } if (leakage && leakage.dataset.spectrogramLeakageBound !== "true") { leakage.dataset.spectrogramLeakageBound = "true"; leakage.addEventListener("input", captureLeakage); leakage.addEventListener("change", commitLeakage); } if (scale && scale.dataset.spectrogramFrequencyScaleBound !== "true") { scale.dataset.spectrogramFrequencyScaleBound = "true"; scale.addEventListener("change", function() { var d = active(), settings = spectrogramSettings(d.spectrogram_settings), next; if (!d || scale.disabled) return; next = Object.assign({}, settings, { frequency_scale:scale.value }); if (JSON.stringify(next) === JSON.stringify(settings)) return; delete spectrogramFrequencyScaleErrors[d.id]; update({ spectrogram_settings:next, spectrogramFrequencyScaleCommit:true }); }); } var limitsControls = document.querySelector("[data-testid='spectrogram-frequency-limits-controls']"); if (limitsControls && limitsControls.dataset.spectrogramFrequencyLimitsBound !== "true") { limitsControls.dataset.spectrogramFrequencyLimitsBound = "true"; [minInput, maxInput].forEach(function(control) { if (!control) return; control.addEventListener("input", captureLimits); control.addEventListener("keydown", function(e) { if (e.key === "Enter") { e.preventDefault(); commitLimits(); } }); }); limitsControls.addEventListener("focusout", function(e) { if (e.relatedTarget && typeof limitsControls.contains === "function" && limitsControls.contains(e.relatedTarget)) return; window.setTimeout(function() { if (!limitsControls.contains(document.activeElement)) commitLimits(); }, 0); }); } }
  function bindSpectrogramPowerLimits() { var controls = document.querySelector("[data-testid='spectrogram-power-limits-controls']"), minInput = document.querySelector("[data-testid='spectrogram-power-min-input']"), maxInput = document.querySelector("[data-testid='spectrogram-power-max-input']"); if (!controls || !minInput || !maxInput || controls.dataset.spectrogramPowerLimitsBound === "true") return; controls.dataset.spectrogramPowerLimitsBound = "true"; function capture() { var d = active(); if (!d) return; spectrogramPowerLimitsDrafts[d.id] = { min_db:minInput.value, max_db:maxInput.value }; delete spectrogramPowerLimitsErrors[d.id]; } function commit() { var d = active(); if (!d || minInput.disabled || maxInput.disabled) return; var minText = minInput.value.trim(), maxText = maxInput.value.trim(), min = Number(minText), max = Number(maxText), settings = spectrogramSettings(d.spectrogram_settings), next; spectrogramPowerLimitsDrafts[d.id] = { min_db:minInput.value, max_db:maxInput.value }; if (!minText && !maxText) next = Object.assign({}, settings, { power_limits:null }); else if (!minText || !maxText || !isFinite(min) || !isFinite(max) || !(min < max)) { delete spectrogramPowerLimitsDrafts[d.id]; spectrogramPowerLimitsErrors[d.id] = "Введите две конечные, строго возрастающие границы мощности в дБ."; render(); return; } else next = Object.assign({}, settings, { power_limits:{ min_db:min, max_db:max, units:"dB" } }); if (JSON.stringify(next) === JSON.stringify(settings)) { delete spectrogramPowerLimitsDrafts[d.id]; delete spectrogramPowerLimitsErrors[d.id]; render(); return; } delete spectrogramPowerLimitsErrors[d.id]; update({ spectrogram_settings:next, spectrogramPowerLimitsCommit:true }); } [minInput, maxInput].forEach(function(control) { control.addEventListener("input", capture); control.addEventListener("keydown", function(e) { if (e.key === "Enter") { e.preventDefault(); commit(); } }); }); controls.addEventListener("focusout", function(e) { if (e.relatedTarget && controls.contains(e.relatedTarget)) return; window.setTimeout(function() { if (!controls.contains(document.activeElement)) commit(); }, 0); }); }
  function mountPersistenceSettings() { var block = document.querySelector("[data-testid='display-settings-panel']"), controls = document.querySelector("[data-testid='persistence-settings']"); if (block && controls && controls.parentNode !== block && typeof block.appendChild === "function") block.appendChild(controls); }
  function renderPersistenceSettings() { mountPersistenceSettings(); var d = active(), block = document.querySelector("[data-testid='persistence-settings']"), leakage = document.querySelector("[data-testid='persistence-leakage-input']"), value = document.querySelector("[data-testid='persistence-leakage-value']"), message = document.querySelector("[data-testid='persistence-leakage-error']"), contractError = persistenceSettingsContractErrors[d.id] || "", enabled = d.plot === "persistence" && !!d.analysis_signal && !contractError, settings = persistenceSettings(d.persistence_settings), draft = persistenceLeakageDrafts[d.id], displayed = contractError ? "" : draft !== undefined ? draft : settings ? String(settings.leakage) : "", errorText = contractError || persistenceLeakageErrors[d.id] || ""; if (!block) return; block.hidden = d.plot !== "persistence"; if (leakage) { leakage.disabled = !enabled; leakage.value = displayed; leakage.setAttribute("aria-invalid", errorText ? "true" : "false"); leakage.setAttribute("aria-label", contractError ? "Настройки Persistence содержат ошибку контракта" : enabled ? "Leakage активного Persistence Display" : "Leakage доступен после добавления сигнала в Persistence Display"); } if (value) value.textContent = displayed; if (message) { message.hidden = !errorText; message.textContent = errorText; } }
  function bindPersistenceSettings() { var leakage = document.querySelector("[data-testid='persistence-leakage-input']"); if (!leakage || leakage.dataset.persistenceLeakageBound === "true") return; leakage.dataset.persistenceLeakageBound = "true"; leakage.addEventListener("input", function() { var d = active(); if (!d) return; persistenceLeakageDrafts[d.id] = leakage.value; delete persistenceLeakageErrors[d.id]; render(); }); leakage.addEventListener("change", function() { var d = active(), text, value, settings, next; if (!d || leakage.disabled) return; text = leakage.value.trim(); value = Number(text); persistenceLeakageDrafts[d.id] = leakage.value; if (!text || !isFinite(value) || value < 0 || value > 1) { delete persistenceLeakageDrafts[d.id]; persistenceLeakageErrors[d.id] = "Введите конечное значение Leakage от 0 до 1."; render(); return; } next = persistenceSettings({ leakage:value }); settings = persistenceSettings(d.persistence_settings); if (JSON.stringify(next) === JSON.stringify(settings)) { delete persistenceLeakageDrafts[d.id]; delete persistenceLeakageErrors[d.id]; render(); return; } delete persistenceLeakageErrors[d.id]; update({ persistence_settings:next, persistenceLeakageCommit:true }); }); }
  function renderSpectrogramQuarantine() { var d = active(), message = spectrogramSettingsContractErrors[d.id] || "", block = document.querySelector("[data-testid='spectrogram-settings']"), contract = document.querySelector("[data-testid='spectrogram-settings-contract-error']"); if (!message) { if (contract) contract.hidden = true; return; } if (block && contract && contract.parentNode !== block && typeof block.appendChild === "function") block.appendChild(contract); if (block) block.hidden = d.plot !== "spectrogram"; ["spectrogram-overlap-percent-input", "spectrogram-leakage-input", "spectrogram-frequency-min-input", "spectrogram-frequency-max-input", "spectrogram-frequency-scale-select", "spectrogram-power-min-input", "spectrogram-power-max-input"].forEach(function(id) { var control = document.querySelector("[data-testid='" + id + "']"); if (control) { control.disabled = true; control.value = ""; } }); var output = document.querySelector("[data-testid='spectrogram-leakage-value']"); if (output) output.textContent = ""; if (contract) { contract.hidden = false; contract.textContent = message; } }
})(window, document);
